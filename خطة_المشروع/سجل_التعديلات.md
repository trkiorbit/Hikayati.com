# سجل التعديلات والأخطاء — حكواتي
**آخر تحديث:** 2026-04-15
**ملاحظة:** هذا الملف يجمع محتوى `تعديلات_كلاو.md` + الأخطاء الجديدة في مكان واحد. أي تغيير يُسجَّل هنا.

---

## الأخطاء والمشاكل المعروفة

### خطأ 1 — عدم تطابق الألوان
| البند | التفاصيل |
|---|---|
| الوصف | `app_colors.dart` يستخدم `#6A0DAD` بينما المواصفات النهائية تحدد `#2D0B5A` |
| الملف | `lib/core/theme/app_colors.dart` |
| التأثير | هوية بصرية غير متطابقة مع المواصفات النهائية المعتمدة |
| الدفعة | 5 |
| الحالة | مفتوح |

---

### خطأ 2 — Fallback مُرمَّز في منطق الرصيد
| البند | التفاصيل |
|---|---|
| الوصف | بعض الأماكن تستخدم `?? 100` عند قراءة الرصيد من Supabase مما قد يُخفي bug حقيقي |
| الملف | `lib/application/use_cases/generate_story_use_case.dart` |
| التأثير | المستخدم قد يرى رصيدًا خاطئًا ويستطيع التوليد بدون رصيد فعلي |
| الدفعة | 3 |
| الحالة | مفتوح |

---

### خطأ 3 — تعدد مصادر الأفاتار
| البند | التفاصيل |
|---|---|
| الوصف | منطق الأفاتار موزع بين avatar_vision_service وavatar_service والـ use case بطريقة يمكن أن تنتج تعارض |
| الملفات | `lib/features/avatar_lab/services/avatar_vision_service.dart` + `avatar_service.dart` + `save_avatar_use_case.dart` |
| التأثير | تخبيط في استرداد بيانات الشخصية وحقن الـ promptSnippet في محرك القصة |
| الدفعة | 1 |
| الحالة | مفتوح |

---

### خطأ 4 — المتجر placeholder فقط
| البند | التفاصيل |
|---|---|
| الوصف | زر "الشحن" في الـ Drawer يفتح SnackBar "قريبًا" فقط، وقسم المتجر في HomeScreen بدون أي route |
| الملف | `lib/features/home/screens/home_screen.dart` (حوالي سطر 420) |
| التأثير | المستخدم لا يستطيع شراء جواهر أو الوصول للمتجر |
| الدفعة | 6 |
| الحالة | مفتوح |

---

### خطأ 5 — صفحات قانونية stub بدون محتوى
| البند | التفاصيل |
|---|---|
| الوصف | `privacy_policy_screen.dart` و `terms_of_service_screen.dart` موجودتان لكن بدون محتوى حقيقي |
| الملف | `lib/features/legal/screens/` |
| التأثير | رفض محتمل من App Store وGoogle Play — مطلوب رابط عام فعال |
| الدفعة | 7 (قبل الرفع) |
| الحالة | مفتوح |

---

### خطأ 6 — Supabase Edge Function غير مستخدمة
| البند | التفاصيل |
|---|---|
| الوصف | `supabase/functions/ai-assistant/index.ts` (Anthropic claude-sonnet) موجودة لكن لا يستدعيها أي كود في التطبيق |
| الملف | `supabase/functions/ai-assistant/index.ts` |
| التأثير | موارد غير مستغلة، وتكلفة محتملة عند النشر |
| القرار المطلوب | إما ربطها بحكيم بدلاً من Pollinations، أو حذفها |
| الحالة | مؤجل — ينتظر قرار تروك |

---

### خطأ 7 — تناقض في تسعير الأفاتار والصوت
| البند | التفاصيل |
|---|---|
| الوصف | `hakeem_service.dart` يخبر المستخدم أن الأفاتار والصوت بـ 20 جوهر، لكن `generate_story_use_case.dart` يحسب صفر |
| الملفات | `lib/features/hakeem/services/hakeem_service.dart` + `lib/application/use_cases/generate_story_use_case.dart` |
| التأثير | تناقض في معلومات المستخدم — خطر شكاوى وعدم ثقة |
| الحالة | معلّق — ينتظر قرار تروك |

---

### خطأ 8 — Google Sign-In قد يفشل على iOS
| البند | التفاصيل |
|---|---|
| الوصف | `auth_service.dart` يحتوي `clientId: null` في بعض الإعدادات |
| الملف | `lib/core/auth/auth_service.dart` |
| التأثير | تسجيل الدخول قد يفشل على iOS بدون clientId صحيح |
| الدفعة | 7 (قبل الإطلاق) |
| الحالة | مفتوح |

---

### خطأ 9 — عدم وجود سياسة الأطفال (COPPA)
| البند | التفاصيل |
|---|---|
| الوصف | التطبيق يستهدف الأطفال ويجمع بيانات (صور، أصوات) لكن لا توجد سياسة COPPA/Children Privacy |
| التأثير | رفض مؤكد من Apple وGoogle Play بدون هذه السياسة |
| الدفعة | 7 (قبل الرفع) |
| الحالة | مفتوح |

---

## سجل القرارات المعتمدة

| التاريخ | القرار | المرجع |
|---|---|---|
| 2026-04-15 | الأفاتار بالاستخراج فقط — ممنوع إدخال يدوي للعمر/الجنس/اللون | تروك |
| 2026-04-15 | السينما عرض فقط — لا API calls عند الإعادة، يقرأ `audio_url` المحفوظ | تروك |
| 2026-04-15 | الصوت العادي = Pollinations، المستنسخ = ElevenLabs فقط | تروك |
| 2026-04-15 | الخصم بعد النجاح الكامل (نص + صور + صوت + حفظ) فقط | تروك |
| 2026-04-15 | الحد الأقصى 10 قصص خاصة لكل مستخدم | تروك |
| 2026-04-15 | الألوان النهائية: #2D0B5A + #FF8C00 + Cairo/Tajawal | تروك |
| 2026-04-15 | IAP إجباري للجواهر الرقمية (Apple StoreKit + Google Play Billing) | Apple/Google Policy |
| 2026-04-15 | المنتجات المادية = متجر ويب خارجي (ليس IAP) | Apple/Google Policy |
| 2026-04-15 | لا استدعاء AI من الشاشات مباشرة — كل شيء عبر Use Cases | تروك (معمارية) |

---

## قرارات معلّقة تنتظر تروك

| الموضوع | الخيارات | الملفات المتأثرة |
|---|---|---|
| تكلفة استنساخ الصوت | 0 جوهر أو 20 جوهر | generate_story_use_case.dart + hakeem_service.dart |
| تكلفة إنشاء الأفاتار | 0 جوهر أو 20 جوهر | save_avatar_use_case.dart + hakeem_service.dart |
| Edge Function | ربطها بحكيم أو حذفها | supabase/functions/ai-assistant/index.ts |

---

## التعديلات من جلسة 2026-04-15

### ما تم إنجازه في الجلسة:
- مراجعة شاملة لـ 44 ملف Dart (6,156 سطر)
- رصد 9 أخطاء وتوثيقها
- تأكيد 7 دفعات كخطة إطلاق نهائية مع تحديد الملفات لكل دفعة
- توثيق قواعد المعمارية الثابتة (6 قواعد)
- توثيق بروتوكول العمل مع تروك
- إنشاء مجلد `خطة_المشروع/` بملفين: الخطة الشاملة + هذا السجل

### ما لم يُنجز (مؤجل للجلسات التالية):
- تنفيذ الدفعات 2-7
- الحصول على قرار تروك بشأن تسعير الأفاتار والصوت

---

### تعديل 2026-04-15 — الدفعة 1: إقفال الأفاتار ✅

**ما تم:**
- `avatar_service.dart`: تصحيح اسم العمود من `avatar_data` إلى `avatar_profile_summary` (كان يحفظ في العمود الخاطئ)
- `save_avatar_use_case.dart`: إعادة كتابة كاملة — يعالج الآن خصم الكريدت (20 جوهرة) + يحسب ويحفظ `prompt_snippet` للحقن في محرك القصة + إضافة `changeClothes()` (5 جواهر)
- `avatar_lab_screen.dart`: حذف الاستدعاء المباشر لـ `SupabaseService.deductCredits()` و Supabase من الشاشة — توجيه `_saveAvatar()` و `_changeClothes()` عبر `SaveAvatarUseCase` (تطبيق قاعدة المعمارية)
- `generate_story_use_case.dart`: يستخدم الآن `prompt_snippet` المحفوظ مباشرةً عند إنشاء القصة بدلاً من إعادة بنائه
- `avatar_vision_service.dart`: حذف `import 'dart:io'` غير المستخدم
- بناء APK نجح ✅

**الحالة:** أُغلق — معايير الإقفال مكتملة
**ملاحظة:** قرار تسعير الأفاتار لا يزال معلّقاً من تروك (20 جوهرة مبدئياً في `SaveAvatarUseCase.avatarCost`)

---

## التعديلات السابقة (من سجل Git)

| الـ Commit | الوصف | التاريخ التقريبي |
|---|---|---|
| `627e6cc` | تنظيف المشروع، تطبيق gitignore صارم | قبل 2026-04-15 |
| `0e08d71` | الانتهاء من المحركات الأساسية | قبل 2026-04-15 |
| `dd06e32` | ضبط حكيم والمكتبة الخاصة واستقرار الصوت | قبل 2026-04-15 |
| `5164e71` | تحسين جودة التسجيل، إلغاء الضوضاء، كاش الصوت | قبل 2026-04-15 |
| `32bca97` | نجاح الأفاتار وربطه بالقصة | قبل 2026-04-15 |

---

## تتبع حالة الدفعات

| # | الدفعة | الحالة | الملف الرئيسي |
|---|---|---|---|
| 1 | إقفال الأفاتار | مكتمل ✅ | avatar_vision_service.dart |
| 2 | إقفال الصوت | مكتمل ✅ | generate_story_use_case.dart |
| 3 | إقفال الرصيد | مكتمل ✅ | generate_story_use_case.dart |
| 4 | إقفال المكتبات | جزئي | private_library_screen.dart |
| 5 | توحيد الهوية | جزئي | app_colors.dart + app_theme.dart |
| 6 | بناء المتجر | جزئي ✅ | store_screen.dart (نسخة أولى) |
| 7 | إطلاق التطبيق | لم يبدأ | App Store Connect + Play Console |
| — | إغلاق طبقة المنتج | مكتمل ✅ | رصيد + دخول + حساب + قانوني + موافقات + أزرار |

---

### تعديل 2026-04-18 — إغلاق طبقة المنتج (Product Closure) ✅

**A) إصلاح عرض الرصيد:**
- `home_screen.dart`: استُبدل StreamBuilder بـ setState + one-shot fetch + stream listener
- الرصيد يظهر فورًا عند تحميل الصفحة (لا initialData: 0)
- يتحدث لحظيًا بعد أي خصم عبر Supabase Realtime

**B) تدفق تسجيل الدخول:**
- Router يعيد التوجيه لـ `/login` إذا لا يوجد session (كان يعمل أصلاً)
- `login_screen.dart`: ألوان محدّثة من AppColors بدل hardcoded

**C) صفحة الحساب الشخصي (جديد):**
- `lib/features/profile/screens/profile_screen.dart`
- تعرض: البريد، الرصيد، حالة الأفاتار، حالة الصوت، عدد القصص
- أزرار: شحن، خصوصية، شروط، محتوى، حذف بيانات، تسجيل خروج

**D) الصفحات القانونية (4 صفحات كاملة):**
- `privacy_policy_screen.dart`: أُعيد كتابتها بالكامل (7 أقسام)
- `terms_of_service_screen.dart`: أُعيد كتابتها بالكامل (8 أقسام)
- `data_deletion_screen.dart`: جديد (6 أقسام)
- `content_policy_screen.dart`: جديد (5 أقسام)

**E) شاشات الموافقة الصريحة (جديد):**
- `avatar_lab_screen.dart`: dialog موافقة مع checkbox قبل تحليل الصورة
- `voice_clone_screen.dart`: dialog موافقة مع checkbox قبل بدء التسجيل

**F) ربط الأزرار:**
- Person icon → `/profile`
- Wallet icon → `/store`
- Store icon → `/store`
- "الشحن" → `/store`
- "المتجر" → `/store`
- "سياسة الخصوصية" → `/privacy-policy`
- "الشروط" → `/terms`
- "حسابي" → `/profile`

**G) شاشة المتجر (جديد):**
- `lib/features/store/screens/store_screen.dart`
- رصيد حالي + 4 باقات (50/100/250/500) + قسم "منتجات قريبًا"

**H-I) توحيد النصوص والهوية:**
- جميع الصفحات الجديدة تستخدم AppColors + نفس الثيم
- لا placeholders، لا debug labels، نبرة عربية احترافية موحدة

---

### تعديل 2026-04-16 — إصلاح Blocker الخطوط + الدفعة 2 + app_theme

### تعديل 2026-04-16 — الدفعة 3: إقفال الرصيد ✅

**ما تم:**
- `lib/application/use_cases/generate_story_use_case.dart`:
  - أضيف `static final Set<String> _deductedStoryIds = {}` لمنع الخصم المزدوج في نفس الجلسة
  - كتلة الخصم تقرأ الرصيد من Supabase **قبل** و**بعد** الخصم مباشرةً
  - اللوق بالشكل المطلوب: `[CREDIT] before=X`, `deducted=X`, `after=X`, `story_id=X`
  - الخصم يأتي فقط بعد: نص ✅ + صور ✅ + صوت ✅ + حفظ في Supabase ✅
  - `?? 0` في قراءة الرصيد (لا `?? 100` في أي مكان)
- `lib/features/home/screens/home_screen.dart`: **لا تغيير مطلوب** — يستخدم Supabase Realtime stream يتحدث تلقائياً عند تغيير الرصيد في DB
- `flutter analyze`: 0 أخطاء ✅

**كيف يمنع الخصم المزدوج:**
- `_isGenerating` lock: يمنع تشغيل `execute()` مرتين في نفس الوقت (race condition)
- `_deductedStoryIds`: يحفظ معرّفات القصص المخصوم عنها — إذا استُدعي الخصم لنفس القصة مرة ثانية (بأي سبب) يطبع تحذيراً ويتجاوز

**كيف يظهر الرصيد من Supabase:**
- `HomeScreen.initState()` ينشئ stream من `profiles.stream(primaryKey: ['user_id'])` مرة واحدة فقط
- Supabase Realtime يدفع التحديث فور تغيير الرصيد في DB → `StreamBuilder` يعيد بناء الـ AppBar فوراً

---

### تعديل 2026-04-16 — إصلاح Blocker الخطوط + الدفعة 2 + app_theme

**ما تم:**
- `pubspec.yaml`: حذف `fonts:` section اليدوية (الملفات غير موجودة) → **blocker محلول**
- `lib/core/theme/app_theme.dart`: تحويل `fontFamily: 'Cairo'/'Tajawal'` إلى `GoogleFonts.cairo()/.tajawal()` ← يستخدم google_fonts المثبّت
- `lib/core/theme/app_theme.dart`: تصحيح `CardTheme` → `CardThemeData`، `DialogTheme` → `DialogThemeData` (Flutter API)
- `lib/core/theme/app_colors.dart`: إضافة `deepBlack` alias لـ `deepNight` ← كان مفقوداً في story_creation_screen + voice_clone_screen
- `lib/application/use_cases/generate_story_use_case.dart`: إصلاح ترتيب TTS — Pollinations أولاً للصوت العادي (حذف منطق OpenAI/Azure)
- `flutter analyze`: 0 أخطاء ✅ (35 info فقط)

**الحالة الجديدة:**
- الدفعة 2 (الصوت): مكتملة ✅
- الدفعة 5 (الهوية): جزئي (الألوان + الخطوط جاهزة، الشاشات لا تزال تحتوي ألوان hardcoded)

---

### تعديل الإغلاق — نقطة استعادة: قبل الاطلاق و الدفع جاهز ✅
**ما تم:**
- إنشاء نقطة استعادة نظيفة في Git ورفعها إلى المستودع.
- إقفال مسار الصوت (استخدام `audio_url` المحفوظ فقط في السينما بدون TTS مباشر).
- إقفال حد المكتبة الخاصة (10 قصص، منع التوليد مع توجيه للحذف عند الامتلاء).
- إصلاح فلتر المحتوى (استخدام Regex للكلمة الكاملة لمنع الإنذارات الكاذبة).
- إصلاح جلسة تسجيل الدخول (Session Check) لمنع تخطي صفحة الدخول بدون توثيق حقيقي.
**الحالة الجديدة:** أُغلق (مرحلة الاستقرار مكتملة، المشروع جاهز لربط المتاجر والدفع).

---

## كيفية استخدام هذا الملف

عند بدء جلسة جديدة:
1. اقرأ قسم "تتبع حالة الدفعات" لمعرفة أين توقف العمل
2. اقرأ "الأخطاء المعروفة" للدفعة الحالية
3. راجع "القرارات المعلّقة" إذا كانت تؤثر على العمل الحالي
4. بعد أي تغيير: حدّث حالة الدفعة المعنية + أضف سطرًا في قسم التعديلات

**تنسيق إضافة تعديل جديد:**
```
### تعديل [التاريخ]
- [ما تم]: وصف موجز
- [الملف]: المسار الكامل
- [الحالة الجديدة]: (أُغلق / جزئي / لا يزال مفتوحًا)
```
