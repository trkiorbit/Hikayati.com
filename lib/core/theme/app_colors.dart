import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors from Visual Identity System
  static const Color primaryPurple = Color(0xFF6A0DAD);
  static const Color warmGold = Color(0xFFFFD700);
  static const Color deepBlack = Color(0xFF000000);

  // New Identifiers requested by user
  static const Color primary = Color(0xFF6A0DAD);
  static const Color secondary = Color(0xFFFFD700);
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);

  // Secondary Colors
  static const Color starWhite = Color(0xFFFFFFFF);
  static const Color successGreen = Color(0xFF4CAF50);
  static const Color alertRed = Color(0xFFF44336);

  // Background and Surface Colors (Derived for UI needs)
  static const Color backgroundColor = Color(0xFFF8F5FB); 
  static const Color surfaceColor = starWhite;
}
