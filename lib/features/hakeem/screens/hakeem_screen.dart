// [LEGACY] تم استبدال هذه الشاشة بـ HakeemChatScreen

import 'package:flutter/material.dart';

class HakeemScreen extends StatelessWidget {
  const HakeemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Deprecated. Use HakeemChatScreen.')));
  }
}
