// [LEGACY] هذا الملف أصبح قديماً وتم إيقاف استخدامه.
// صناعة الأفاتار تتم الآن عبر AvatarLabScreen فقط بناءً على الدستور.

import 'package:flutter/material.dart';

class CreateAvatarScreen extends StatelessWidget {
  const CreateAvatarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Deprecated. Use AvatarLab.')));
  }
}
