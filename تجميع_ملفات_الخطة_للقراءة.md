


============================================================
FILE: D:\Hikayati.com\خطة_المشروع\الخطة_الشاملة.md
============================================================

# حكواتي — الخطة الشاملة للمشروع
**آخر تحديث:** 2026-04-15
**المرحلة الحالية:** إغلاق واستقرار قبل الإطلاق
**الأولوية:** أفاتار ← رصيد ← صوت ← مكتبة ← هوية ← متجر ← إطلاق

---

## 1. نظرة عامة على المشروع

| البند | التفاصيل |
|---|---|
| اسم المشروع | حكواتي (Hikayati) |
| الهدف | تطبيق قصص أطفال مدعوم بالذكاء الاصطناعي — يولّد قصصًا سينمائية بشخصية الطفل |
| الفئة المستهدفة | أطفال 3-10 سنوات / والديهم (عربي أولاً) |
| التقنيات | Flutter + Supabase + Pollinations AI + ElevenLabs |
| المنصات | Android + iOS (ويندوز/ويب: للتطوير فقط) |
| المستودع | D:\Hikayati.com |
| اللغة الأساسية | العربية (RTL) |
| نمط المعمارية | Services → Use Cases → Screens (بدون UI generation) |

---

## 2. هيكل الكود الحالي

```
lib/
├── core/
│   ├── router/app_router.dart        ← GoRouter — 11 مسار
│   ├── auth/auth_service.dart        ← Google Sign-In + Supabase
│   ├── network/supabase_service.dart ← RPC + تهيئة Supabase
│   └── theme/
│       ├── app_colors.dart           ← الألوان الحالية (تحتاج تحديث)
│       └── app_theme.dart            ← Light/Dark theme
├── application/use_cases/
│   ├── generate_story_use_case.dart  ← محرك التوليد الرئيسي (291 سطر)
│   ├── save_avatar_use_case.dart
│   ├── get_private_stories_use_case.dart
│   ├── get_public_stories_use_case.dart
│   ├── unlock_public_story_use_case.dart
│   ├── delete_story_use_case.dart
│   └── auth_use_cases.dart
└── features/
    ├── auth/screens/login_screen.dart
    ├── home/screens/home_screen.dart           ← 587 سطر
    ├── story_engine/
    │   ├── screens/story_creation_screen.dart  ← 438 سطر
    │   ├── screens/intro_cinematic_screen.dart ← 231 سطر
    │   ├── screens/cinema_screen.dart          ← 380 سطر
    │   ├── services/unified_engine.dart        ← 236 سطر
    │   ├── services/audio_service.dart         ← 99 سطر
    │   └── services/content_monitor_service.dart ← 38 سطر
    ├── avatar_lab/
    │   ├── screens/avatar_lab_screen.dart      ← 439 سطر
    │   └── services/avatar_vision_service.dart ← 168 سطر
    ├── hakeem/
    │   ├── screens/hakeem_chat_screen.dart     ← 218 سطر
    │   └── services/hakeem_service.dart        ← 80 سطر
    ├── library/
    │   ├── screens/private_library_screen.dart ← 522 سطر
    │   ├── screens/public_library_screen.dart  ← 359 سطر
    │   └── services/library_service.dart       ← 80 سطر
    ├── voice_clone/
    │   └── screens/voice_clone_screen.dart     ← 376 سطر
    └── legal/
        ├── screens/privacy_policy_screen.dart  ← stub
        └── screens/terms_of_service_screen.dart ← stub
```

**إجمالي:** 44 ملف Dart / 6,156 سطر

---

## 3. جداول Supabase الحالية

| الجدول | الغرض | الأعمدة الأساسية |
|---|---|---|
| `profiles` | بيانات المستخدم | user_id, credits, avatar_data, avatar_profile_summary, language |
| `stories` | قصص المستخدمين | id, user_id, title, scenes_json, cover_image, voice_type, created_at |
| `public_stories` | مكتبة عامة | id, title, scenes_json, cover_image, price_credits |
| `purchases` | سجل فتح القصص | user_id, story_id, unlock_type='access' |

**RPC Functions:**
- `deduct_credits(p_user_id, p_amount, p_reason)` — خصم آمن مع فحص رصيد
- `ensureProfileExists(userId)` — إنشاء ملف شخصي برصيد 100 عند الحاجة

**Storage Buckets:**
- `avatars/` — صور الأفاتار
- `story_audio/` — ملفات صوت القصص (`{userId}/{storyId}_scene_{index}.mp3`)

---

## 4. قواعد المعمارية الثابتة (لا تُكسر)

1. **الواجهة عرض فقط** — لا AI calls من داخل الشاشات مباشرة
2. **السينما عرض فقط** — لا API calls عند إعادة التشغيل، يقرأ من `audio_url` المحفوظ
3. **كل التوليد عبر Use Cases** — generate_story_use_case هو الوسيط الوحيد
4. **الأفاتار بالاستخراج فقط** — ممنوع إدخال العمر/الجنس/اللون يدويًا
5. **خصم الرصيد بعد النجاح فقط** — لا خصم إذا فشل أي خطوة
6. **مصدر واحد للحقيقة** — Supabase هو المرجع، لا fallback مُرمَّز

---

## 5. نموذج التكلفة الحالي (جواهر)

| العملية | التكلفة |
|---|---|
| إنشاء قصة | 10 جواهر |
| فتح قصة عامة | 10 جواهر (متغير حسب `price_credits`) |
| إنشاء أفاتار جديد | **20 كريدت** ✅ |
| إنشاء صوت مستنسخ (أول مرة) | **20 كريدت** ✅ |
| توليد قصة (نص + صور + صوت) | **10 كريدت** ✅ |
| فتح قصة من المكتبة العامة | **متغير** حسب عدد المشاهد (يُضبط من الأدمن) |
| إعادة فتح قصة خاصة | **مجاني** ✅ |
| إعادة فتح قصة خاصة | مجاني |
| الرصيد الابتدائي | 100 جواهر |

> **محسوم 2026-04-15:** إنشاء أفاتار = **20 كريدت** | توليد القصة = **10 كريدت** | تكلفة استنساخ الصوت لا تزال معلّقة.

---

## 6. الحالة الحالية لكل نظام

| النظام | الحالة | ملاحظة |
|---|---|---|
| تسجيل الدخول | يعمل | Google Sign-In + Supabase |
| الشاشة الرئيسية | يعمل جزئيًا | الرصيد حقيقي، المتجر placeholder |
| توليد القصة | يعمل | يحتاج اختبار شامل |
| السينما | يعمل | lifecycle الصوت يحتاج اختبار |
| الأفاتار | يعمل جزئيًا | متعدد المصادر — يحتاج إقفال |
| الصوت العادي | يعمل | Pollinations TTS |
| استنساخ الصوت | يعمل | ElevenLabs |
| المكتبة الخاصة | يعمل جزئيًا | يحتاج اختبار فتح بدون خصم |
| المكتبة العامة | يعمل | الفتح يخصم 10 جواهر |
| حكيم (AI Chat) | يعمل | Pollinations Chat API |
| المتجر | placeholder | SnackBar "قريباً" فقط |
| صفحات قانونية | stub | محتوى فارغ |
| الهوية البصرية | غير متطابقة | #6A0DAD بدل #2D0B5A |

---

## 7. خطة الإطلاق العاجلة — 7 دفعات

### الدفعة 1 — إقفال الأفاتار
**المشكلة:** تعدد المصادر يسبب تخبيط في استرداد بيانات الشخصية
**الهدف:** مصدر واحد نهائي، 4 نتائج حقيقية، حفظ يدخل مباشرة في محرك القصة

**الملفات المعنية:**
- `lib/features/avatar_lab/services/avatar_vision_service.dart`
- `lib/features/avatar_lab/services/avatar_service.dart`
- `lib/application/use_cases/save_avatar_use_case.dart`
- `lib/application/use_cases/generate_story_use_case.dart`

**معايير الإقفال:**
- [ ] Google Vision API يستخرج face_description بدون أي مدخل يدوي
- [ ] 4 خيارات أفاتار تُولَّد عبر Pollinations فعليًا
- [ ] المستخدم يختار واحدة وتُحفظ في `profiles.avatar_profile_summary`
- [ ] `promptSnippet` يُحقن في محرك القصة بشكل صحيح
- [ ] لا أسماء نماذج مزدوجة في الكود

---

### الدفعة 2 — إقفال الصوت
**المشكلة:** lifecycle الصوت غير مستقر، احتمال تشغيل صوت بعد dispose
**الهدف:** Pollinations للصوت العادي، ElevenLabs للمستنسخ، السينما تقرأ `audio_url` فقط

**الملفات المعنية:**
- `lib/features/story_engine/services/audio_service.dart`
- `lib/features/story_engine/screens/cinema_screen.dart`
- `lib/application/use_cases/generate_story_use_case.dart`

**معايير الإقفال:**
- [ ] `_isDisposed` يمنع أي عملية بعد التخلص
- [ ] السينما لا تستدعي TTS مباشرة — تقرأ `audio_url` المحفوظ فقط
- [ ] تأخير صامت آمن (500ms-1000ms) إذا لم يتوفر صوت
- [ ] lifecycle مستقر عند التنقل بين المشاهد والخروج
- [ ] ElevenLabs voice_id يُحفظ ويُسترد صحيحًا

---

### الدفعة 3 — إقفال الرصيد
**المشكلة:** وجود `?? 100` في بعض الأماكن يخفي مشاكل فعلية
**الهدف:** Supabase مصدر الحقيقة الوحيد، UI يتحدث لحظيًا

**الملفات المعنية:**
- `lib/core/network/supabase_service.dart`
- `lib/application/use_cases/generate_story_use_case.dart`
- `lib/features/home/screens/home_screen.dart`

**معايير الإقفال:**
- [ ] حذف جميع `?? 100` من منطق الرصيد
- [ ] `creditsStream` في HomeScreen يقرأ من Supabase لحظيًا
- [ ] رسالة خطأ واضحة عند نقص الرصيد
- [ ] الرقم يظهر بدون كسور عشرية
- [ ] الخصم لا يحدث إلا بعد حفظ القصة كاملاً

---

### الدفعة 4 — إقفال المكتبات
**المشكلة:** إعادة فتح القصص قد تستدعي API غير ضروري
**الهدف:** مكتبة خاصة = بدون خصم بدون API، مكتبة عامة = فتح بـ 10 جواهر مرة واحدة

**الملفات المعنية:**
- `lib/features/library/screens/private_library_screen.dart`
- `lib/features/library/screens/public_library_screen.dart`
- `lib/features/library/services/library_service.dart`
- `lib/application/use_cases/unlock_public_story_use_case.dart`

**معايير الإقفال:**
- [ ] فتح القصة الخاصة = `fromLibrary: true` → السينما تعرض فقط
- [ ] لا تُولَّد موارد جديدة عند فتح القصة المحفوظة
- [ ] الحد الأقصى 10 قصص خاصة مُطبَّق في 3 أماكن
- [ ] المكتبة العامة تعرض حالة locked/unlocked بوضوح
- [ ] حذف القصة ينظف الصوت من storage أيضًا

---

### الدفعة 5 — توحيد الهوية البصرية
**المشكلة:** الألوان الحالية (#6A0DAD) لا تطابق المواصفات النهائية
**الهدف:** نظام theme موحّد، لا ألوان hardcoded في أي مكان

**الملفات المعنية:**
- `lib/core/theme/app_colors.dart`
- `lib/core/theme/app_theme.dart`
- جميع شاشات الـ features

**المواصفات البصرية النهائية:**
```
الألوان المعتمدة:
primaryDeepPurple: #2D0B5A   ← بنفسجي ملكي داكن (الأساسي)
vibrantOrange:     #FF8C00   ← برتقالي حيوي (الثانوي)
warmGold:          #FFD700   ← ذهبي دافئ (التمييز)
deepNight:         #0D0D1A   ← خلفية ليلية
glassWhite:        #F5F0FF   ← نص أبيض دافئ

الخطوط:
headlineFont: Cairo     ← العناوين
bodyFont: Tajawal       ← النصوص

الشعور العام: سينمائي دافئ / Glassmorphism خفيف
```

**معايير الإقفال:**
- [ ] ملف `app_colors.dart` محدَّث بالألوان الجديدة
- [ ] لا `Color(0xFF...)` مُرمَّزة في شاشات features
- [ ] Cairo و Tajawal مضافان في pubspec.yaml
- [ ] جميع الأزرار والبطاقات تسحب من ThemeData

---

### الدفعة 6 — بناء المتجر

#### القسم الرقمي — داخل التطبيق (IAP إجباري بسياسة Apple/Google)
```
lib/features/store/
├── screens/store_screen.dart         ← شاشة المتجر الرئيسية
├── screens/credit_packages_tab.dart  ← باقات الجواهر
└── screens/my_orders_tab.dart        ← طلباتي وفواتيري
```

**الباقات المقترحة:**
| الباقة | الجواهر | التقنية |
|---|---|---|
| Plus | 100 جوهر | Apple StoreKit / Google Play Billing |
| Pro | 500 جوهر | Apple StoreKit / Google Play Billing |
| Premium | 1000 جوهر | Apple StoreKit / Google Play Billing |

#### القسم المادي — متجر ويب خارجي (مدفوعات خارج IAP)
```
متجر ويب مستقل موحد الهوية مع التطبيق:
- كتيب مطبوع للقصة
- تيشيرت بشخصية الطفل
- ملصقات حكواتي
- هدايا حكواتي
```

**الأتمتة (مرحلة ثانية من المتجر):**
- Webhook عند الدفع → إنشاء فاتورة PDF في Supabase Storage
- إرسال بريد إلكتروني تلقائي للمستخدم
- صفحة "طلباتي" داخل التطبيق لتتبع الطلبات المادية

**قاعدة الصياغة الآمنة (لتجنب رفض Apple/Google):**

| صياغة خاطئة | صياغة صحيحة |
|---|---|
| "اشتر الكتيب وخذ 100 كريدت" | "اطلب كتيب حكواتي" |
| "الكتيب يحتوي جواهر" | "منتجات حكواتي" |
| ربط مادي برقمي صريح | "يتضمن مزايا إضافية داخل التطبيق" |

---

### الدفعة 7 — إطلاق التطبيق

#### قبل الرفع (إجباري):
- [ ] إزالة جميع keys/secrets من المستودع
- [ ] تعطيل أي شاشة admin/debug
- [ ] لا أزرار ميتة (dead buttons) في أي شاشة
- [ ] مراجعة المحتوى للأطفال
- [ ] اختبار build release على جهاز Android حقيقي + iOS

#### Apple App Store:
- [ ] Apple Developer Program ($99/سنة)
- [ ] Bundle ID في App Store Connect
- [ ] إنشاء In-App Purchases للجواهر (StoreKit 2)
- [ ] لقطات شاشة (iPhone 6.7" + iPad 12.9")
- [ ] سياسة الخصوصية على رابط عام فعال
- [ ] وصف التطبيق بالعربية + الكلمات المفتاحية
- [ ] تصنيف العمر: 4+
- [ ] رفع build عبر Xcode/Transporter
- [ ] ربط IAP بالنسخة وإرسال للمراجعة

#### Google Play:
- [ ] Play Console ($25 مرة واحدة)
- [ ] Data Safety Form (بيانات الأطفال — إجباري)
- [ ] سياسة الخصوصية على رابط عام
- [ ] Google Play Billing للجواهر
- [ ] AAB (Android App Bundle) — ليس APK
- [ ] Closed Testing → Internal Testing → Production
- [ ] content rating: Everyone / ESRB E

---

## 8. الصفحات القانونية المطلوبة قبل الإطلاق

> **تنبيه:** حكواتي يتعامل مع بيانات الأطفال، صور، أصوات، ومدفوعات — الصفحات يجب أن تكون مخصصة وليست نماذج عامة.

| الصفحة | الوضع الحالي | الأولوية |
|---|---|---|
| سياسة الخصوصية | stub موجود | عاجل قبل الإطلاق |
| الشروط والأحكام | stub موجود | عاجل قبل الإطلاق |
| سياسة الأطفال (COPPA) | غير موجود | عاجل قبل الإطلاق |
| سياسة حذف البيانات | غير موجود | مطلوب لـ Google Play |
| سياسة الاسترداد | غير موجود | مطلوب |
| سياسة المشتريات الرقمية | غير موجود | مطلوب مع المتجر |
| سياسة الشحن والمنتجات المادية | غير موجود | مطلوب مع المتجر المادي |

---

## 9. الشاشة الرئيسية — الهيكل النهائي المعتمد

```
╔══════════════════════════════════╗
║  شعار حكواتي  | رصيد | حساب ⚙  ║  <- AppBar
╠══════════════════════════════════╣
║                                  ║
║   [بانر رئيسي]                   ║
║   "حوّل طفلك إلى بطل قصة"       ║
║                                  ║
║   ╔════════╗  ╔═══════════════╗  ║
║   ║ اصنع   ║  ║ اروِ القصة   ║  ║
║   ║ بطلك   ║  ║ بصوتك        ║  ║
║   ╚════════╝  ╚═══════════════╝  ║
║                                  ║
║   [زر بارز] ✨ أنشئ قصة ✨       ║
║                                  ║
╠══════════════════════════════════╣
║  المكتبة العامة <----------      ║
╠══════════════════════════════════╣
║  مكتبتي <------------------      ║
╠══════════════════════════════════╣
║  متجر حكواتي <-------------      ║
╠══════════════════════════════════╣
║  خصوصية | شروط | عن حكواتي      ║  <- Footer
╚══════════════════════════════════╝
```

**قاعدة:** جميع الأزرار مربوطة فعليًا — لا placeholder بدون route أو وظيفة.

---

## 10. مسارات GoRouter الحالية

```
/            ← HomeScreen
/login       ← LoginScreen
/hakeem      ← HakeemChatScreen
/avatar-lab  ← AvatarLabScreen
/create-story ← StoryCreationScreen
/cinema      ← CinemaScreen
/intro-cinematic ← IntroCinematicScreen
/public-library  ← PublicLibraryScreen
/private-library ← PrivateLibraryScreen
/voice-clone ← VoiceCloneScreen
/generation-loading ← GenerationLoadingScreen
/app-intro   ← AppIntroScreen (معطّل حالياً)
```

---

## 11. خطة التطوير المستقبلي (بعد الإطلاق)

### المرحلة الثانية — تعزيز المنتج

| الميزة | الوصف | الأولوية |
|---|---|---|
| تعدد اللغات | قصص بالإنجليزية والفرنسية | عالية |
| قصص أطول | 6-8 مشاهد بدل 3 | عالية |
| مشاركة القصة | رابط مشاركة للعائلة | عالية |
| ملابس الأفاتار | تغيير ملابس الشخصية بدون تعديل ملامح | متوسطة |
| إحصائيات الوالدين | لوحة تتبع نشاط الطفل | متوسطة |
| أصوات جديدة | توسيع خيارات أصوات Pollinations | منخفضة |
| قصص تعليمية | ربط القصة بمحتوى تعليمي | متوسطة |

### المرحلة الثالثة — توسع

| الميزة | الوصف |
|---|---|
| اشتراكات | خطط شهرية/سنوية بدل الجواهر الفردية |
| المكتبة العامة المتوسعة | قصص أكثر، فئات متعددة |
| لوحة إدارة | لوحة web للأدمن لإدارة القصص والمستخدمين |
| مجتمع القصص | مشاركة القصص في المكتبة العامة |
| طباعة تلقائية | ربط مع خدمة طباعة لتوصيل الكتيبات |

---

## 12. المهارات المطلوبة

### تقنية أساسية
- **Flutter/Dart** (متقدم) — شاشات، State Management، Animations، Platform Channels
- **Supabase** — PostgreSQL، RPC Functions، Storage، Edge Functions، Auth، Realtime
- **Riverpod** — موجود في المشروع، يحتاج توسيع للـ state management
- **GoRouter** — Navigation patterns، extra data passing

### APIs الخارجية
- **Pollinations AI** — Text Generation، Image Generation، TTS (نص + صور + صوت)
- **ElevenLabs** — Voice Cloning، Multilingual TTS V2
- **Google Vision API** — Face Detection، Feature Extraction للأفاتار
- **Google Sign-In** — OAuth flow

### إطلاق التطبيق
- **Apple App Store Connect** — Build submission، IAP، Review process، Certificates
- **Google Play Console** — AAB، Play Billing، Data Safety Form
- **Code Signing** — iOS certificates + provisioning profiles، Android keystore
- **Fastlane** (اختياري) — أتمتة البناء والرفع

### المتجر والمدفوعات
- **Apple StoreKit 2** — In-App Purchases في iOS
- **Google Play Billing Library 6+** — In-App Purchases في Android
- **Webhooks** — استقبال إشعارات الدفع من المتاجر
- **PDF Generation** — إنشاء فواتير تلقائية (Deno/Edge Functions)

### تصميم وهوية
- **UI/UX للأطفال** — Glassmorphism، أنيميشن، Responsive
- **Cairo/Tajawal** — التعامل مع الخطوط العربية في Flutter
- **Asset Management** — أيقونات، صور، فيديو، splash screens

### محتوى وقانوني
- كتابة محتوى عربي للأطفال (3-10 سنوات)
- صياغة السياسات القانونية (خصوصية، شروط، COPPA للأطفال)
- Data Safety Form لـ Google Play

---

## 13. بروتوكول العمل مع تروك

> المرجع: ملف `# TROUK PROFILE SKILL.txt` في جذر المشروع

| القاعدة | التفصيل |
|---|---|
| اللغة | العربية دائمًا |
| الأسلوب | فكرة + سلبيات أولًا، ثم التنفيذ |
| الكود | كاملًا في مربع واحد غير مجزأ |
| المسار | قبل الكود دائمًا |
| ممنوع | "تأكد من" في الردود |
| نهاية كل رد | "هل نجح؟" |
| الاسم | تروك (ليس Türk ولا Turki) |
| البيئة | Windows 11 + PowerShell / D:\Hikayati.com |

---

## 14. الملفات الحرجة التي تحتاج مراجعة عند بدء أي جلسة

```
1. lib/application/use_cases/generate_story_use_case.dart   ← منطق التوليد الكامل
2. lib/core/router/app_router.dart                          ← المسارات والتوجيه
3. lib/features/home/screens/home_screen.dart               ← الشاشة الرئيسية
4. lib/core/theme/app_colors.dart                          ← ألوان النظام
5. تعديلات_كلاو.md                                         ← سجل التغييرات القديم
6. خطة_المشروع/سجل_التعديلات.md                           ← سجل الأخطاء والتعديلات الموحّد
```

---

## 15. الروابط والمراجع التقنية

- **Supabase Project URL:** `https://yuqipgwjepxohnouiais.supabase.co`
- **Pollinations AI Docs:** `https://pollinations.ai`
- **ElevenLabs API:** `https://api.elevenlabs.io`
- **Apple Review Guidelines:** `developer.apple.com/app-store/review/guidelines/`
- **Google Play Policy (Billing):** `support.google.com/googleplay/android-developer/answer/9858738`
- **Google Play Privacy Requirements:** `support.google.com/googleplay/android-developer/answer/9888076`



============================================================
FILE: D:\Hikayati.com\خطة_المشروع\سجل_التعديلات.md
============================================================

# سجل التعديلات والأخطاء — حكواتي
**آخر تحديث:** 2026-04-15
**ملاحظة:** هذا الملف يجمع محتوى `تعديلات_كلاو.md` + الأخطاء الجديدة في مكان واحد. أي تغيير يُسجَّل هنا.

---

## الأخطاء والمشاكل المعروفة

### خطأ 1 — عدم تطابق الألوان
| البند | التفاصيل |
|---|---|
| الوصف | `app_colors.dart` يستخدم `#6A0DAD` بينما المواصفات النهائية تحدد `#2D0B5A` |
| الملف | `lib/core/theme/app_colors.dart` |
| التأثير | هوية بصرية غير متطابقة مع المواصفات النهائية المعتمدة |
| الدفعة | 5 |
| الحالة | مفتوح |

---

### خطأ 2 — Fallback مُرمَّز في منطق الرصيد
| البند | التفاصيل |
|---|---|
| الوصف | بعض الأماكن تستخدم `?? 100` عند قراءة الرصيد من Supabase مما قد يُخفي bug حقيقي |
| الملف | `lib/application/use_cases/generate_story_use_case.dart` |
| التأثير | المستخدم قد يرى رصيدًا خاطئًا ويستطيع التوليد بدون رصيد فعلي |
| الدفعة | 3 |
| الحالة | مفتوح |

---

### خطأ 3 — تعدد مصادر الأفاتار
| البند | التفاصيل |
|---|---|
| الوصف | منطق الأفاتار موزع بين avatar_vision_service وavatar_service والـ use case بطريقة يمكن أن تنتج تعارض |
| الملفات | `lib/features/avatar_lab/services/avatar_vision_service.dart` + `avatar_service.dart` + `save_avatar_use_case.dart` |
| التأثير | تخبيط في استرداد بيانات الشخصية وحقن الـ promptSnippet في محرك القصة |
| الدفعة | 1 |
| الحالة | مفتوح |

---

### خطأ 4 — المتجر placeholder فقط
| البند | التفاصيل |
|---|---|
| الوصف | زر "الشحن" في الـ Drawer يفتح SnackBar "قريبًا" فقط، وقسم المتجر في HomeScreen بدون أي route |
| الملف | `lib/features/home/screens/home_screen.dart` (حوالي سطر 420) |
| التأثير | المستخدم لا يستطيع شراء جواهر أو الوصول للمتجر |
| الدفعة | 6 |
| الحالة | مفتوح |

---

### خطأ 5 — صفحات قانونية stub بدون محتوى
| البند | التفاصيل |
|---|---|
| الوصف | `privacy_policy_screen.dart` و `terms_of_service_screen.dart` موجودتان لكن بدون محتوى حقيقي |
| الملف | `lib/features/legal/screens/` |
| التأثير | رفض محتمل من App Store وGoogle Play — مطلوب رابط عام فعال |
| الدفعة | 7 (قبل الرفع) |
| الحالة | مفتوح |

---

### خطأ 6 — Supabase Edge Function غير مستخدمة
| البند | التفاصيل |
|---|---|
| الوصف | `supabase/functions/ai-assistant/index.ts` (Anthropic claude-sonnet) موجودة لكن لا يستدعيها أي كود في التطبيق |
| الملف | `supabase/functions/ai-assistant/index.ts` |
| التأثير | موارد غير مستغلة، وتكلفة محتملة عند النشر |
| القرار المطلوب | إما ربطها بحكيم بدلاً من Pollinations، أو حذفها |
| الحالة | مؤجل — ينتظر قرار تروك |

---

### خطأ 7 — تناقض في تسعير الأفاتار والصوت
| البند | التفاصيل |
|---|---|
| الوصف | `hakeem_service.dart` يخبر المستخدم أن الأفاتار والصوت بـ 20 جوهر، لكن `generate_story_use_case.dart` يحسب صفر |
| الملفات | `lib/features/hakeem/services/hakeem_service.dart` + `lib/application/use_cases/generate_story_use_case.dart` |
| التأثير | تناقض في معلومات المستخدم — خطر شكاوى وعدم ثقة |
| الحالة | معلّق — ينتظر قرار تروك |

---

### خطأ 8 — Google Sign-In قد يفشل على iOS
| البند | التفاصيل |
|---|---|
| الوصف | `auth_service.dart` يحتوي `clientId: null` في بعض الإعدادات |
| الملف | `lib/core/auth/auth_service.dart` |
| التأثير | تسجيل الدخول قد يفشل على iOS بدون clientId صحيح |
| الدفعة | 7 (قبل الإطلاق) |
| الحالة | مفتوح |

---

### خطأ 9 — عدم وجود سياسة الأطفال (COPPA)
| البند | التفاصيل |
|---|---|
| الوصف | التطبيق يستهدف الأطفال ويجمع بيانات (صور، أصوات) لكن لا توجد سياسة COPPA/Children Privacy |
| التأثير | رفض مؤكد من Apple وGoogle Play بدون هذه السياسة |
| الدفعة | 7 (قبل الرفع) |
| الحالة | مفتوح |

---

## سجل القرارات المعتمدة

| التاريخ | القرار | المرجع |
|---|---|---|
| 2026-04-15 | الأفاتار بالاستخراج فقط — ممنوع إدخال يدوي للعمر/الجنس/اللون | تروك |
| 2026-04-15 | السينما عرض فقط — لا API calls عند الإعادة، يقرأ `audio_url` المحفوظ | تروك |
| 2026-04-15 | الصوت العادي = Pollinations، المستنسخ = ElevenLabs فقط | تروك |
| 2026-04-15 | الخصم بعد النجاح الكامل (نص + صور + صوت + حفظ) فقط | تروك |
| 2026-04-15 | الحد الأقصى 10 قصص خاصة لكل مستخدم | تروك |
| 2026-04-15 | الألوان النهائية: #2D0B5A + #FF8C00 + Cairo/Tajawal | تروك |
| 2026-04-15 | IAP إجباري للجواهر الرقمية (Apple StoreKit + Google Play Billing) | Apple/Google Policy |
| 2026-04-15 | المنتجات المادية = متجر ويب خارجي (ليس IAP) | Apple/Google Policy |
| 2026-04-15 | لا استدعاء AI من الشاشات مباشرة — كل شيء عبر Use Cases | تروك (معمارية) |

---

## قرارات معلّقة تنتظر تروك

| الموضوع | الخيارات | الملفات المتأثرة |
|---|---|---|
| تكلفة استنساخ الصوت | 0 جوهر أو 20 جوهر | generate_story_use_case.dart + hakeem_service.dart |
| تكلفة إنشاء الأفاتار | 0 جوهر أو 20 جوهر | save_avatar_use_case.dart + hakeem_service.dart |
| Edge Function | ربطها بحكيم أو حذفها | supabase/functions/ai-assistant/index.ts |

---

## التعديلات من جلسة 2026-04-15

### ما تم إنجازه في الجلسة:
- مراجعة شاملة لـ 44 ملف Dart (6,156 سطر)
- رصد 9 أخطاء وتوثيقها
- تأكيد 7 دفعات كخطة إطلاق نهائية مع تحديد الملفات لكل دفعة
- توثيق قواعد المعمارية الثابتة (6 قواعد)
- توثيق بروتوكول العمل مع تروك
- إنشاء مجلد `خطة_المشروع/` بملفين: الخطة الشاملة + هذا السجل

### ما لم يُنجز (مؤجل للجلسات التالية):
- تنفيذ الدفعات 2-7
- الحصول على قرار تروك بشأن تسعير الأفاتار والصوت

---

### تعديل 2026-04-15 — الدفعة 1: إقفال الأفاتار ✅

**ما تم:**
- `avatar_service.dart`: تصحيح اسم العمود من `avatar_data` إلى `avatar_profile_summary` (كان يحفظ في العمود الخاطئ)
- `save_avatar_use_case.dart`: إعادة كتابة كاملة — يعالج الآن خصم الكريدت (20 جوهرة) + يحسب ويحفظ `prompt_snippet` للحقن في محرك القصة + إضافة `changeClothes()` (5 جواهر)
- `avatar_lab_screen.dart`: حذف الاستدعاء المباشر لـ `SupabaseService.deductCredits()` و Supabase من الشاشة — توجيه `_saveAvatar()` و `_changeClothes()` عبر `SaveAvatarUseCase` (تطبيق قاعدة المعمارية)
- `generate_story_use_case.dart`: يستخدم الآن `prompt_snippet` المحفوظ مباشرةً عند إنشاء القصة بدلاً من إعادة بنائه
- `avatar_vision_service.dart`: حذف `import 'dart:io'` غير المستخدم
- بناء APK نجح ✅

**الحالة:** أُغلق — معايير الإقفال مكتملة
**ملاحظة:** قرار تسعير الأفاتار لا يزال معلّقاً من تروك (20 جوهرة مبدئياً في `SaveAvatarUseCase.avatarCost`)

---

## التعديلات السابقة (من سجل Git)

| الـ Commit | الوصف | التاريخ التقريبي |
|---|---|---|
| `627e6cc` | تنظيف المشروع، تطبيق gitignore صارم | قبل 2026-04-15 |
| `0e08d71` | الانتهاء من المحركات الأساسية | قبل 2026-04-15 |
| `dd06e32` | ضبط حكيم والمكتبة الخاصة واستقرار الصوت | قبل 2026-04-15 |
| `5164e71` | تحسين جودة التسجيل، إلغاء الضوضاء، كاش الصوت | قبل 2026-04-15 |
| `32bca97` | نجاح الأفاتار وربطه بالقصة | قبل 2026-04-15 |

---

## تتبع حالة الدفعات

| # | الدفعة | الحالة | الملف الرئيسي |
|---|---|---|---|
| 1 | إقفال الأفاتار | مكتمل ✅ | avatar_vision_service.dart |
| 2 | إقفال الصوت | مكتمل ✅ | generate_story_use_case.dart |
| 3 | إقفال الرصيد | مكتمل ✅ | generate_story_use_case.dart |
| 4 | إقفال المكتبات | جزئي | private_library_screen.dart |
| 5 | توحيد الهوية | جزئي | app_colors.dart + app_theme.dart |
| 6 | بناء المتجر | جزئي ✅ | store_screen.dart (نسخة أولى) |
| 7 | إطلاق التطبيق | لم يبدأ | App Store Connect + Play Console |
| — | إغلاق طبقة المنتج | مكتمل ✅ | رصيد + دخول + حساب + قانوني + موافقات + أزرار |

---

### تعديل 2026-04-18 — إغلاق طبقة المنتج (Product Closure) ✅

**A) إصلاح عرض الرصيد:**
- `home_screen.dart`: استُبدل StreamBuilder بـ setState + one-shot fetch + stream listener
- الرصيد يظهر فورًا عند تحميل الصفحة (لا initialData: 0)
- يتحدث لحظيًا بعد أي خصم عبر Supabase Realtime

**B) تدفق تسجيل الدخول:**
- Router يعيد التوجيه لـ `/login` إذا لا يوجد session (كان يعمل أصلاً)
- `login_screen.dart`: ألوان محدّثة من AppColors بدل hardcoded

**C) صفحة الحساب الشخصي (جديد):**
- `lib/features/profile/screens/profile_screen.dart`
- تعرض: البريد، الرصيد، حالة الأفاتار، حالة الصوت، عدد القصص
- أزرار: شحن، خصوصية، شروط، محتوى، حذف بيانات، تسجيل خروج

**D) الصفحات القانونية (4 صفحات كاملة):**
- `privacy_policy_screen.dart`: أُعيد كتابتها بالكامل (7 أقسام)
- `terms_of_service_screen.dart`: أُعيد كتابتها بالكامل (8 أقسام)
- `data_deletion_screen.dart`: جديد (6 أقسام)
- `content_policy_screen.dart`: جديد (5 أقسام)

**E) شاشات الموافقة الصريحة (جديد):**
- `avatar_lab_screen.dart`: dialog موافقة مع checkbox قبل تحليل الصورة
- `voice_clone_screen.dart`: dialog موافقة مع checkbox قبل بدء التسجيل

**F) ربط الأزرار:**
- Person icon → `/profile`
- Wallet icon → `/store`
- Store icon → `/store`
- "الشحن" → `/store`
- "المتجر" → `/store`
- "سياسة الخصوصية" → `/privacy-policy`
- "الشروط" → `/terms`
- "حسابي" → `/profile`

**G) شاشة المتجر (جديد):**
- `lib/features/store/screens/store_screen.dart`
- رصيد حالي + 4 باقات (50/100/250/500) + قسم "منتجات قريبًا"

**H-I) توحيد النصوص والهوية:**
- جميع الصفحات الجديدة تستخدم AppColors + نفس الثيم
- لا placeholders، لا debug labels، نبرة عربية احترافية موحدة

---

### تعديل 2026-04-16 — إصلاح Blocker الخطوط + الدفعة 2 + app_theme

### تعديل 2026-04-16 — الدفعة 3: إقفال الرصيد ✅

**ما تم:**
- `lib/application/use_cases/generate_story_use_case.dart`:
  - أضيف `static final Set<String> _deductedStoryIds = {}` لمنع الخصم المزدوج في نفس الجلسة
  - كتلة الخصم تقرأ الرصيد من Supabase **قبل** و**بعد** الخصم مباشرةً
  - اللوق بالشكل المطلوب: `[CREDIT] before=X`, `deducted=X`, `after=X`, `story_id=X`
  - الخصم يأتي فقط بعد: نص ✅ + صور ✅ + صوت ✅ + حفظ في Supabase ✅
  - `?? 0` في قراءة الرصيد (لا `?? 100` في أي مكان)
- `lib/features/home/screens/home_screen.dart`: **لا تغيير مطلوب** — يستخدم Supabase Realtime stream يتحدث تلقائياً عند تغيير الرصيد في DB
- `flutter analyze`: 0 أخطاء ✅

**كيف يمنع الخصم المزدوج:**
- `_isGenerating` lock: يمنع تشغيل `execute()` مرتين في نفس الوقت (race condition)
- `_deductedStoryIds`: يحفظ معرّفات القصص المخصوم عنها — إذا استُدعي الخصم لنفس القصة مرة ثانية (بأي سبب) يطبع تحذيراً ويتجاوز

**كيف يظهر الرصيد من Supabase:**
- `HomeScreen.initState()` ينشئ stream من `profiles.stream(primaryKey: ['user_id'])` مرة واحدة فقط
- Supabase Realtime يدفع التحديث فور تغيير الرصيد في DB → `StreamBuilder` يعيد بناء الـ AppBar فوراً

---

### تعديل 2026-04-16 — إصلاح Blocker الخطوط + الدفعة 2 + app_theme

**ما تم:**
- `pubspec.yaml`: حذف `fonts:` section اليدوية (الملفات غير موجودة) → **blocker محلول**
- `lib/core/theme/app_theme.dart`: تحويل `fontFamily: 'Cairo'/'Tajawal'` إلى `GoogleFonts.cairo()/.tajawal()` ← يستخدم google_fonts المثبّت
- `lib/core/theme/app_theme.dart`: تصحيح `CardTheme` → `CardThemeData`، `DialogTheme` → `DialogThemeData` (Flutter API)
- `lib/core/theme/app_colors.dart`: إضافة `deepBlack` alias لـ `deepNight` ← كان مفقوداً في story_creation_screen + voice_clone_screen
- `lib/application/use_cases/generate_story_use_case.dart`: إصلاح ترتيب TTS — Pollinations أولاً للصوت العادي (حذف منطق OpenAI/Azure)
- `flutter analyze`: 0 أخطاء ✅ (35 info فقط)

**الحالة الجديدة:**
- الدفعة 2 (الصوت): مكتملة ✅
- الدفعة 5 (الهوية): جزئي (الألوان + الخطوط جاهزة، الشاشات لا تزال تحتوي ألوان hardcoded)

---

### تعديل الإغلاق — نقطة استعادة: قبل الاطلاق و الدفع جاهز ✅
**ما تم:**
- إنشاء نقطة استعادة نظيفة في Git ورفعها إلى المستودع.
- إقفال مسار الصوت (استخدام `audio_url` المحفوظ فقط في السينما بدون TTS مباشر).
- إقفال حد المكتبة الخاصة (10 قصص، منع التوليد مع توجيه للحذف عند الامتلاء).
- إصلاح فلتر المحتوى (استخدام Regex للكلمة الكاملة لمنع الإنذارات الكاذبة).
- إصلاح جلسة تسجيل الدخول (Session Check) لمنع تخطي صفحة الدخول بدون توثيق حقيقي.
**الحالة الجديدة:** أُغلق (مرحلة الاستقرار مكتملة، المشروع جاهز لربط المتاجر والدفع).

---

### نقطة استعادة الطوارئ (Emergency Backup) 🛡️
**التاريخ:** (نقطة الاستقرار الحالية بعد أخطاء البناء)
**ما تم إنجازه:**
- إنشاء نسخة احتياطية مضغوطة نظيفة (Zip) من الكود المصدري ورفعها إلى القرص C أثناء تشغيل التطبيق.
- **المسار:** `C:\Hikayati_Emergency_Backup.zip`
- تم استخدام `git archive` لضمان ضغط الكود الصافي فقط وتجاهل مجلدات البناء (`build/`) الثقيلة لمنع أي تعارض.
**الحالة الجديدة:** نسخة احتياطية محلية متوفرة للاسترجاع الفوري عند الطوارئ، لتأمين مرحلة الاستقرار.

---

## كيفية استخدام هذا الملف

عند بدء جلسة جديدة:
1. اقرأ قسم "تتبع حالة الدفعات" لمعرفة أين توقف العمل
2. اقرأ "الأخطاء المعروفة" للدفعة الحالية
3. راجع "القرارات المعلّقة" إذا كانت تؤثر على العمل الحالي
4. بعد أي تغيير: حدّث حالة الدفعة المعنية + أضف سطرًا في قسم التعديلات

**تنسيق إضافة تعديل جديد:**
```
### تعديل [التاريخ]
- [ما تم]: وصف موجز
- [الملف]: المسار الكامل
- [الحالة الجديدة]: (أُغلق / جزئي / لا يزال مفتوحًا)
```

---

### تعديل 2026-04-23 — خطة المتجر الخارجي النهائية
- [ما تم]: إنشاء خطة إطلاق متجر حكواتي الخارجي الشاملة (hikayati.com على Hostinger)
- [الملف]: `خطة_المشروع/متجر_حكواتي_الخارجي.md` + `متجر_حكواتي_خطة_الإطلاق_النهائية.md`
- [يشمل]: بنية المجلدات، جداول Supabase (orders/invoices/credit_cards)، Edge Functions، وسائل دفع (Moyasar + Stripe)، جدول زمني كامل
- [قرار]: المتجر في مجلد منفصل D:\Hikayati-Store\ — نفس Supabase — Moyasar أولاً لمدى
- [الحالة الجديدة]: خطة مكتملة، جاهزة للتنفيذ بعد الراتب



============================================================
FILE: D:\Hikayati.com\خطة_المشروع\متجر_حكواتي_الخارجي.md
============================================================

# متجر حكواتي الخارجي — خطة البناء الكاملة
**الدومين:** hikayati.com (Hostinger)
**الهدف:** متجر طباعة حسب الطلب + شحن كريدت، بنفس الهوية البصرية للتطبيق
**آخر تحديث:** 2026-04-18

---

## 1. الفرق بين المتجر الداخلي والخارجي

| البُعد | داخل التطبيق (IAP) | المتجر الخارجي (hikayati.com) |
|---|---|---|
| **ما يُباع** | باقات الكريدت فقط | منتجات مادية + كروت شحن كريدت |
| **الدفع** | Apple StoreKit + Google Play Billing | Stripe / PayPal / مدى / Apple Pay (ويب) |
| **لماذا منفصل** | Apple/Google يمنعان بيع رقمي بدون IAP | المنتجات المادية **مستثناة** من IAP بسياسة Apple/Google |
| **العمولة** | 15-30% لـ Apple/Google | 2.9% + $0.30 لـ Stripe فقط |
| **التوصيل** | فوري (كريدت في الحساب) | شحن مادي + تأكيد بريد |

**قاعدة ذهبية**: داخل التطبيق لا تذكر المتجر الخارجي بصياغة تربط منتجاً مادياً بمكافأة رقمية (Apple يرفض).

---

## 2. الهوية البصرية الموحّدة (مطابقة للتطبيق)

### الألوان (hex codes)
```css
:root {
  /* أساسية */
  --deep-purple: #2D0B5A;     /* بنفسجي ملكي داكن — الأساسي */
  --vibrant-orange: #FF8C00;  /* برتقالي حيوي — الثانوي */
  --warm-gold: #FFD700;       /* ذهبي دافئ — التمييز */
  --deep-night: #0D0D1A;      /* خلفية ليلية */
  --glass-white: #F5F0FF;     /* نص أبيض دافئ */
  --card-surface: #1A122A;    /* سطح بطاقات */
  --purple-glow: #9B72CF;     /* بنفسجي فاتح للنصوص */

  /* تجارية */
  --price-green: #4CAF50;     /* أخضر للإضافة/الربح */
  --price-red: #FF5252;       /* أحمر للخصم */

  /* أبعاد */
  --radius-card: 14px;
  --radius-button: 12px;
}
```

### الخطوط (Google Fonts)
```html
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
```

```css
body { font-family: 'Tajawal', sans-serif; direction: rtl; }
h1, h2, h3 { font-family: 'Cairo', sans-serif; font-weight: 900; }
```

### الأيقونات
- **نجمة الكريدت**: SVG من مكتبة Material Icons (`stars`)
- جميع الأيقونات: outlined style، بحجم 20-32px

### الشعار
- اسم: حكواتي
- خط: Cairo Bold
- لون: `#FF8C00` على خلفية `#2D0B5A`
- ملف SVG مستقل يُستخدم في كل الصفحات

---

## 3. هيكل الصفحات

```
hikayati.com/
├── index.html              ← الصفحة الرئيسية
├── store/
│   ├── index.html          ← قائمة المنتجات
│   ├── tshirts/            ← تيشيرتات بشخصية الطفل
│   ├── booklets/           ← كتيبات قصص مطبوعة
│   ├── magazines/          ← مجلات شهرية
│   └── credit-cards/       ← كروت شحن كريدت
├── product/
│   └── [slug].html         ← صفحة منتج مفرد
├── cart.html               ← سلة التسوق
├── checkout.html           ← إتمام الطلب
├── account/
│   ├── orders.html         ← طلباتي
│   └── invoices.html       ← فواتيري
├── about.html              ← عن حكواتي
├── contact.html            ← اتصل بنا
├── privacy.html            ← سياسة الخصوصية (نفس التطبيق)
├── terms.html              ← الشروط والأحكام
├── refund.html             ← سياسة الاسترداد والشحن
└── assets/
    ├── css/style.css
    ├── js/app.js
    └── images/
```

---

## 4. الصفحة الرئيسية — المحتوى الجاهز

### Hero Section
```html
<section class="hero">
  <div class="container">
    <h1>حوّل طفلك إلى بطل قصة حقيقية</h1>
    <p class="subtitle">
      تيشيرتات، كتيبات، ومجلات مطبوعة بشخصية طفلك من قصصه المفضلة
    </p>
    <div class="hero-cta">
      <a href="/store" class="btn-primary">تسوّق الآن</a>
      <a href="https://apps.apple.com/..." class="btn-secondary">
        حمّل تطبيق حكواتي
      </a>
    </div>
  </div>
</section>
```

### Features Grid
```
┌─────────────────┬─────────────────┬─────────────────┐
│   🎨 تخصيص      │   📦 شحن سريع   │   ⭐ جودة عالية │
│   كامل         │   3-7 أيام      │   طباعة احترافية│
└─────────────────┴─────────────────┴─────────────────┘
```

### Products Showcase (4 بطاقات)
- **تيشيرت بطلك** — ابتداءً من 89 ريال
- **كتيب القصة** — ابتداءً من 59 ريال
- **مجلة حكواتي الشهرية** — اشتراك 149 ريال/شهر
- **كرت شحن كريدت** — 100 ⭐ بـ 29 ريال

### How It Works
```
1. اختر منتجك  →  2. ارفع صورة طفلك  →  3. ندخل في الطباعة  →  4. يصلك البيت
```

### Testimonials + CTA نهائي

---

## 5. المنتجات التفصيلية

### أ) تيشيرتات (T-Shirts)

| البند | التفاصيل |
|---|---|
| المقاسات | 2-4، 4-6، 6-8، 8-10، 10-12 سنة |
| الألوان | أبيض / أسود / بنفسجي / وردي |
| الخامة | قطن 100% — 180 GSM |
| الطباعة | DTG (Direct to Garment) عالي الدقة |
| السعر | 89 ريال (قطعة) / 79 ريال (2+ قطع) |
| التخصيص | المستخدم يرفع صورة أفاتار طفله من التطبيق |

**Schema للطلب:**
```json
{
  "product_type": "tshirt",
  "avatar_image_url": "https://...",
  "hero_name": "محمد",
  "size": "6-8",
  "color": "white",
  "quantity": 1,
  "price_sar": 89
}
```

### ب) كتيبات القصص (Booklets)

| البند | التفاصيل |
|---|---|
| الحجم | A5 (148×210 mm) |
| الصفحات | 8 صفحات (غلاف + 6 مشاهد + خلفي) |
| الورق | ورق مصقول 200 gsm |
| الغلاف | كرتون مقوى |
| السعر | 59 ريال (كتيب واحد) |
| التخصيص | المستخدم يختار قصة من مكتبته الخاصة |

**Schema:**
```json
{
  "product_type": "booklet",
  "story_id": "uuid-from-supabase",
  "story_title": "مغامرة محمد في الفضاء",
  "scenes": [...],
  "price_sar": 59
}
```

### ج) مجلة حكواتي الشهرية (Subscription)

| البند | التفاصيل |
|---|---|
| النوع | اشتراك شهري |
| المحتوى | 4 قصص مختلفة + نشاطات + ألوان |
| الحجم | A4، 24 صفحة |
| السعر | 149 ريال/شهر (توصيل مجاني) |
| التجديد | تلقائي — يمكن الإلغاء |

### د) كروت شحن الكريدت (Credit Cards)

هذه **بديل IAP داخل التطبيق** للسوق السعودي/الخليجي حيث البطاقات الائتمانية غير منتشرة.

| الباقة | الكريدت | السعر |
|---|---|---|
| Mini | +50 ⭐ | 19 ريال |
| Starter | +100 ⭐ | 35 ريال |
| Pro | +250 ⭐ | 79 ريال |
| Ultimate | +500 ⭐ | 149 ريال |

**آلية الاستخدام:**
1. العميل يشتري الكرت
2. يستلم كود 12 خانة عبر البريد
3. في التطبيق → Store → "استبدال كرت" → يُدخل الكود
4. الكريدت يُضاف فوراً لحسابه

**Schema:**
```json
{
  "product_type": "credit_card",
  "credits": 100,
  "code": "XXXX-XXXX-XXXX",
  "status": "unused | used",
  "user_id": null,
  "redeemed_at": null,
  "price_sar": 35
}
```

---

## 6. المعمارية التقنية

### البنية الأساسية (على Hostinger)

```
Frontend:  HTML/CSS/JS ثابت (أسرع + أرخص)
         أو Next.js/Astro (أفضل SEO)

Backend:  Supabase (نفس DB التطبيق)
         - جدول orders
         - جدول products
         - جدول invoices
         - جدول credit_cards
         
Payment:  Stripe / Moyasar (محلي سعودي)

Webhook:  Edge Function في Supabase
         تستقبل webhook من Stripe → تنشئ order → ترسل إشعار
```

### جداول Supabase الجديدة المطلوبة

```sql
-- طلبات المتجر
CREATE TABLE orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  product_type TEXT CHECK (product_type IN ('tshirt', 'booklet', 'magazine', 'credit_card')),
  product_data JSONB,  -- تفاصيل المنتج (مقاس، لون، story_id...)
  quantity INT DEFAULT 1,
  total_sar NUMERIC(10,2),
  status TEXT CHECK (status IN ('pending', 'paid', 'printing', 'shipped', 'delivered', 'cancelled')),
  payment_provider TEXT,  -- stripe / moyasar
  payment_id TEXT,
  shipping_address JSONB,
  tracking_number TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- الفواتير
CREATE TABLE invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id UUID REFERENCES orders(id),
  invoice_number TEXT UNIQUE,
  pdf_url TEXT,
  issued_at TIMESTAMPTZ DEFAULT NOW(),
  total_sar NUMERIC(10,2),
  vat_sar NUMERIC(10,2)
);

-- كروت شحن الكريدت
CREATE TABLE credit_cards (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT UNIQUE NOT NULL,
  credits INT NOT NULL,
  price_sar NUMERIC(10,2),
  status TEXT CHECK (status IN ('unused', 'used', 'expired')) DEFAULT 'unused',
  redeemed_by UUID REFERENCES auth.users(id),
  redeemed_at TIMESTAMPTZ,
  order_id UUID REFERENCES orders(id),
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### تدفق الطلب (Order Flow)

```
1. المستخدم في المتجر → يختار منتج
2. يضيف للسلة → Checkout
3. Stripe Checkout Session → بطاقة/Apple Pay
4. Payment success → Stripe webhook
5. Supabase Edge Function:
   a. يُنشئ row في orders (status=paid)
   b. يُنشئ invoice PDF ويرفعه لـ Supabase Storage
   c. يرسل بريد تأكيد للعميل
   d. إذا credit_card → ينشئ code ويُرسله
   e. إذا منتج مادي → يرسل Webhook لشريك الطباعة
6. شريك الطباعة → ينتج ويشحن
7. تحديث status تلقائي عبر API الشريك
```

### Edge Function: stripe-webhook

```typescript
// supabase/functions/stripe-webhook/index.ts
import Stripe from 'https://esm.sh/stripe@14';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const stripe = new Stripe(Deno.env.get('STRIPE_SECRET_KEY')!);
const supabase = createClient(
  Deno.env.get('SUPABASE_URL')!,
  Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
);

Deno.serve(async (req) => {
  const sig = req.headers.get('stripe-signature')!;
  const body = await req.text();
  
  const event = stripe.webhooks.constructEvent(
    body, sig, Deno.env.get('STRIPE_WEBHOOK_SECRET')!
  );

  if (event.type === 'checkout.session.completed') {
    const session = event.data.object;
    const metadata = session.metadata;
    
    // 1. Create order
    const { data: order } = await supabase.from('orders').insert({
      user_id: metadata.user_id,
      product_type: metadata.product_type,
      product_data: JSON.parse(metadata.product_data),
      total_sar: session.amount_total / 100,
      status: 'paid',
      payment_provider: 'stripe',
      payment_id: session.payment_intent,
    }).select().single();

    // 2. If credit_card → generate code
    if (metadata.product_type === 'credit_card') {
      const code = generateCode();
      await supabase.from('credit_cards').insert({
        code,
        credits: metadata.credits,
        price_sar: session.amount_total / 100,
        order_id: order.id,
      });
      await sendEmail(session.customer_email, 'credit_card', { code });
    }

    // 3. If physical → notify printer
    if (['tshirt', 'booklet', 'magazine'].includes(metadata.product_type)) {
      await notifyPrinter(order);
    }

    // 4. Generate invoice PDF
    await generateInvoice(order.id);
  }

  return new Response('OK');
});
```

---

## 7. الأساسيات التي يحتاجها أي نموذج AI لبناء المتجر

### أ) الأصول الأساسية (Assets)

```
✅ شعار حكواتي (SVG + PNG)
✅ ألوان الهوية (ملف css/tailwind.config.js)
✅ الخطوط (Cairo + Tajawal من Google Fonts)
✅ أيقونة النجمة ⭐ (SVG)
✅ صور نموذجية للمنتجات (mockups)
```

### ب) المعلومات التجارية

```
✅ قائمة المنتجات + أسعارها
✅ المقاسات والألوان المتاحة (للتيشيرتات)
✅ مواصفات الطباعة (الورق، الحجم، الجودة)
✅ شريك الطباعة (Printify / Printful / محلي)
✅ بوابة الدفع المعتمدة (Stripe / Moyasar)
✅ سياسة الشحن (المناطق + الأسعار + الوقت)
✅ سياسة الاسترداد
```

### ج) الربط مع Supabase

```
✅ Supabase URL + Anon Key
✅ Service Role Key (للـ Edge Functions فقط، لا تُنشر)
✅ RLS Policies لجدول orders
✅ Storage bucket للفواتير PDF
```

### د) البريد الإلكتروني والإشعارات

```
✅ Resend / SendGrid API Key
✅ قوالب البريد:
   - تأكيد الطلب
   - شحن الطلب
   - كرت الكريدت (مع الكود)
   - الفاتورة
```

---

## 8. Prompt جاهز لأي نموذج AI لبناء المتجر

```markdown
أنت مطور ويب متخصص. ابنِ متجر إلكتروني كامل على:
الدومين: hikayati.com (Hostinger shared hosting)

**الهوية البصرية** (إلزامي):
- بنفسجي #2D0B5A، برتقالي #FF8C00، ذهبي #FFD700
- خلفية داكنة #0D0D1A، نص أبيض #F5F0FF
- خطوط: Cairo (عناوين) + Tajawal (نصوص)
- RTL دائماً
- Glassmorphism خفيف، حواف 14px

**المنتجات الأساسية:**
1. تيشيرت بشخصية الطفل — 89 ريال
2. كتيب قصة مطبوع — 59 ريال
3. اشتراك مجلة شهرية — 149 ريال/شهر
4. كروت شحن كريدت — 19 / 35 / 79 / 149 ريال

**التقنيات المطلوبة:**
- Frontend: Next.js + Tailwind CSS (أو HTML ثابت إذا كان shared hosting محدود)
- Backend: Supabase (URL: [from .env])
- Payment: Stripe Checkout
- Email: Resend

**جداول Supabase الجاهزة:**
[الصق الـ SQL من قسم 6]

**الصفحات المطلوبة:**
[الصق هيكل الصفحات من قسم 3]

**متطلبات UX:**
- عربية RTL
- جوال أولاً (Mobile First)
- سرعة عالية (Lighthouse 90+)
- SEO كامل (Arabic meta tags)

**الربط مع التطبيق:**
- كروت الكريدت تُستبدل في تطبيق Flutter عبر الكود الـ 12 خانة
- طلبات التيشيرت تستقبل صورة الأفاتار من Supabase Storage
- طلبات الكتيب تقرأ story_id من جدول stories في Supabase

ابدأ ببناء:
1. الصفحة الرئيسية
2. صفحة المنتجات
3. صفحة منتج مفرد
4. السلة + Checkout
5. Stripe Webhook Edge Function
6. صفحة "طلباتي"

سلّم: كود نظيف + README كامل + Git commits واضحة.
```

---

## 9. Timeline المقترح

### المرحلة أ — قبل الراتب (الأسبوع الحالي)
- [ ] تحديد شريك الطباعة (Printify / Printful / محلي)
- [ ] جمع صور نموذجية للمنتجات (5 تيشيرتات، 3 كتيبات)
- [ ] تسجيل حساب Stripe / Moyasar
- [ ] كتابة نصوص المنتجات النهائية

### المرحلة ب — بعد الراتب (الأسبوع الأول)
- [ ] إنشاء جداول Supabase الجديدة (orders, invoices, credit_cards)
- [ ] رفع الهيكل الأساسي (HTML/Next.js) على Hostinger
- [ ] ربط الدومين hikayati.com

### المرحلة ج — بعد الراتب (الأسبوع الثاني)
- [ ] Stripe webhook + Edge Functions
- [ ] نظام كروت الكريدت (توليد + استبدال)
- [ ] صفحة "طلباتي" في التطبيق (قراءة من Supabase)
- [ ] اختبار تدفق الطلب الكامل

### المرحلة د — الإطلاق
- [ ] صور إعلانية + فيديو
- [ ] إطلاق المتجر مع 3 منتجات فقط (تيشيرت + كتيب + كرت كريدت)
- [ ] المجلة الشهرية لاحقاً بعد الطلب الأول

---

## 10. ملاحظات استراتيجية

### ربح ذكي من كروت الكريدت
- كرت 100 ⭐ بـ 35 ريال = 0.35 ريال/⭐
- نفس الكريدت في App Store بـ $8.99 ≈ 34 ريال لكن Apple يأخذ 30% (24 ريال صافي)
- **المتجر الخارجي يعطيك 35 ريال كاملاً** — ربح أعلى
- ضع كروت بأسعار ترويجية أرخص من App Store → يدفع المستخدمون من المتجر بدل IAP

### عزل Apple/Google بالصياغة
- في التطبيق: **لا تذكر** "اشتر من hikayati.com واحصل على كريدت"
- في المتجر الخارجي: اعرض كروت الكريدت كمنتج مستقل
- **الرابط من التطبيق → المتجر** يجب أن يكون عبر زر بريء: "منتجات حكواتي" بدون وعد بكريدت

### استخدم نفس Supabase
- لا تُنشئ DB منفصلة
- نفس جدول `profiles` يحتوي الرصيد
- استبدال الكرت من داخل التطبيق يزيد `credits` مباشرة
- فواتيرك ومستخدميك في مكان واحد

---

## 11. المخرجات النهائية لهذه الخطة

بعد تنفيذ هذه الخطة سيكون عندك:

1. ✅ متجر كامل على hikayati.com
2. ✅ 4 منتجات مباشرة للبيع
3. ✅ نظام دفع آمن (Stripe)
4. ✅ فواتير تلقائية PDF
5. ✅ بديل IAP (كروت الكريدت) — عمولة أقل
6. ✅ تكامل كامل مع التطبيق عبر Supabase
7. ✅ هوية بصرية موحّدة مع التطبيق
8. ✅ جاهز للإعلانات وSEO

**الوقت المتوقع للتنفيذ:** 2-3 أسابيع بعد الراتب مع AI assistance



============================================================
FILE: D:\Hikayati.com\خطة_المشروع\جاهزية_الإطلاق.md
============================================================

# جاهزية الإطلاق — Launch Readiness
**آخر تحديث:** 2026-04-23
**الحالة:** قبل ربط الدفع

---

## ✅ جاهز للإنتاج (Production-Ready)

| الميزة | الملف | الحالة |
|---|---|---|
| Login / Session | `lib/core/router/app_router.dart` | ✅ مقفل |
| رصيد لحظي (Realtime) | `lib/features/home/screens/home_screen.dart` | ✅ مقفل |
| خصم بعد النجاح فقط + anti-double | `lib/application/use_cases/generate_story_use_case.dart` | ✅ مقفل |
| فتح قصة خاصة بدون خصم | `lib/core/router/app_router.dart` (fromLibrary) | ✅ مقفل |
| صفحات قانونية (4) | `lib/features/legal/` | ✅ مقفل |
| هوية بصرية + ⭐ موحّدة | `lib/core/theme/` | ✅ مقفل |
| نظام ألوان أحمر/أخضر/برتقالي | `lib/features/store/screens/store_screen.dart` | ✅ مقفل |
| تسعير ديناميكي لحظي في إنشاء القصة | `lib/features/story_engine/screens/story_creation_screen.dart` | ✅ مقفل |
| متجر كريدت (4 باقات) — بصري فقط | `lib/features/store/screens/store_screen.dart` | ✅ جاهز لـ IAP |
| الشاشة الرئيسية كاملة | `lib/features/home/screens/home_screen.dart` | ✅ مقفل |
| المكتبة الخاصة | `lib/features/library/` | ✅ مقفل |
| المكتبة العامة + unlock | `lib/application/use_cases/unlock_public_story_use_case.dart` | ✅ مقفل |

---

## ⏳ مؤجل إلى بعد الراتب (لا يمنع إطلاق مغلق)

| البند | السبب | الخطوة التالية |
|---|---|---|
| IAP Apple StoreKit | يحتاج Apple Developer + اشتراك | بعد الراتب |
| Google Play Billing | يحتاج Play Console + اشتراك | بعد الراتب |
| Moyasar (المتجر الخارجي) | يحتاج تسجيل تجاري | بعد الراتب |
| Stripe (دولي) | بعد Moyasar | المرحلة الثانية |
| ElevenLabs (cloned voice) | فاتورة معلّقة | يوم الاثنين قبل الإطلاق |
| متجر Hostinger (hikayati.com) | يحتاج IAP + Moyasar أولاً | بعد الدفع |

---

## 📦 Product IDs مقترحة للـ IAP

```
com.hikayati.credits.50      → +50 ⭐   19 ريال
com.hikayati.credits.100     → +100 ⭐  35 ريال
com.hikayati.credits.250     → +250 ⭐  79 ريال
com.hikayati.credits.500     → +500 ⭐  149 ريال
```

---

## 🧪 المسارات الأربعة للاختبار قبل الإطلاق

1. إنشاء قصة عادية (بدون أفاتار/صوت) → خصم 10 ⭐
2. إنشاء قصة مع أفاتار + صوت مستنسخ → خصم 40 ⭐
3. فتح قصة من المكتبة الخاصة → **صفر خصم**
4. فتح قصة من المكتبة العامة (غير مفتوحة) → خصم 10 ⭐

---

## 📝 نصوص شاشات الشراء (IAP)

### نجاح الشراء
> "تم إضافة [X] ⭐ إلى رصيدك\! رصيدك الحالي: [Y] ⭐"

### فشل الشراء
> "لم يتم الشراء. يمكنك المحاولة مرة أخرى أو التواصل معنا."

### الشراء معلّق
> "طلبك قيد المراجعة، سيُضاف الرصيد خلال دقائق."




============================================================
FILE: D:\Hikayati.com\HIKAYATI_CANONICAL_SOURCE_OF_TRUTH.md
============================================================

# HIKAYATI_CANONICAL_SOURCE_OF_TRUTH.md

## الغرض
هذا الملف هو **مرجع الحسم** للمشروع الحالي.
يوضع في **جذر المشروع** ويُقرأ قبل أي تنفيذ.
إذا تعارض أي فهم سابق أو تقرير أو اجتهاد من وكيل مع هذا الملف، **يُرفض الاجتهاد** ويُعتمد هذا الملف.

---

## 1) ما هو المشروع الحالي؟
المشروع الحالي هو:
**إعادة بناء حكواتي من الصفر** اعتماداً على ملف **"من الصفر"** كمرجع تأسيسي لبناء المنتج الحالي.

### مهم جداً
- **هذا المشروع ليس استكمالاً تقنياً للنسخة الضائعة.**
- **هذا المشروع ليس ترميمًا مباشرًا للنسخ القديمة.**
- النسخ القديمة، المحادثات القديمة، وتقارير الاسترجاع:
  - تُستخدم فقط كمرجع لفهم الرؤية التجارية والمنتج الذي كان مطلوباً الوصول إليه.
  - **ولا تُستخدم كمصدر قرار أعلى من ملف "من الصفر" في البنية الحالية.**

---

## 2) ما وظيفة الملفات القديمة؟
الملفات القديمة والمحادثات القديمة دورها فقط:
- توضيح ما الذي كان المستخدم يريد الوصول إليه.
- كشف الأخطاء السابقة والانهيارات والخلط.
- تذكير الفريق أن المنتج النهائي المقصود أكبر من مجرد "مولد قصة".

لكنها **لا تعطي الإذن** بإدخال سلوك جديد يخالف المشروع الحالي أو يغيّر التسعير أو يقلب المنطق.

---

## 3) حقيقة المنتج
حكواتي ليس مجرد شاشة توليد قصة.
المنتج المقصود يتكون من هذه الأعمدة:

1. **اصنع بطلك**
   - نظام الأفاتار
   - جزء أساسي من هوية المنتج
   - ليس ميزة تجميلية
   - وليس ميزة مؤجلة

2. **اروي القصة بصوتك**
   - نظام الصوت / استنساخ الصوت
   - جزء أساسي من تميز المنتج
   - ليس إضافة ثانوية

3. **القصة السينمائية**
   - النص
   - الصورة
   - الصوت
   - العرض السينمائي

4. **المكتبة الخاصة**
   - قصص المستخدم نفسه

5. **المكتبة العامة**
   - قصص مولدة مسبقاً
   - تُفتح بالكريدت

6. **المتجر**
   - لاحقاً للبيع الملموس والمنتجات المطبوعة والملفات
   - لكن لا يسبق تفعيل أعمدة المنتج الأساسية

---

## 4) المرجع الأعلى في القرارات
ترتيب المراجع في هذا المشروع يكون كالتالي:

1. هذا الملف `HIKAYATI_CANONICAL_SOURCE_OF_TRUTH.md`
2. ملف `HIKAYATI_MASTER_SPEC_KIT_FULL.md`
3. ملف "من الصفر"
4. ملفات الحالة الحالية في جذر المشروع
5. الملفات القديمة والمحادثات القديمة كمرجع سياقي فقط

---

## 5) تصحيح سوء الفهم الحالي (إلزامي)
### الخطأ الذي وقع فيه الوكيل
الوكيل قام بإضافة:
- **مكافأة +10 كريدت عند إنشاء الأفاتار لأول مرة**

### هذا السلوك مرفوض
لأن منطق المشروع الحالي هو:
- الأفاتار **خيار مدفوع/مكلف**
- الصوت **خيار مدفوع/مكلف**
- كلاهما **يزيدان تكلفة توليد القصة**
- وليس منطق المشروع أن يحصل المستخدم على "مكافأة" لأنه فعّل الأفاتار

### القرار الصحيح
- **لا توجد مكافأة +10 كريدت عند إنشاء الأفاتار**
- الصحيح:
  - إذا فعّل المستخدم الأفاتار داخل توليد القصة:
    - يضاف **+10 كريدت** على تكلفة التوليد
  - إذا فعّل الصوت:
    - يضاف **+10 كريدت** على تكلفة التوليد
  - إذا فعّل الاثنين:
    - تجمع القيم على السعر الأساسي

---

## 6) القواعد التجارية الأساسية (Canonical Business Rules)
### الأسعار الأساسية الحالية
هذه القيم تُعامل كمرجع أولي حتى تُعدل صراحة:

- **توليد القصة الأساسي** = السعر الأساسي الحالي في التطبيق
- **الأفاتار داخل التوليد** = +10 كريدت
- **الصوت / اروي القصة بصوتك** = +10 كريدت
- **فتح قصة من المكتبة العامة** = 10 كريدت

### الخطط
- **Plus** = 20 كريدت
- **Pro** = 50 كريدت + مشاهدة 5 قصص من المكتبة العامة شهرياً

> لا يجوز لوكيل أن يغيّر هذه القواعد من نفسه.

---

## 7) قواعد واجهات المنتج
### الصفحة الرئيسية
يجب أن تحتوي على:
- **مكتبتي** (بدلاً من القصص المميزة)
- **المكتبة العامة**
- عناصر الوصول العليا

### القائمة / الحساب
من الحساب أو القائمة العليا يجب أن يصل المستخدم إلى:
- مكتبتي
- اصنع بطلك
- اروي القصة بصوتك
- الشحن
- المتجر
- سياسة الخصوصية

### المكتبة الخاصة
- تعرض قصص المستخدم فقط
- مرتبطة بـ `user_id`
- لها مكان واضح وليس مجرد واجهة غير مفعلة

### المكتبة العامة
- قصص مولدة مسبقاً
- القصة مقفلة أولاً
- عند الضغط عليها:
  - تظهر رسالة أن فتحها = 10 كريدت
- بعد الفتح:
  - تصبح مملوكة بشكل دائم للمستخدم

---

## 8) سياسة الاحتفاظ بالمحتوى
### المكتبة الخاصة
- القصص تبقى **90 يوماً (3 أشهر)** كسياسة تشغيلية حالية
- ثم يمكن حذفها بآلية مجدولة لاحقاً
- لكن لا يُطبق الحذف إلا بعد تفعيل المكتبة الخاصة بشكل كامل واختباره

---

## 9) حماية المحتوى
هذا مطلب منتج مهم:
- يجب منع التصوير أو التسجيل السهل من:
  - المكتبة الخاصة
  - شاشة السينما

لكن:
- لا يبدأ الوكيل بتنفيذه قبل تثبيت التدفقات الأساسية
- وعند تنفيذه يكون كدفعة مستقلة واضحة

---

## 10) ما الذي يُنفذ الآن؟
### المرحلة الحالية الفعلية
هذه ليست Phase عشوائية جديدة.
هذه مرحلة اسمها:

## **PRODUCT ACTIVATION CORRECTION**

ومعناها:
- تفعيل وربط ما هو موجود شكلياً
- تصحيح الانحرافات
- منع الاجتهادات التجارية الخاطئة
- تثبيت أعمدة المنتج الأساسية

### أولويات التنفيذ الحالية بالترتيب
1. إزالة منطق **مكافأة +10** للأفاتار
2. تثبيت القاعدة الصحيحة:
   - الأفاتار = تكلفة إضافية داخل التوليد
3. تفعيل الدخول الحقيقي إلى:
   - مكتبتي
   - اصنع بطلك
   - اروي القصة بصوتك
4. استبدال "القصص المميزة" بـ "مكتبتي"
5. إظهار تكلفة الأفاتار والصوت داخل شاشة التوليد بوضوح
6. خصم الإجمالي الصحيح عند الإنشاء
7. تفعيل فتح القصص العامة بـ 10 كريدت
8. بعدها فقط ننتقل إلى الدفع والمتجر

---

## 11) ممنوعات على الوكلاء
### ممنوع على أي وكيل:
- افتراض أن النسخة القديمة هي المرجع الأعلى
- اختراع منطق مكافآت أو خصومات غير منصوص عليه
- اعتبار الأفاتار Feature تجميلية مؤجلة
- القفز إلى Settings أو Store Payments أو Video
- تغيير أسعار الكريدت من نفسه
- تغيير المعمارية بلا داعٍ
- الاعتماد على ذاكرة المحادثات إذا تعارضت مع هذا الملف

---

## 12) طريقة العمل الإلزامية
### عند كل جلسة
يجب على مدير الوكلاء أن يبدأ بـ:
1. قراءة هذا الملف
2. قراءة `HIKAYATI_MASTER_SPEC_KIT_FULL.md`
3. قراءة `HIKAYATI_RUNTIME_STATUS.md`
4. قراءة `task.md`
5. ثم تحديد:
   - Current Batch
   - Next Batch
   - ما الذي تغير
   - ما الذي يمنع الانتقال

---

## 13) قاعدة الحسم
إذا ظهر تعارض بين:
- تقرير سابق
- محادثة سابقة
- اجتهاد وكيل
- تنفيذ حالي

فالحسم يكون بهذا الترتيب:
1. هذا الملف
2. حالة المشروع الحالية الفعلية
3. المرجع التأسيسي "من الصفر"

---

## 14) الرسالة المختصرة التي يجب أن يفهمها أي وكيل
**المشروع الحالي هو إعادة بناء من الصفر للوصول إلى منتج سبق أن كان المستخدم يريد الوصول إليه.
النسخ القديمة ليست أساس التنفيذ الحالي.
الأفاتار والصوت من أساس المنتج.
لكن تسعيرهما الصحيح هو كلفة إضافية على التوليد، وليس مكافأة للمستخدم.**

---

## 15) أمر التنفيذ الفوري
إذا قرأ الوكيل هذا الملف، فالخطوة التالية المباشرة هي:

### Batch A
- إزالة أي منطق يمنح +10 كريدت عند إنشاء الأفاتار
- تعديل المنطق بحيث يصبح:
  - الأفاتار = +10 كريدت على التوليد
  - الصوت = +10 كريدت على التوليد

### Batch B
- تفعيل مكتبتي وربطها بالمستخدم
- استبدال القصص المميزة بها في الرئيسية

### Batch C
- تفعيل الدخول إلى:
  - اصنع بطلك
  - اروي القصة بصوتك
  - من القائمة / الحساب

### Batch D
- تفعيل Unlock للمكتبة العامة بـ 10 كريدت

لا شيء خارج ذلك الآن.

---

## 16) ملاحظة أخيرة
إذا ضاع السياق في أي جلسة:
- لا يجوز اختراع تفسير جديد
- ولا يجوز الرجوع مباشرة للنسخ القديمة واتخاذها كأساس
- يجب الرجوع أولاً إلى هذا الملف ثم إلى ملف الـ Master Spec ثم إلى حالة المشروع الحالية



============================================================
FILE: D:\Hikayati.com\HIKAYATI_MASTER_SPEC_KIT_FULL.md
============================================================

# HIKAYATI_MASTER_SPEC_KIT_FULL.md

# 🚀 HIKAYATI — MASTER SPEC KIT (PRO VERSION)

هذا الملف هو:
👉 الدستور الكامل
👉 نظام التشغيل
👉 خطة التنفيذ
👉 نظام إدارة الوكلاء

أي Agent أو AI يعمل على المشروع يجب أن يعتمد هذا الملف فقط.

---

# 🎯 1. تعريف المشروع

## الاسم:
Hikayati (حكواتي)

## النوع:
AI Storytelling Platform + Cinematic Experience + Monetization System

## الهدف:
تحويل الطفل إلى بطل قصة مخصصة تُعرض كتجربة سينمائية قابلة للحفظ والبيع.

---

# 🧠 2. المعمارية (Architecture)

## Layers:

### 1. Presentation
- Flutter UI
- Screens only
- NO business logic

### 2. Application
- UseCases فقط
- orchestration logic

### 3. Domain
- Models
- Entities

### 4. Services
- AI Services
- Backend Services

### 5. Backend
- Supabase

---

# ⚠️ 3. القوانين الصارمة (NON-NEGOTIABLE)

1. UI لا تستدعي AI مباشرة
2. CinemaScreen = عرض فقط
3. IntroCinematicScreen = تحميل فقط
4. Story generation فقط داخل UseCase
5. Avatar لا يدخل StoryWizard
6. كل Service منفصلة
7. لا Feature خارج المرحلة الحالية

---

# 🔄 4. التدفق الأساسي (FLOW)

StoryWizard → GenerateStoryUseCase → UnifiedEngine → Intro → Cinema

---

# 🧠 5. Story Engine

## المدخلات:
- heroName
- age
- style

## المخرجات:
- storyData

---

# 🗄️ 6. قاعدة البيانات

Tables:
- profiles
- stories
- public_stories
- unlocked_stories
- credits
- orders

---

# 🛒 7. نظام الربح

- Credits
- Unlock
- Digital
- Physical (future)

---

# 🧪 8. مراحل المشروع (PHASES)

## Phase A — Stabilization
Fix runtime issues

## Phase 1 — Story Flow
Generate + Cinema

## Phase 2 — Auth + Save
Auth + Private Library

## Phase 3 — Public Library
Explore + Unlock

## Phase 4 — Settings

## Phase 5 — Store

## Phase 6 — Avatar + Voice

## Phase 7 — Advanced AI

---

# 🧩 9. نظام التنفيذ (BATCH SYSTEM)

## كل العمل يتم عبر Batches فقط

### كل Batch يحتوي:
- الهدف
- الملفات
- الوكلاء
- التحقق

---

# 🧠 10. إدارة الوكلاء

## القواعد:
- لا يعمل أكثر من 3 وكلاء إلا عند الحاجة
- كل وكيل له ملفات محددة
- ممنوع لمس ملفات خارج المهمة

---

# 🔁 11. نظام الاستئناف (CRITICAL)

عند التوقف:

يجب حفظ:
- Current Phase
- Current Batch
- NEXT_BATCH

عند العودة:
- يبدأ من NEXT_BATCH مباشرة
- لا يسأل المستخدم

---

# 📊 12. ملفات الحالة

## REQUIRED:

### HIKAYATI_RUNTIME_STATUS.md
### task.md
### سجل_الأخطاء.md

---

# 🧨 13. تقليل التوكن (CRITICAL)

## ممنوع:
- تحليل المشروع كامل كل مرة
- إعادة شرح Spec
- تنفيذ مراحل كاملة دفعة واحدة

## المطلوب:
- Batch صغيرة
- تحقق بعد كل Batch

---

# 🔧 14. WORKFLOW

1. Read State
2. Define Batch
3. Execute
4. Verify
5. Update State

---

# 🧪 15. VERIFICATION

### كل Batch:
- flutter analyze
- runtime test

---

# 🎯 16. Definition of Done

- Create story
- Watch story
- Save story
- Unlock story

---

# 🚨 17. BLOCKER POLICY

إذا ظهر خطأ:
- سجل في سجل_الأخطاء.md
- أنشئ Batch خاصة
- لا تكمل

---

# 🚀 18. EXECUTION COMMAND

اقرأ هذا الملف فقط.

ابدأ من الحالة الحالية.

قسّم العمل إلى Batches.

نفّذ Batch واحدة فقط في كل مرة.

تحقق.

حدث الحالة.

ثم أكمل.

---

# 🔚 النهاية

هذا الملف = النظام الكامل

أي انحراف عنه = خطأ معماري



============================================================
FILE: D:\Hikayati.com\HIKAYATI_AGENT_MASTER_SPEC.md
============================================================

# HIKAYATI AGENT MASTER SPEC

## 0) Purpose of this file
This file is the single source of truth for any agent, AI builder, or coding manager working on Hikayati.
It exists to prevent architectural drift, feature mixing, and random implementation.

Any agent reading this file must follow it as an execution contract, not as inspiration.
If any existing code conflicts with this file, this file wins unless an explicit newer decision document says otherwise.

---

## 1) Product definition
Hikayati is an AI storytelling platform for families.
The product transforms the child from a passive listener into the hero of a personalized story, then transforms the story into a cinematic visual/audio experience, then into saved memories and optionally into products sold via the in-app store.

Hikayati is NOT:
- only a story generator
- only a kids app
- only a cinema viewer
- only a store

Hikayati IS:
- a personalized story creation product
- a cinematic storytelling experience
- a library product
- a credits-based monetization product
- a store-enabled memory product
- a platform that can later expand into avatar, voice clone, video, education, and schools

---

## 2) Non-negotiable architectural laws
These rules are mandatory.

### LAW 1: UI does not generate stories
Flutter screens collect input, trigger actions, show loading states, and render output.
They must not own core orchestration logic.

### LAW 2: CinemaScreen is display-only
CinemaScreen must never contain story generation logic, image generation logic, avatar analysis logic, purchase logic, or persistence logic.
It only receives storyData and plays/displays it.

### LAW 3: Avatar is NOT part of the main story form layout logic
Avatar is an optional input source.
It is not the center of the Story Wizard.
It must not take over the create-story screen.
The agent must not redesign the Story Wizard around avatar.

### LAW 4: Avatar belongs to its own domain
Avatar setup/editing belongs in:
- Avatar Lab
- Account > Avatar Management
- optional picker inside Story Wizard

It does NOT belong as the dominant logic or dominant page content inside Story Wizard.

### LAW 5: Services are isolated
The following are separate services:
- text generation service
- image generation service
- normal TTS narration service
- voice cloning service
- avatar/vision analysis service
- payments service
- credits service

No agent may collapse them into one confusing mega-service.

### LAW 6: Public Library and Private Library are different domains
Public Library:
- discovery
- marketing
- featured stories
- free stories
- paid stories by credit
- seasonal content

Private Library:
- stories created by the user
- stories unlocked by the user
- stories purchased by the user
- saved story assets

### LAW 7: Store is a first-class feature, but not phase-one entrypoint
The store is core to the long-term business model.
However, the app must not open directly into the Store as the main first screen.
The first screen is Home.

### LAW 8: Build in phases
The agent must not implement all systems in one pass.
It must build by approved phases only.

### LAW 9: Child privacy is architectural
Do not design flows that permanently store sensitive child media by default.
Avatar is optional. Child voice cloning is forbidden. Parent voice cloning only.

### LAW 10: If unclear, preserve separation of concerns
When in doubt, keep logic outside screens and preserve modular boundaries.

---

## 3) Target system layers

### Layer A — Flutter Application Layer
Responsibilities:
- screens
- routing
- widgets
- user input forms
- media playback UI
- loading and error states
- localization UI

### Layer B — Story Engine / Application Orchestration Layer
Responsibilities:
- receives create-story request
- validates inputs
- decides whether to generate via AI or reuse existing content
- triggers text/image/audio services
- builds final storyData object
- triggers save/unlock/post-processing workflows

This is the real coordinator.

### Layer C — AI Services Layer
Independent services:
1. Text Generation Service
2. Image Generation Service
3. Narration TTS Service
4. Voice Clone Service
5. Avatar Vision Service

### Layer D — Backend Layer (Supabase)
Responsibilities:
- auth
- profiles
- credits balance
- stories storage
- public stories
- unlock records
- orders
- purchases
- storage buckets
- RPC / edge functions

---

## 4) Official feature phases
The agent must build only according to this roadmap.

### Phase 1 — Core Experience
Mandatory deliverables:
- Home screen
- Story Wizard screen
- Story Engine orchestration
- Text generation
- Image generation
- Cinema screen

Goal:
A user can create a personalized story and watch it in cinematic mode.

### Phase 2 — Real User + Persistence
Mandatory deliverables:
- authentication
- profile loading
- save story
- private library

Goal:
Created stories can be saved and replayed.

### Phase 3 — Public Library + Credits
Mandatory deliverables:
- public library
- free vs paid stories
- unlock flow
- deduct_credits RPC or equivalent secure backend action

Goal:
A user can unlock public stories using credits.

### Phase 4 — Settings + Policies + Standard Audio
Mandatory deliverables:
- settings screen
- app language
- story language
- sound/music preferences
- privacy policy page
- terms page
- standard narration playback

### Phase 5 — Store + Payments
Mandatory deliverables:
- store screen
- credit packages
- product catalog shell
- digital export shell
- payment integration
- order creation tracking

### Phase 6 — Advanced Personalization
Mandatory deliverables:
- avatar lab
- avatar management
- voice clone for parent only

### Phase 7 — Expansion
Optional later expansion:
- Hakim assistant
- video generation
- education mode
- schools mode

Important phase rule:
If the current project already has Cinema working, the next task is to stabilize the surrounding architecture and complete missing phase items, not to randomly jump to avatar or store dominance.

---

## 5) Screen map

### 5.1 HomeScreen
Purpose:
The main entrypoint of the app.

Contains:
- app branding/logo
- account access
- settings access
- language change
- featured public stories section
- create story primary CTA
- private library entry
- store entry
- policy links

Recommended top-to-bottom order:
1. top bar
2. hero banner
3. public stories preview
4. create story CTA
5. featured items / promotions
6. store teaser
7. account and policy links

### 5.2 StoryWizardScreen
Purpose:
Collect inputs needed to create a story.

Contains:
- child name
- child age
- story style
- image style
- narrator voice choice
- music on/off
- story language
- optional avatar selector
- visible credit cost
- generate button

Important:
Avatar is an optional section or selector.
It must not dominate the page.
It must not replace the normal story form.

Voice options:
- male narrator
- female narrator
- cloned parent voice (only if available)

Image style options:
- cartoon
- anime
- realistic
- avatar-assisted

Story style options:
- funny
- calm
- adventure
- educational
- exciting
- family

Music options:
- with music
- without music

### 5.3 CinemaScreen
Purpose:
Display the generated story as a cinematic experience.

Entry behavior:
After clicking Generate, user navigates to CinemaScreen immediately with loading/intro experience.

Cinema intro may include:
- dark background
- logo reveal
- short intro music
- opening phrase

Cinema elements:
- scene image
- scene text
- scene counter
- progress indicator
- next/previous if needed
- sound controls
- music controls
- close button
- save/open library CTA after completion

Strict rule:
CinemaScreen only displays storyData.

### 5.4 PublicLibraryScreen
Purpose:
Discovery, marketing, and credit-based unlocks.

Shows:
- free stories
- premium stories
- seasonal stories
- featured stories
- educational stories later

Behavior:
- free story -> open directly
- paid story -> confirm deduction then unlock
- bundled item -> open purchase or unlock path

### 5.5 PrivateLibraryScreen
Purpose:
The user’s owned stories.

Contains:
- created stories
- unlocked stories
- purchased stories
- product-related story records later

Behavior:
- tap -> open story
- if empty -> honest empty state, no fake placeholders
- sort/filter by date/language/type/source

### 5.6 AccountScreen
Contains:
- display name
- email
- credits balance
- number of stories
- purchases
- cloned voice management
- avatar management
- language
- logout

### 5.7 SettingsScreen
Contains:
- app language
- default story language
- sound preferences
- music toggle
- reading speed
- image quality
- notifications
- privacy
- terms
- delete account

### 5.8 StoreScreen
Purpose:
Monetization hub.

Sells:
- credits packages
- PDF/booklet export
- later printed booklet
- later t-shirt / poster / sticker / seasonal items

Important:
Store is core business, but it is not the dominant starting screen.

### 5.9 AvatarLabScreen
Purpose:
Manage avatar setup separately from story generation.

Contains:
- upload avatar source images
- analyze images via Avatar Vision Service
- produce editable visual descriptor
- save avatar profile to backend
- preview avatar profile data

This screen exists to STOP avatar logic from polluting StoryWizard.

---

## 6) Button map and navigation contract
This section exists because the agent is getting lost on where each button should lead.

### HomeScreen buttons
- "أنشئ قصة" -> navigate to StoryWizardScreen
- "المكتبة العامة" section card tap -> navigate to PublicLibraryScreen or open specific story
- "مكتبتي" -> navigate to PrivateLibraryScreen
- "المتجر" -> navigate to StoreScreen
- "الحساب" -> navigate to AccountScreen
- "الإعدادات" -> navigate to SettingsScreen
- "السياسة / الشروط" -> navigate to policy pages

### StoryWizardScreen buttons/actions
- narrator voice selector -> updates local form state only
- image style selector -> updates local form state only
- avatar picker -> opens Avatar Picker modal or AvatarLabScreen selection flow
- add another avatar -> opens additional avatar slot section, does not replace main form
- "توليد" ->
  1. validate form
  2. optionally pre-check credits if generation is paid
  3. create CreateStoryRequest
  4. navigate to CinemaScreen with loading state
  5. trigger StoryEngine orchestration

Important prohibition:
Clicking avatar controls must NOT reroute the user into a full avatar-first app experience unless the user explicitly enters Avatar Lab.

### CinemaScreen buttons/actions
- close X -> exit to HomeScreen or previous safe route
- save story -> call save story use case, then show success
- open library -> navigate to PrivateLibraryScreen
- replay -> restart cinema playback only
- next/previous -> scene navigation only
- sound toggle -> toggle narration only
- music toggle -> toggle background music only

Cinema buttons must not:
- regenerate the story directly
- re-run avatar analysis directly
- open payment flow unexpectedly

### PublicLibraryScreen buttons/actions
- open free story -> open viewer/cinema directly
- open paid story -> confirm credit deduction -> unlock -> add to user ownership -> open
- buy package -> navigate to StoreScreen

### PrivateLibraryScreen buttons/actions
- open story -> CinemaScreen or Story Details screen
- reorder/filter -> local state/query update only

### StoreScreen buttons/actions
- buy credits -> payment flow
- buy digital export -> order flow
- buy physical product -> order flow
- back -> HomeScreen or previous screen

### AccountScreen buttons/actions
- manage avatar -> AvatarLabScreen
- manage cloned voice -> Voice Profile management flow
- logout -> auth logout and return to auth/home

### AvatarLabScreen buttons/actions
- upload image -> media picker
- analyze -> AvatarVisionService
- edit descriptor -> local form editor
- save -> persist avatar profile to backend
- use in next story -> return selected avatar profile to StoryWizardScreen

---

## 7) Core domain entities

### CreateStoryRequest
Fields:
- childName
- childAge
- storyStyle
- imageStyle
- narratorVoiceMode
- storyLanguage
- musicEnabled
- avatarProfileIds optional
- additionalCharacters optional
- userId optional

### StoryData
Fields:
- storyId
- title
- summary
- language
- coverImageUrl
- audioMode
- musicEnabled
- scenes[]

### SceneData
Fields:
- sceneNumber
- text
- imagePrompt
- imageUrl
- audioUrl optional

### AvatarProfile
Fields:
- id
- ownerUserId
- displayName
- descriptorText
- styleHints
- referenceAssets optional
- createdAt

---

## 8) Backend data model
Supabase owns these domains.

### profiles
- user_id
- display_name
- credits_balance
- language
- settings_json
- cloned_voice_profile_id nullable
- avatar_profile_summary nullable

### stories
- id
- user_id
- title
- summary
- language
- scenes_json
- cover_image_url
- audio_mode
- source_type
- created_at

### public_stories
- id
- title
- summary
- cover_image_url
- category
- access_type
- price_credits
- scenes_json
- status

### unlocked_stories
- id
- user_id
- public_story_id
- credits_paid
- unlocked_at

### orders
- id
- user_id
- story_id nullable
- product_type
- amount
- payment_status
- order_status
- created_at

### credits_transactions
- id
- user_id
- amount
- type
- reason
- reference_id
- created_at

### avatar_profiles
- id
- user_id
- display_name
- descriptor_json or descriptor_text
- reference_assets_json
- created_at
- updated_at

---

## 9) Required backend actions
At minimum, support these use cases.

### deduct_credits(user_id, amount, reason)
Secure backend operation.
Never trust client-only deduction.

### unlock_public_story(user_id, story_id)
Must verify access type and balance.

### save_story_result(payload)
Stores story data and links it to the user.

### save_avatar_profile(payload)
Stores avatar descriptor/profile separately from story generation.

---

## 10) Agent implementation rules
The coding agent must obey these rules.

1. Do not move avatar to the center of StoryWizard.
2. Do not place avatar analysis logic inside CinemaScreen.
3. Do not make StoreScreen the app home.
4. Do not add payment SDK complexity before the approved payment phase unless explicitly requested.
5. Do not keep core AI orchestration inside widget code.
6. Do not mix normal narration with voice cloning in one unclear service.
7. Do not fake library data when the library is empty.
8. Do not create hidden navigation routes with no map in this file.
9. Do not change screen responsibilities without updating this document.
10. If a feature seems related to avatar, first ask: does this belong to AvatarLab or only as an optional picker in StoryWizard?

---

## 11) Current project status assumptions
Based on existing progress, the project already reached a point where a cinematic story can be shown inside CinemaScreen.
This means:
- Cinema is already conceptually proven
- The problem is now architecture drift and unclear plan
- The agent must stop improvising and align with this master spec

Therefore, current priority is NOT “invent new product direction”.
Current priority IS:
- stabilize architecture
- finish missing phase boundaries
- restore screen responsibilities
- keep avatar modular and optional

---

## 12) Immediate correction plan for the current codebase
If the current codebase is confused, apply this order:

### Step 1
Confirm routes and screen ownership:
- Home
- StoryWizard
- Cinema
- PrivateLibrary
- PublicLibrary
- Store
- Account
- Settings
- AvatarLab

### Step 2
Refactor CinemaScreen so it only consumes StoryData.

### Step 3
Refactor StoryWizard so avatar is a small optional section, not the whole page.

### Step 4
Extract orchestration into StoryEngine / use case layer.

### Step 5
Keep AvatarVisionService separate and only called from AvatarLab or optional avatar selection flow.

### Step 6
Stabilize save flow and private library.

### Step 7
Only after that, implement public library + credits.

---

## 13) Suggested folder structure

lib/
  core/
    constants/
    theme/
    routes/
    utils/
  features/
    home/
    story_wizard/
    cinema/
    public_library/
    private_library/
    store/
    account/
    settings/
    auth/
    avatar_lab/
  application/
    story_engine/
    use_cases/
  services/
    ai_text/
    ai_image/
    ai_tts/
    ai_voice_clone/
    avatar_vision/
    payments/
    credits/
  data/
    models/
    repositories/
    datasources/
  backend/
    supabase/

---

## 14) Acceptance criteria for each major screen

### HomeScreen done when:
- it is the default entrypoint after auth/main routing
- create story button works
- store entry works
- library entries work
- there is no broken dead-end button

### StoryWizard done when:
- user can enter core story fields
- avatar is optional and not dominant
- generate button triggers story creation flow
- user transitions to cinema flow properly

### CinemaScreen done when:
- it renders received storyData only
- it can show scene image/text progression
- it does not own generation logic

### AvatarLab done when:
- user can upload source image(s)
- agent can analyze and save avatar profile
- avatar can later be selected from StoryWizard

### Store done when:
- it shows credit packages and/or product cards
- navigation works
- payment flow is phase-appropriate

---

## 15) Definition of done for the stable MVP baseline
The stable MVP baseline is complete when all of these are true:
- user lands on Home, not Store
- user can open StoryWizard
- user can generate a personalized story
- user is taken into Cinema flow
- Cinema displays story content correctly
- story can be saved
- saved stories appear in Private Library
- architecture keeps avatar optional and modular
- no screen owns another screen’s business logic

---

## 16) Final instruction to the agent manager
Do not improvise product structure.
Do not redistribute responsibilities arbitrarily.
Do not let AvatarLab consume StoryWizard.
Do not let Cinema become an engine.
Do not let Store become Home.

Build according to this file exactly.
When uncertain, preserve separation of concerns and phase order.




============================================================
FILE: D:\Hikayati.com\HIKAYATI_ORCHESTRATOR_PROTOCOL.md
============================================================

# HIKAYATI_ORCHESTRATOR_PROTOCOL.md

## Purpose
This file is the execution protocol for the manager agent and all worker agents.
It is mandatory. The manager agent must distribute work by phase, preserve state, and resume from the exact stopping point on the next session without requiring the user to restate context.

---

## Authority Order
The agents must obey documents in this order:
1. `HIKAYATI_AGENT_MASTER_SPEC.md`
2. `stabilization_report.md`
3. `architecture_report.md`
4. `implementation_plan.md`
5. `task.md`
6. This file `HIKAYATI_ORCHESTRATOR_PROTOCOL.md`

If two documents conflict, the higher document wins.

---

## Current Project Truth (must be assumed unless updated by a newer report)
- LAW 1 has been refactored toward a use-case flow.
- `StoryCreationScreen` must remain input-only.
- `IntroCinematicScreen` must remain loading/transition-only.
- `CinemaScreen` must remain display-only.
- Avatar features must remain isolated from story generation flow.
- No worker may place avatar generation logic inside Story Wizard or Cinema.

---

## Runtime Issues Already Observed
These are active defects and must be prioritized before any new feature work:
1. `HakeemService` chat model error:
   - runtime log shows: `models/gemini-pro is not found for API version v1beta`
   - implication: Hakeem chat is using an invalid or deprecated model identifier/configuration.
2. Story persistence schema mismatch:
   - runtime log shows: `Could not find the 'cover_image' column of 'stories' in the schema cache`
   - implication: code and Supabase schema are out of sync.
3. Performance issue:
   - runtime log shows skipped frames and main-thread overload warnings.
   - implication: cinematic/audio/image flow needs profiling before expansion.
4. Android manifest warning:
   - `android:enableOnBackInvokedCallback="true"` not enabled.

No worker may ignore these issues and jump to feature expansion.

---

## Manager Agent Responsibilities
The manager agent must:
- Break work into phases and sub-phases.
- Assign each sub-phase to worker agents with explicit file scope.
- Prevent overlap between workers.
- Maintain a persistent progress ledger.
- Produce a closure report at the end of each working session.
- On the next session, resume from the last unfinished checkpoint automatically.

The manager agent must never ask the user to restate the project plan if the project files already contain it.

---

## Required Persistent Files In Project Root
The manager agent must create and maintain these files in the project root:

### 1) `PROJECT_STATUS.md`
Single source of truth for current phase, active blockers, latest decisions, and next step.

### 2) `TASK_BACKLOG.md`
Ordered backlog of all tasks by priority.
Each task must include:
- Task ID
- Title
- Phase
- Priority
- Status (`todo`, `in_progress`, `blocked`, `done`)
- Owner (`manager`, `worker-A`, etc.)
- File scope
- Acceptance criteria

### 3) `SESSION_HANDOFF.md`
End-of-session handoff file.
Must include:
- What was completed today
- What is currently in progress
- Exact next action to execute first next session
- Known blockers
- Commands to run for verification

### 4) `DECISIONS_LOG.md`
All architecture decisions.
Each entry must include:
- Date/time
- Decision
- Why
- Files affected
- Whether reversible

### 5) `TEST_CHECKLIST.md`
Per-phase verification checklist.
No phase is marked done without passing its checklist.

---

## Execution Rules
1. Only one phase may be actively expanded at a time.
2. No new feature work before current blockers are resolved.
3. Any worker touching routing must list all affected routes before editing.
4. Any worker touching backend persistence must verify schema compatibility first.
5. Any worker touching cinematic playback must test:
   - load
   - scene transition
   - audio start/stop
   - back navigation
6. Any worker touching Hakeem must not reintroduce avatar analysis into chat service.
7. Dead code must be identified explicitly before deletion.
8. No worker may silently rename files, routes, or models without logging it in `DECISIONS_LOG.md`.

---

## Phase Plan (authoritative)

### Phase A — Stabilize Runtime
Goal: eliminate runtime defects in current flow.

Tasks:
- Fix Hakeem model configuration/runtime call.
- Fix `stories.cover_image` schema/code mismatch.
- Enable Android back callback in manifest.
- Investigate skipped frames in cinematic flow.
- Verify full create-story-to-cinema-to-exit loop.

Exit criteria:
- No runtime exceptions in the primary story flow.
- Story save either works fully or is intentionally disabled with explicit fallback.
- Hakeem chat either works or is gated/disabled cleanly.

### Phase B — Align Persistence
Goal: align Supabase schema and repositories with the master spec.

Tasks:
- Audit stories table fields.
- Add migration or patch code for missing columns.
- Verify insert/update/select models.
- Document schema truth.

Exit criteria:
- Story save path passes real test.
- Schema documented in repo.

### Phase C — Phase 2 Product Work
Goal: auth + save + private library.

Tasks:
- Auth flow audit
- Save story finalization
- Private Library data flow
- Re-open saved story in Cinema

Exit criteria:
- Signed-in user can generate, save, reopen story.

No phase after C may start until C is complete and signed off.

---

## Worker Assignment Template
For every task assignment, the manager agent must use this structure:

- Task ID:
- Goal:
- Files allowed to change:
- Files forbidden to change:
- Inputs:
- Expected output:
- Verification command:
- Risk notes:

---

## Session Resume Rule
At the start of any future session, the manager agent must read in this exact order:
1. `HIKAYATI_AGENT_MASTER_SPEC.md`
2. `PROJECT_STATUS.md`
3. `SESSION_HANDOFF.md`
4. `TASK_BACKLOG.md`
5. `DECISIONS_LOG.md`

Then it must output:
- Current phase
- Last completed task
- First next task
- Blockers

Then continue immediately.

---

## Forbidden Behaviors
- Do not ask the user to explain the architecture again.
- Do not add avatar logic into story generation screens.
- Do not add store/settings/public library work during runtime stabilization.
- Do not start Phase 3+ while runtime issues remain unresolved.
- Do not mark tasks done without verification evidence.

---

## Definition of “Done” for any task
A task is only `done` if all of the following are true:
- Code changed
- App still analyzes/builds
- Runtime flow relevant to the task was tested
- Result logged in `SESSION_HANDOFF.md`
- Decision logged if architecture changed

---

## First Mandatory Next Actions
The manager agent must do these now, before any other work:
1. Create/update the five persistent root files.
2. Register the current runtime defects from logs.
3. Open a new task batch for Phase A.
4. Assign workers only to Phase A stabilization.
5. Return a short execution plan before modifying code.



============================================================
FILE: D:\Hikayati.com\HIKAYATI_EXECUTION_LOCK.md
============================================================

# HIKAYATI_EXECUTION_LOCK.md

## 🎯 الهدف
إغلاق مرحلة Product Activation بالكامل وتحويل المشروع إلى منتج يعمل فعليًا بدون أي خلل منطقي أو UI ميت.

---

## 🔴 تنبيه إجباري
تم تحديث هذا الملف بالكامل.

❗ يجب:
- تجاهل أي نسخة سابقة
- إعادة قراءة الملف من البداية
- مراجعة التنفيذ الحالي مقارنة بهذا الملف
- إصلاح أي انحراف فورًا

---

## 🧠 القوانين غير القابلة للكسر

1. ❌ لا يوجد زر بدون وظيفة
2. ❌ لا يوجد خصم وهمي
3. ❌ لا يوجد توليد بدون ربط حقيقي
4. ❌ الأفاتار يجب أن يؤثر فعليًا في الصور
5. ❌ لا يتم إرسال الصور مباشرة من Story Engine عند وجود أفاتار
6. ❌ حكيم ليس مراقب
7. ✅ المراقب طبقة مستقلة فوق الجميع (نص + صورة)
8. ✅ الكريدت يظهر دائمًا (عدا السينما)

---

## 💰 نظام الكريدت (نهائي)

### إنشاء
- أفاتار: 20
- صوت مستنسخ: 20

### استخدام
- قصة: 10
- أفاتار: +10
- صوت: +10

---

## 🧬 نظام الأفاتار (إجباري)

### التدفق
رفع صورة
→ Vision Analyzer
→ استخراج:
- face_description
- body_traits
- skin / hair / age
- clothes

→ شاشة:
- الملامح (ثابتة)
- الملابس (قابلة للتعديل)

→ اعتماد → خصم 20

→ حفظ:
profiles.avatar_data

---

## قواعد الأفاتار
- الملامح ثابتة بعد الإنشاء
- الملابس قابلة للتعديل (بكريدت)
- يمكن إنشاء أكثر من أفاتار

---

## 🎬 نظام الصور (النظام النهائي)

### القانون
الصورة = المشهد + الهوية + الملابس + الستايل

---

## 🔥 Prompt Builder (إجباري)
هذا أهم جزء في المشروع.

### المدخلات:
- scene_description
- avatar_identity (اختياري)
- current_clothes
- style

### الناتج:
scene_description

Main character:
[face_description + traits]

Wearing:
[current_clothes]

Style:
[selected_style]

Same character across all scenes.

---

## مسارات الصور

### بدون أفاتار
Story Engine
→ image_prompt
→ Content Monitor
→ Image API

### مع أفاتار
Story Engine
→ Prompt Builder
→ Content Monitor
→ Image API

---

## 🧬 تثبيت الشخصية (إجباري)
- حقن الهوية في كل صورة
- لا تعتمد على صورة واحدة فقط
- لا تغيّر style داخل القصة
- لا تغيّر structure للـ prompt
- يجب الحفاظ على نفس الشخصية عبر جميع المشاهد

---

## 🔐 Content Monitor
يفحص:
- النص
- الصور

قبل:
- الإرسال
- العرض

---

## 🎤 الصوت

### عادي
→ مزود رخيص

### مستنسخ
→ ElevenLabs

---

## 🎥 السينما
- عرض فقط
- لا كريدت
- لا API
- لا إعادة تحميل
- لا منطق توليد داخلها

---

## 📚 المكتبات

### الخاصة
- بدون خصم
- بدون API
- ملك المستخدم
- يمكن إعادة الفتح بدون تكلفة جديدة

### العامة
- مغلقة
- فتح = 10
- تصبح دائمة بعد الفتح

---

## 🧭 التنقل (إجباري)

### الصفحة الرئيسية
- مكتبتي
- اصنع بطلك
- اصنع قصة
- الصوت
- المتجر

### القاعدة
أي زر → صفحة حقيقية أو Placeholder واضح

---

## 🤖 حكيم

### داخل التطبيق
- اقتراح
- تحسين
- مساعدة ذكية

### خارج التطبيق
- تسويق
- Google
- جذب المستخدم

---

## 🔧 Critical Fixes (إعادة فحص إلزامي)
لا تفترض أنها صحيحة — تحقق منها:

1. Avatar Upload + Vision Analyzer يعمل فعليًا
2. avatar_data يُستخدم داخل Prompt Builder
3. Image Service يستقبل prompt النهائي وليس القديم
4. UI الكريدت مربوط فعليًا بالحساب
5. المكتبة الخاصة تعرض قصص حقيقية
6. القوائم الشفافة تم تحويلها لصفحات حقيقية
7. يوجد Logout ويعمل
8. صفحة الصوت موجودة (حتى لو placeholder)
9. النجمة (الكريدت) مربوطة وتحدث لحظيًا

---

## 🚫 ممنوع
- إضافة ميزات جديدة
- تغيير الهيكل
- تحسينات شكلية بدون منطق
- القفز لمراحل لاحقة قبل إغلاق هذه المرحلة

---

## ✅ شرط الإغلاق
لا تعتبر المرحلة مكتملة إلا إذا:

- المستخدم ينشئ أفاتار
- يولد قصة
- يرى نفس الشخصية عبر المشاهد
- تنحفظ القصة
- يفتحها لاحقًا بدون خصم
- تكون العناصر الرئيسية غير ميتة

---

## 📌 المراجع التي يجب قراءتها مع هذا الملف
1. HIKAYATI_CANONICAL_SOURCE_OF_TRUTH.md
2. SPEC\IMAGE_SYSTEM.md
3. SPEC\AI_KEYS_MAP.md
4. SPEC\SYSTEM_FLOW.md
5. SPEC\USER_JOURNEY.md
6. HIKAYATI_RUNTIME_STATUS.md

---

## 🎯 تعليمات التنفيذ لمدير الوكلاء
- قارن التنفيذ الحالي مع هذا الملف
- حدّد الانحرافات
- أصلح فقط
- لا تضف ميزات جديدة
- اعمل على Batches صغيرة
- بعد كل Batch:
  - flutter analyze
  - تحديث task.md
  - تحديث HIKAYATI_RUNTIME_STATUS.md
  - تسجيل أي blocker في سجل_الأخطاء.md



============================================================
FILE: D:\Hikayati.com\تعديلات_كلاو.md
============================================================

# سجل تعديلات كلاو (Claude Edit Log)

> هذا الملف يسجل كل تعديل أو خطة أو خطأ يقوم به كلاو على مشروع حكواتي.
> القراءة إلزامية قبل أي جلسة جديدة.

---

## 2026-04-15 — جلسة المراجعة الأولى

### ما تم
- قراءة ملفات الحسم (CANONICAL / EXECUTION_LOCK / RUNTIME_STATUS / pubspec).
- استعراض بنية `lib/` (auth, avatar_lab, hakeem, home, legal, library, story_engine, voice_clone).
- إنشاء هذا السجل.

### خطط مقترحة للإطلاق
المراحل الأربع مُعرّفة في `HIKAYATI_CANONICAL_SOURCE_OF_TRUTH.md` (Batch A–D).
ستُنفذ بالترتيب وبدفعات صغيرة + `flutter analyze` بعد كل دفعة.

### تنبيه: تعارض يجب حسمه قبل أي تعديل على الكريدت
- ملف `HIKAYATI_CANONICAL_SOURCE_OF_TRUTH.md` يقول:
  - Plus = 20 كريدت، Pro = 50 كريدت.
  - الأفاتار/الصوت داخل التوليد = +10 كريدت.
  - لا ذكر لتكلفة "إنشاء" الأفاتار أو الصوت.
- ملف `HIKAYATI_EXECUTION_LOCK.md` يقول:
  - إنشاء أفاتار = 20 كريدت، إنشاء صوت مستنسخ = 20 كريدت.
  - قصة = 10، أفاتار داخلها = +10، صوت داخلها = +10.

المطلوب من TURKI: تحديد أيهما الحاسم قبل كتابة أي كود يخص الخصم.

### أخطاء/Blockers
- لا توجد حالياً. الحالة في `HIKAYATI_RUNTIME_STATUS.md` = لا Blockers.

---

## 2026-04-15 — بريفينغ تروك (لغة العمل الرسمية)

**اسم المستخدم**: تروك (لا تركي).

**المعمارية الثابتة (لا تُغيّر)**:
- UI عرض فقط، لا توليد داخله.
- Cinema عرض فقط، بلا API.
- كل توليد عبر Story Engine.
- الأفاتار هوية مستخرجة من صورة الطفل بدون أي إدخال يدوي (ممنوع: age/gender/skinTone/hair).
- توليد 4 صور أفاتار والمستخدم يختار واحدة.
- خصم الكريدت بعد نجاح النص + الصور + الصوت معاً فقط.

**أولويات الإطلاق**:
1. Avatar: تضارب أسماء + حفظ فاشل + عدم دخوله التوليد.
2. Credit: ثابت 100، غير متزامن مع DB.
3. Audio: lifecycle crash + dispose error.
4. Library: فتح بدون توليد جديد.
5. Performance: تقليل dropped frames.

**قواعد صارمة**: لا Dummy، لا ميزات جديدة، لا تغيير معمارية، لا زر بدون وظيفة، لا API عند إعادة فتح قصة محفوظة.

**أسلوب الرد**: فكرة + سلبيات باختصار، خطوات مرقمة، كل أمر في مربع منفصل، مسار الملف قبل الكود، الكود كامل، بلا "تأكد من"، وإنهاء بـ "هل نجح / ماذا ظهر لك؟".

**محسوم 2026-04-15**: إنشاء أفاتار جديد = 20 كريدت | توليد القصة = 10 كريدت (مطبّق في الكود ✅).

---

## 2026-04-15 — حزمة الإقفال النهائية (Launch Master Plan) من تروك

هذه الحزمة تصبح **المرجع الرسمي للإطلاق** وتعلو على أي خطة قديمة في ملفات المشروع عند التعارض، باستثناء المعمارية المثبتة في `CANONICAL` و `EXECUTION_LOCK`.

### الحالة الحالية
إغلاق واستقرار، لا إعادة بناء. مسار القصة موجود، السينما عرض فقط، التطبيق يحتاج إقفال 4 أنظمة ثم تركيب الهوية والمتجر.

### الدفعات السبع للإطلاق (بالترتيب — لا تجاوز)
1. **الدفعة 1 — إقفال الأفاتار**: مصدر واحد فقط للعقد، 4 نتائج حقيقية، حفظ صالح، يدخل فعلياً في `image path` داخل Story Engine.
2. **الدفعة 2 — إقفال الصوت**: Pollinations للصوت العادي، ElevenLabs للمستنسخ، السينما من `audio_url` فقط، lifecycle مستقر.
3. **الدفعة 3 — إقفال الكريدت**: مصدر الحقيقة Supabase، إزالة أي fallback قديم/ثابت 100، تحديث UI بعد كل عملية، عرض بدون كسور.
4. **الدفعة 4 — إقفال المكتبتين**: حفظ القصة، إعادة فتح بدون خصم ولا API، unlock للمكتبة العامة = 10 كريدت، رفض "لا توجد مشاهد".
5. **الدفعة 5 — توحيد الهوية**: نظام Theme واحد (colors/typography/spacing/app_theme/brand_assets)؛ لا ألوان hardcoded. هوية سينمائية: بنفسجي ملكي داكن + ذهبي هادئ + خلفيات ليلية + Glassmorphism خفيف + Cairo/Tajawal.
6. **الدفعة 6 — المتجر**:
   - **رقمي داخل التطبيق**: باقات كريدت + اشتراكات + طلباتي/فواتيري. عبر IAP (Apple StoreKit) و Google Play Billing حصراً.
   - **مادي على متجر ويب موحّد الهوية**: كتيبات، تيشيرتات، ملصقات، هدايا. لا يُربط المنتج المادي داخل التطبيق بوعد كريدت.
   - طلبات + webhooks + فواتير تلقائية (PDF/HTML) في Supabase Storage + بريد للمستخدم + Cron لإكمال الفواتير الناقصة.
7. **الدفعة 7 — إطلاق المتاجر**: App Store Connect + Google Play Console + IAP products + Data Safety + Generative AI declaration + Privacy + Families Policy + screenshots + video + ASO.

### الهوية البصرية النهائية
- Primary: بنفسجي ملكي داكن.
- Secondary: ذهبي هادئ.
- Backgrounds: تدرجات ليلية عميقة.
- Text: أبيض دافئ/رمادي فاتح.
- Cards: Glassmorphism خفيف.
- Corners: مستديرة واضحة.
- Icons: خط موحّد بسيط غير طفولي.
- Fonts: Cairo للعناوين + Tajawal/Cairo للنصوص.
- Feel: "سينمائي دافئ" لا "ألعاب أطفال صاخبة".
- قاعدة: نفس الهوية على التطبيق ومتجر الويب، لكن التطبيق = إنشاء/مشاهدة، المتجر = منتج/اقتناء.

### هيكل الصفحة الرئيسية النهائية
أعلى: شعار + رصيد + حساب + إعدادات + تغيير لغة.
بانر: "حوّل طفلك إلى بطل قصة" + زر "أنشئ قصة".
أزرار بارزة: "اصنع بطلك" / "اروِ القصة بصوتك".
أقسام: المكتبة العامة → مكتبتي → متجر حكواتي.
أسفل: سياسة الخصوصية / الشروط / من نحن / اتصل بنا.

### الأزرار الإلزامية التي يجب أن تعمل فعلياً (لا Placeholder مبهم)
أنشئ قصة، اصنع بطلك، اروِ القصة بصوتك، مكتبتي، المكتبة العامة، المتجر، الحساب، الإعدادات، سياسة الخصوصية، الشروط.

### قواعد صياغة المتجر (لتجنب رفض Apple/Google)
داخل التطبيق **ممنوع**:
- "اشتر الكتيب وخذ 100 كريدت"
- "الكتيب يحتوي كريدت"

داخل التطبيق **آمن**:
- "اطلب كتيب حكواتي"
- "منتجات حكواتي"
- "يتضمن مزايا إضافية داخل التطبيق" (بدون تحديد كريدت صريح مرتبط بمنتج مادي)

### الصفحات القانونية المطلوبة قبل الإطلاق
- سياسة الخصوصية (تذكر صراحة: Supabase، Pollinations، Gemini، ElevenLabs، صور الأطفال، تسجيلات الصوت، مدة الاحتفاظ، الحذف).
- الشروط والأحكام.
- سياسة الأطفال والوصاية الأبوية (Families Policy).
- سياسة حذف البيانات.
- سياسة الاسترداد.
- سياسة المشتريات الرقمية.
- سياسة المشتريات المادية والشحن.

### نقاط مخاطر إلزامية للإطلاق
1. IAP إجباري للسلع الرقمية على iOS/Android.
2. Consent صريح قبل استنساخ الصوت (ElevenLabs) + سجل موافقة.
3. إعلان Generative AI Content على Google Play.
4. Bundle ID / Package Name / Signing Keys يُثبّتان من الآن.
5. Performance pass بين الدفعة 4 والدفعة 5.

### التسليمات الثلاثة القادمة (بالترتيب المقترح من كلاو)
1. هيكل الصفحات والأزرار النهائي (مرجع يقود التنفيذ).
2. نصوص الإطلاق الجاهزة (headlines/buttons/empty states/errors/purchase confirmations).
3. البرومبت التنفيذي الشامل لمدير الوكلاء (يعتمد على 1 و 2).

### قرارات معلّقة تنتظر تروك
- تسعير إنشاء الأفاتار/الصوت (0 أم 20).

---

## 2026-04-15 — تسليم التصاميم النهائية (v1)

**المخرجات**:
- `design/launch_ui_v1.html` — معاينة الشاشات الأربعة (الرئيسية / أنشئ قصة / المكتبات / الإعدادات).
- `design/design_spec_v1.md` — مواصفات ألوان/خطوط/مكونات/مسار التنفيذ.

**الهوية المعتمدة** (من `الهوية البصرية/hakawati_app_visual_identity_and_ideas.md`):
- Deep Purple `#2D0B5A` + Vibrant Orange `#FF8C00` + Gold `#FFD700` + Cairo.

**ملاحظة**: `lib/core/theme/app_colors.dart` الحالي لا يطابق الهوية (يستخدم `#6A0DAD` بدون مبرر). سيُستبدل في الدفعة 5.

---

## 2026-04-15 — موقّت التوليد + Bug وصف المشهد

### الموقّت
- أضيف `_generationStart = DateTime.now()` في `initState()` في `intro_cinematic_screen.dart`
- أضيف log عند `_navigateToCinema()` يطبع الوقت الكامل بشكل واضح:
  ```
  ╔══════════════════════════════════════╗
  ║  ⏱️  وقت التوليد الكامل: XX.Xs   ║
  ╚══════════════════════════════════════╝
  ```

### Bug مكتشف — وصف المشهد فارغ
**اللوج**: `[PromptBuilder] -> A scene.` في 3 مشاهد متتالية
**السبب**: story text generation أنتج وصف مشهد `A scene.` فقط — احتمالان: موضوع "السمنه" تسبب في أن LLM لا يولد قصة حقيقية، أو content_monitor شطب النص.
**التأثير**: الصور الثلاث ستكون متطابقة تقريباً بدون تمييز.
**الأولوية**: تحقيق في unified_engine/story_generation_service — سيُعالج قبل الإطلاق.
**الدفعة**: تُضاف لـ Dفعة 2 (جودة التوليد).

### Pollinations TTS ✅
- `[AudioProvider] Using Pollinations TTS — voice=fable, hasKey=true` ظهر بنجاح
- الصوت العادي (non-cloned) يعمل عبر Pollinations بمفتاح صحيح ✅

**الترتيب الذي طلبه تروك**: الرئيسية → أنشئ قصة → المكتبات → الإعدادات. منفذ.

---

## 2026-04-15 — إصلاح الأولوية 1 (أفاتار) + إصلاح الصوت + Race Condition

### التعديلات على `lib/application/use_cases/generate_story_use_case.dart`

**1 — قفل Race Condition (Critical)**:
- أضيف `static bool _isGenerating = false;`
- guard في بداية `execute()`: يرفض الطلب المكرر برسالة واضحة
- `finally { _isGenerating = false; }` لضمان التحرير دائماً
- **النتيجة في اللوج**: التوليد استُدعي مرة واحدة بدل ثلاث ✅

**2 — إصلاح مسار الصوت العادي (Non-Cloned)**:
- السابق: `OPENAI_API_KEY` → `AZURE_TTS_KEY` → Google TTS (رديء)
- الجديد: Pollinations TTS أولاً مع `Authorization: Bearer POLLINATIONS_AUDIO_API_KEY`
- إذا فشل Pollinations → Google TTS كآخر ملاذ
- **السبب**: `.env` يحتوي `POLLINATIONS_AUDIO_API_KEY` لكن الكود لم يقرأه أبداً
- المفتاح يُمرر عبر Bearer Header (ليس URL param) حسب توصية Gemini ✅

**3 — الأولوية 1 — إقفال الأفاتار (من Opus)**:
- `avatar_service.dart`: يحفظ في `avatar_profile_summary` بدل `avatar_data`
- `save_avatar_use_case.dart`: أعيدت كتابته — يخصم، يبني `prompt_snippet`، دالة `changeClothes()`
- `avatar_lab_screen.dart`: حُذف استدعاء Supabase المباشر — يستدعي Use Case فقط
- `generate_story_use_case.dart`: يستخدم `prompt_snippet` المحفوظ مباشرة ✅

### حالة الدفعات
| الدفعة | الحالة |
|---|---|
| 1 — الأفاتار | ✅ مُغلقة |
| 2 — الصوت | 🟡 Pollinations موصول، ElevenLabs cloned يعمل، Pollinations regular بانتظار اختبار |
| 3 — الرصيد | 🟡 جزئي — تم إصلاح عرض الرصيد في HomeScreen |
| 4 — المكتبات | 🔴 لم تبدأ |

---

## 2026-04-15 — إصلاح الرصيد يظهر صفر (الدفعة 3 — جزئي)

**المشكلة**: `HomeScreen extends StatelessWidget` ← كل rebuild ينشئ `creditsStream` جديد يبدأ من `initialData: 0`.

**الإصلاح**: تحويل `HomeScreen` إلى `StatefulWidget`.
- `_creditsStream` يُنشأ مرة واحدة في `initState()`
- إذا كان `userId` فارغاً → `Stream.value(0)` (آمن)
- الـ stream الحقيقي يُنشأ بعد التحقق من المستخدم

**الملف**: `lib/features/home/screens/home_screen.dart`

### مشاهدات اللوج
- `BLASTBufferQueue Can not acquire next buffer` — خطأ GPU من الجهاز (moto g04s) وليس Flutter
- `[VoiceMap] ! صوت غير معروف: cloned → fallback to echo` — تحذير بصري فقط، المسار الفعلي صحيح (useClonedVoice=true يستخدم ElevenLabs)



============================================================
FILE: D:\Hikayati.com\سجل_الأخطاء.md
============================================================

# HIKAYATI — سجل الأخطاء (Error Log)

هذا الملف مخصص لتسجيل أي أعطال أو بلوكرات تظهر أثناء تنفيذ الـ Batches ليتم تخصيص Batch منفصلة لحلها بناءً على طلب HIKAYATI_MASTER_SPEC_KIT_FULL.

## الأخطاء الحالية:

---

### [مُصلَح] ❌ خطأ بناء: `debugPrint` غير معرّف في `supabase_service.dart`
- **التاريخ:** 2026-03-21
- **الخطأ:** `Method not found: 'debugPrint'`
- **السبب:** إضافة وظيفة `ensureProfileExists` استخدمت `debugPrint` بدون import مناسب.
- **الحل:** إضافة `import 'package:flutter/foundation.dart';` في أول الملف.
- **الحالة:** ✅ مُصلَح

---

### [مُصلَح] ❌ `AvatarVisionService` يفشل: موديل غير مدعوم
- **التاريخ:** 2026-03-21
- **الخطأ:** `models/gemini-1.5-flash is not found for API version v1beta`
- **الحل:** تغيير الموديل إلى `gemini-2.0-flash`.
- **الحالة:** ✅ مُصلَح

---

### [مُصلَح] ❌ فشل حفظ القصص: `stories_user_id_fkey`
- **التاريخ:** 2026-03-21
- **الخطأ:** `Key is not present in table "profiles"`
- **الحل:** إضافة `ensureProfileExists()` في `SupabaseService` واستدعائها في `UnifiedEngine`.
- **الحالة:** ✅ مُصلَح

---

### [مُصلَح] ❌ فشل توليد صور Pollinations (401 Unauthorized)
- **التاريخ:** 2026-03-21
- **الخطأ:** الصور لا تظهر، والاستجابة 401.
- **السبب:** المعامل المستخدم في الرابط كان `&key=` بينما الصحيح أحياناً `&api_key=` أو بدون مفتاح للخدمة المجانية.
- **الحل:** توحيد الصيغة في `UnifiedEngine` إلى `&api_key=` ومعالجة الحالة إذا كان المفتاح فارغاً.
- **الحالة:** ✅ مُصلَح

### [مُصلَح] ❌ AvatarVisionService يعيد بيانات وهمية (Dummy)
- **التاريخ:** 2026-03-21
- **الخطأ:** الأفاتار دائماً عمره 7 سنوات وبنفس الوصف.
- **الحل:** استبدال الدالة بدالة `generateAvatarOptions` التي تقبل المدخلات (العمر، الجنس..) وتولد خيارات حقيقية.
- **الحالة:** ✅ مُصلَح

### [مُصلَح] ❌ فشل البناء: `Member not found: 'analyzeChildImage'`
- **التاريخ:** 2026-03-21
- **الخطأ:** استدعاء دالة محذوفة في `AvatarLabScreen`.
- **السبب:** تم تحديث `AvatarVisionService` لإزالة الاعتماد على Gemini واستبدال `analyzeChildImage` بـ `generateAvatarOptions`، لكن الـ UI لم يُحدّث.
- **الحل:** تحديث `AvatarLabScreen` لاستخدام المنطق الجديد (إدخال بيانات بدلاً من رفع صورة للتحليل).
- **الحالة:** ✅ قيد الإصلاح

### [تنبيه] ⚠️ سجل فشل نماذج Gemini Vision (للتوثيق)
- **التاريخ:** 2026-03-21
- **المشكلة:** فشل متكرر في تحليل الصور باستخدام `gemini-1.5-flash`, `gemini-1.5-flash-latest`, `gemini-1.5-pro`, `gemini-pro-vision` مع خطأ `404 Not Found` أو `API version not supported` في المنطقة الحالية.
- **القرار:** تم التخلي تماماً عن استخدام Gemini Vision للتحليل المباشر في الأفاتار واستبداله بمدخلات المستخدم المباشرة + خيارات مولدة محلياً لضمان الاستقرار.
- **الحالة:** 🚫 مغلق (لن يتم استخدام هذه النماذج للتحليل مرة أخرى).

### [مُصلَح] ❌ فشل في تحميل الصورة 502 في شاشة الأفاتار
- **التاريخ:** 2026-03-21
- **الخطأ:** `502 Bad Gateway` و `NetworkImageLoadException` عند محاولة عرض خيارات الأفاتار.
- **السبب:** إرسال صورة Base64 ضخمة إلى `text.pollinations.ai` قد يتسبب بانهيار وكيلهم (Proxy). كذلك استخدام النطاق غير المستقر `image.pollinations.ai/prompt/`.
- **الحل:** إضافة إطار حماية `try-catch` لخدمة الرؤية وتوفير سمات افتراضية كبديل لمنع التوقف. وتوحيد رابط استخراج الصور إلى `gen.pollinations.ai/image/`. وتمت إضافة `errorBuilder` لشاشة الـ UI.
- **الحالة:** ✅ مُصلَح



============================================================
FILE: D:\Hikayati.com\PROJECT_STATUS.md
============================================================

# PROJECT STATUS — حكواتي
**آخر تحديث:** 2026-04-23
**المرحلة الحالية:** دفعة ٦ — ما قبل الدفع

---

## الوضع الآن

| الدفعة | الاسم | الحالة |
|---|---|---|
| 1 | إقفال الأفاتار | ✅ مكتمل |
| 2 | إقفال الصوت | ✅ مكتمل |
| 3 | إقفال الكريدت | ✅ مكتمل |
| 4 | إقفال المكتبات | ✅ مكتمل |
| 5 | توحيد الهوية + ⭐ + أحمر/أخضر | ✅ مكتمل — commit: 38f8bc4 |
| 6 | المتجر الداخلي (IAP) | ⏳ ينتظر الراتب |
| 7 | الإطلاق App Store + Play | ⏳ بعد IAP |

---

## آخر commit

```
38f8bc4 — Credit Currency Lock + Remove Included + Dynamic Total
Tag: before_payment_credit_currency_lock
```

---

## القرارات المقفلة

- كروت الكريدت الخارجية: **موثّقة** في `خطة_المشروع/متجر_حكواتي_الخارجي.md`
- الاستبدال يتم داخل التطبيق عبر كود 12 خانة → يضيف credits مباشرة في Supabase
- لا ربط مباشر بين "متجر hikayati.com" وميزة رقمية داخل التطبيق (Apple Policy)
- Moyasar أولاً للمتجر الخارجي، IAP للمتجر الداخلي

---

## المعلّق لحل خارجي

| البند | المسؤول | الموعد |
|---|---|---|
| ElevenLabs فاتورة | تروك | الاثنين |
| Apple Developer اشتراك | تروك | بعد الراتب |
| Moyasar تسجيل | تروك | بعد الراتب |
| صور منتجات المتجر (5 تيشيرت + 3 كتيب) | تروك | قبل إطلاق المتجر |




============================================================
FILE: D:\Hikayati.com\TEST_CHECKLIST.md
============================================================

# TEST CHECKLIST — حكواتي
**آخر تحديث:** 2026-04-23

---

## ✅ مسارات الاختبار الأساسية (قبل كل إطلاق)

### المسار 1 — إنشاء قصة عادية
- [ ] المستخدم مسجّل الدخول
- [ ] رصيد ظاهر صحيح في AppBar
- [ ] صفحة إنشاء القصة تعرض: القصة = -10 ⭐
- [ ] لا يظهر "مضمن" أو "مجاني" في أي مكان
- [ ] ضغط "اصنع السحر" يبدأ التوليد
- [ ] بعد النجاح: يُخصم 10 ⭐ من الرصيد
- [ ] الرصيد في AppBar يتحدث فوراً
- [ ] القصة تظهر في المكتبة الخاصة

### المسار 2 — إنشاء قصة مع أفاتار + صوت مستنسخ
- [ ] تفعيل الأفاتار → الإجمالي يصبح -20 ⭐
- [ ] تفعيل الصوت المستنسخ → الإجمالي يصبح -40 ⭐
- [ ] الزر يعرض: "اصنع السحر\! -40 ⭐"
- [ ] بعد النجاح: خصم 40 ⭐ فقط (ليس مرتين)

### المسار 3 — فتح قصة من المكتبة الخاصة
- [ ] الضغط على قصة محفوظة
- [ ] تفتح مباشرة بدون توليد
- [ ] **صفر خصم** من الرصيد
- [ ] الرصيد لا يتغير

### المسار 4 — فتح قصة من المكتبة العامة (جديدة)
- [ ] الضغط على قصة عامة غير مفتوحة مسبقاً
- [ ] يظهر تأكيد الخصم
- [ ] بعد الموافقة: خصم 10 ⭐
- [ ] القصة تُحفظ في مكتبته الخاصة
- [ ] إعادة فتحها = صفر خصم

---

## 🛒 اختبار المتجر (بعد ربط IAP)

- [ ] شاشة المتجر تعرض 4 باقات بالأخضر (+50/+100/+250/+500 ⭐)
- [ ] الضغط على باقة يفتح IAP نافذة Apple/Google
- [ ] بعد الشراء الناجح: الرصيد يزيد فوراً
- [ ] لا تظهر أي ميزة "مجانية" داخل الباقة

---

## 🌐 اختبار المتجر الخارجي (hikayati.com — بعد البناء)

- [ ] شراء كرت كريدت → يصل الكود بالبريد
- [ ] إدخال الكود في التطبيق → يُضاف الكريدت فوراً
- [ ] كود مستخدم مسبقاً → يظهر خطأ واضح
- [ ] كود منتهي الصلاحية → يظهر خطأ واضح




============================================================
FILE: D:\Hikayati.com\متجر_حكواتي_خطة_الإطلاق_النهائية.md
============================================================

# خطة إطلاق متجر حكواتي الخارجي — النهائية الكاملة
**تاريخ:** 2026-04-23  
**الدومين:** hikayati.com (Hostinger Premium)  
**الهدف:** متجر ويب كامل مرتبط بنفس Supabase التطبيق

---

## ❶ قرار المجلد — إجابة مباشرة

**المتجر يكون في مجلد منفصل تماماً خارج مجلد Flutter.**

```
D:\
├── Hikayati.com\          ← تطبيق Flutter (كما هو الآن)
└── Hikayati-Store\        ← المتجر الخارجي (جديد، منفصل)
    ├── public\
    ├── src\
    ├── supabase\           ← Edge Functions فقط
    └── package.json
```

**السبب:**
- التطبيق Flutter + Dart. المتجر Next.js + HTML. لا علاقة تقنية بينهما في الكود.
- نفس Supabase يخدمهما — الفصل في الكود، الوحدة في البيانات.
- Hostinger يستضيف المتجر (Next.js على Node.js plan أو HTML ثابت).

---

## ❷ Supabase — نفس المشروع بدون نقاش

**لا تنشئ مشروع Supabase جديد.**

الجداول الموجودة مسبقاً تُستخدم مباشرة:
- `profiles` ← يحتوي credits، user_id
- `stories` ← لطلبات الكتيبات (story_id)
- `purchases` ← للمشتريات الرقمية

**جداول تُضاف للمتجر الخارجي:**

```sql
-- جدول الطلبات
CREATE TABLE public.orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  guest_email TEXT,
  product_type TEXT CHECK (product_type IN ('tshirt','booklet','magazine','credit_card')),
  product_data JSONB,
  quantity INT DEFAULT 1,
  subtotal_sar NUMERIC(10,2),
  vat_sar NUMERIC(10,2) GENERATED ALWAYS AS (subtotal_sar * 0.15) STORED,
  total_sar NUMERIC(10,2) GENERATED ALWAYS AS (subtotal_sar * 1.15) STORED,
  status TEXT CHECK (status IN ('pending','paid','printing','shipped','delivered','cancelled')) DEFAULT 'pending',
  payment_provider TEXT CHECK (payment_provider IN ('moyasar','stripe')),
  payment_id TEXT,
  shipping_address JSONB,
  tracking_number TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
ALTER TABLE public.orders ENABLE ROW LEVEL SECURITY;
CREATE POLICY "User sees own orders" ON public.orders FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Guest order insert" ON public.orders FOR INSERT WITH CHECK (true);

-- جدول الفواتير
CREATE TABLE public.invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id UUID REFERENCES public.orders(id) ON DELETE CASCADE,
  invoice_number TEXT UNIQUE,
  pdf_url TEXT,
  total_sar NUMERIC(10,2),
  vat_sar NUMERIC(10,2),
  issued_at TIMESTAMPTZ DEFAULT NOW()
);
ALTER TABLE public.invoices ENABLE ROW LEVEL SECURITY;
CREATE POLICY "User sees own invoices" ON public.invoices FOR SELECT
  USING (order_id IN (SELECT id FROM public.orders WHERE user_id = auth.uid()));

-- جدول كروت شحن الكريدت
CREATE TABLE public.credit_cards (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT UNIQUE NOT NULL,
  credits INT NOT NULL,
  price_sar NUMERIC(10,2),
  status TEXT CHECK (status IN ('unused','used','expired')) DEFAULT 'unused',
  redeemed_by UUID REFERENCES auth.users(id),
  redeemed_at TIMESTAMPTZ,
  order_id UUID REFERENCES public.orders(id),
  expires_at TIMESTAMPTZ DEFAULT (NOW() + INTERVAL '1 year'),
  created_at TIMESTAMPTZ DEFAULT NOW()
);
ALTER TABLE public.credit_cards ENABLE ROW LEVEL SECURITY;
CREATE POLICY "User redeems own card" ON public.credit_cards FOR UPDATE
  USING (status = 'unused') WITH CHECK (auth.uid() = redeemed_by);
CREATE POLICY "Service role full access" ON public.credit_cards USING (true);
```

---

## ❸ المنتجات الكاملة مع الأسعار

### أ) تيشيرت بشخصية الطفل

| البند | التفاصيل |
|---|---|
| السعر | 89 ريال / قطعة واحدة |
| العرض | 79 ريال / قطعتين فأكثر |
| المقاسات | 2-4، 4-6، 6-8، 8-10، 10-12 سنة |
| الألوان | أبيض، أسود، بنفسجي، وردي |
| الخامة | قطن 100% — 180 GSM |
| الطباعة | DTG عالي الدقة |
| التخصيص | صورة أفاتار الطفل من التطبيق |
| وقت الشحن | 5-7 أيام عمل |

### ب) كتيب القصة المطبوع

| البند | التفاصيل |
|---|---|
| السعر | 59 ريال |
| الحجم | A5 (148×210 mm) |
| الصفحات | 8 صفحات (غلاف + 6 مشاهد + خلفي) |
| الورق | مصقول 200 gsm |
| الغلاف | كرتون مقوى |
| التخصيص | قصة محددة من مكتبة المستخدم في التطبيق |
| وقت الشحن | 5-7 أيام عمل |

### ج) مجلة حكواتي الشهرية

| البند | التفاصيل |
|---|---|
| السعر | 149 ريال/شهر |
| المحتوى | 4 قصص + نشاطات + صفحات تلوين |
| الحجم | A4 — 24 صفحة |
| التوصيل | مجاني داخل المملكة |
| التجديد | تلقائي — قابل للإلغاء في أي وقت |

### د) كروت شحن الكريدت ⭐

| الباقة | الكريدت | السعر | مقارنة App Store |
|---|---|---|---|
| Mini | +50 ⭐ | 19 ريال | أرخص |
| Starter | +100 ⭐ | 35 ريال | أرخص |
| Pro | +250 ⭐ | 79 ريال | أرخص |
| Ultimate | +500 ⭐ | 149 ريال | أرخص |

**آلية الاستخدام:**
1. المستخدم يشتري الكرت من hikayati.com
2. يستلم كود 12 خانة بالبريد الإلكتروني
3. في التطبيق → المتجر → "استبدال كرت"
4. يُدخل الكود → الكريدت يُضاف فوراً

---

## ❹ وسائل الدفع — الكاملة

### الأساسي: Moyasar (السوق السعودي)
**الأهم لأن مدى = 80%+ من مدفوعات السعودية**

| الوسيلة | الغطاء |
|---|---|
| مدى (mada) | أكثر استخداماً في السعودية |
| Visa / Mastercard | البطاقات الدولية |
| Apple Pay (ويب) | iOS Safari |
| STC Pay | دفع سريع محلي |
| SADAD | الخدمات الحكومية والفواتير |

```
الرسوم: 2% + 1 ريال (مدى) / 2.9% + 1 ريال (Visa/Mastercard)
متطلبات: سجل تجاري سعودي أو فرد معرّف (Nafath)
التفعيل: 3-7 أيام عمل
الموقع: moyasar.com
```

### الثانوي: Stripe (الدفع الدولي)

| الوسيلة | الغطاء |
|---|---|
| Visa / Mastercard | جميع الدول |
| Apple Pay (ويب) | iOS Safari — عالمي |
| Google Pay | Android Chrome — عالمي |
| AMEX | بطاقات أمريكان إكسبريس |

```
الرسوم: 2.9% + $0.30 لكل معاملة
متطلبات: بطاقة ائتمانية للتسجيل + بيانات هوية
التفعيل: فوري
الموقع: stripe.com
```

**القرار التشغيلي:**
- الصفحة تعرض **Moyasar أولاً** (مدى + Apple Pay)
- ثم خيار "الدفع الدولي" يفتح Stripe
- لا تدفع Stripe حتى تحصل على Moyasar

---

## ❺ المعمارية التقنية الكاملة

```
hikayati.com (Hostinger Node.js Plan)
├── Next.js 14 (App Router)
├── Tailwind CSS
├── Supabase JS Client (نفس المشروع)
├── Moyasar JS SDK
├── Stripe JS SDK
└── Resend (البريد الإلكتروني)

Supabase (نفس المشروع الموجود)
├── Database (tables: orders, invoices, credit_cards)
├── Storage (bucket: invoices-pdf)
├── Edge Functions:
│   ├── moyasar-webhook
│   ├── stripe-webhook
│   ├── redeem-credit-card
│   └── generate-invoice
└── Auth (نفس المستخدمين من التطبيق)
```

### هيكل مجلد المشروع

```
D:\Hikayati-Store\
├── app\
│   ├── page.tsx                  ← الصفحة الرئيسية
│   ├── store\
│   │   ├── page.tsx              ← كل المنتجات
│   │   ├── tshirts\page.tsx
│   │   ├── booklets\page.tsx
│   │   ├── magazines\page.tsx
│   │   └── credit-cards\page.tsx
│   ├── product\[slug]\page.tsx   ← صفحة منتج
│   ├── cart\page.tsx             ← السلة
│   ├── checkout\page.tsx         ← إتمام الطلب
│   ├── account\
│   │   ├── orders\page.tsx       ← طلباتي
│   │   └── invoices\page.tsx     ← فواتيري
│   ├── about\page.tsx
│   ├── contact\page.tsx
│   ├── privacy\page.tsx
│   ├── terms\page.tsx
│   └── refund\page.tsx
├── components\
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── ProductCard.tsx
│   ├── Cart.tsx
│   ├── PaymentForm.tsx           ← Moyasar + Stripe
│   └── CreditCardWidget.tsx
├── lib\
│   ├── supabase.ts
│   ├── moyasar.ts
│   ├── stripe.ts
│   └── email.ts
├── supabase\
│   └── functions\
│       ├── moyasar-webhook\
│       ├── stripe-webhook\
│       ├── redeem-credit-card\
│       └── generate-invoice\
├── public\
│   └── images\
├── .env.local
├── next.config.js
├── tailwind.config.js
└── package.json
```

---

## ❻ تدفق الطلب الكامل (Order Flow)

```
المستخدم يختار منتج
        ↓
يضيفه للسلة (localStorage)
        ↓
Checkout → يدخل بيانات الشحن
        ↓
يختار وسيلة الدفع
   ├── Moyasar → Hosted Checkout → Redirect back
   └── Stripe → Stripe Checkout → Redirect back
        ↓
Payment Success → Webhook يُطلق
        ↓
Edge Function تعمل:
   ① يُنشئ row في orders (status=paid)
   ② يُنشئ فاتورة PDF → Storage
   ③ بريد تأكيد الطلب → العميل
   ④ إذا credit_card:
      - يُنشئ كود 12 خانة
      - بريد مع الكود
   ⑤ إذا منتج مادي:
      - Webhook → شريك الطباعة
        ↓
شريك الطباعة ينتج ويشحن
        ↓
API الشريك يحدث status → shipped
        ↓
بريد "تم شحن طلبك" + رقم التتبع
```

---

## ❼ Edge Functions

### 1. moyasar-webhook

```typescript
// supabase/functions/moyasar-webhook/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const supabase = createClient(
  Deno.env.get('SUPABASE_URL')!,
  Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
);

Deno.serve(async (req) => {
  const body = await req.json();

  if (body.type === 'payment' && body.status === 'paid') {
    const metadata = body.metadata;

    const { data: order } = await supabase
      .from('orders')
      .insert({
        user_id: metadata.user_id || null,
        guest_email: metadata.guest_email || null,
        product_type: metadata.product_type,
        product_data: JSON.parse(metadata.product_data),
        subtotal_sar: body.amount / 100,
        quantity: metadata.quantity || 1,
        status: 'paid',
        payment_provider: 'moyasar',
        payment_id: body.id,
        shipping_address: JSON.parse(metadata.shipping_address || '{}'),
      })
      .select()
      .single();

    if (metadata.product_type === 'credit_card') {
      const code = generateCode();
      await supabase.from('credit_cards').insert({
        code,
        credits: parseInt(metadata.credits),
        price_sar: body.amount / 100,
        order_id: order.id,
      });
      await sendEmail(metadata.guest_email || metadata.user_email, 'credit_code', { code });
    } else {
      await notifyPrinter(order);
    }

    await generateInvoicePDF(order.id);
  }

  return new Response('OK');
});

function generateCode(): string {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
  const segments = [];
  for (let i = 0; i < 3; i++) {
    let seg = '';
    for (let j = 0; j < 4; j++) {
      seg += chars[Math.floor(Math.random() * chars.length)];
    }
    segments.push(seg);
  }
  return segments.join('-');
}
```

### 2. redeem-credit-card

```typescript
// supabase/functions/redeem-credit-card/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

Deno.serve(async (req) => {
  const { code, user_id } = await req.json();

  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  );

  // التحقق من الكود
  const { data: card, error } = await supabase
    .from('credit_cards')
    .select('*')
    .eq('code', code.toUpperCase())
    .eq('status', 'unused')
    .single();

  if (error || !card) {
    return new Response(JSON.stringify({ error: 'كود غير صالح أو مستخدم مسبقاً' }), { status: 400 });
  }

  if (card.expires_at && new Date(card.expires_at) < new Date()) {
    return new Response(JSON.stringify({ error: 'انتهت صلاحية الكرت' }), { status: 400 });
  }

  // تحديث الكرت
  await supabase.from('credit_cards').update({
    status: 'used',
    redeemed_by: user_id,
    redeemed_at: new Date().toISOString(),
  }).eq('id', card.id);

  // إضافة الكريدت للمستخدم
  await supabase.rpc('deduct_credits', {
    p_user_id: user_id,
    p_amount: -card.credits,  // سالب = إضافة
    p_reason: `credit_card_redeem:${code}`,
  });

  return new Response(JSON.stringify({
    success: true,
    credits_added: card.credits,
    message: `تم إضافة ${card.credits} ⭐ لحسابك`,
  }));
});
```

---

## ❽ متطلبات ما قبل الإطلاق — Checklist كاملة

### البنية التحتية
- [ ] إنشاء مجلد `D:\Hikayati-Store\`
- [ ] تهيئة مشروع Next.js 14
- [ ] ربط الدومين hikayati.com بـ Node.js hosting في Hostinger
- [ ] إعداد SSL (مجاني من Hostinger)

### Supabase
- [ ] تشغيل SQL الجداول الثلاثة (orders, invoices, credit_cards)
- [ ] إنشاء bucket `invoices-pdf` في Storage
- [ ] نشر الـ 4 Edge Functions
- [ ] اختبار webhook locally بـ Supabase CLI

### Moyasar (أولوية)
- [ ] تسجيل حساب على moyasar.com
- [ ] رفع وثائق الهوية / السجل التجاري
- [ ] الحصول على `publishable_api_key` + `secret_api_key`
- [ ] إعداد webhook URL في لوحة Moyasar
- [ ] اختبار دفعة بـ بطاقة اختبار مدى

### Stripe (ثانوي)
- [ ] تسجيل حساب على stripe.com
- [ ] الحصول على `publishable_key` + `secret_key`
- [ ] إعداد Webhook في Stripe Dashboard
- [ ] تفعيل Apple Pay + Google Pay

### البريد الإلكتروني
- [ ] تسجيل Resend (مجاني: 3000 بريد/شهر)
- [ ] ربط دومين `noreply@hikayati.com`
- [ ] إنشاء قوالب:
  - تأكيد الطلب
  - كود الكريدت
  - طلب تم شحنه
  - الفاتورة

### المحتوى
- [ ] 5 صور منتجات تيشيرت (mockups)
- [ ] 3 صور منتجات كتيبات
- [ ] صور كروت الكريدت (4 باقات)
- [ ] نصوص جميع الصفحات بالعربية
- [ ] سياسة الاسترداد والشحن (ملزمة قانونياً)
- [ ] سياسة الخصوصية (نفس التطبيق)

### الصفحات القانونية (إلزامية لبوابات الدفع)
- [ ] `/privacy` — سياسة الخصوصية
- [ ] `/terms` — الشروط والأحكام
- [ ] `/refund` — سياسة الاسترداد والشحن
- [ ] `/contact` — صفحة تواصل حقيقية (Moyasar تطلبها)

### اختبار نهائي
- [ ] طلب تيشيرت كامل من A إلى Z
- [ ] طلب كرت كريدت + استبداله في التطبيق
- [ ] اختبار على جوال (iOS Safari + Android Chrome)
- [ ] اختبار Apple Pay ويب
- [ ] فاتورة PDF تُرسل بالبريد

---

## ❾ الجدول الزمني الواقعي

| المرحلة | المهام | الوقت |
|---|---|---|
| **أ — الإعداد** | إنشاء Next.js + ربط Supabase + تهيئة Hostinger | يوم 1 |
| **ب — الصفحات** | الرئيسية + المنتجات + صفحة منتج + السلة | أيام 2-4 |
| **ج — الدفع** | Moyasar + Stripe + Checkout كامل | أيام 5-7 |
| **د — الباكند** | Edge Functions + Webhooks + البريد | أيام 8-10 |
| **هـ — المحتوى** | صور المنتجات + النصوص + الصفحات القانونية | أيام 11-12 |
| **و — الاختبار** | اختبار كامل لكل سيناريو | أيام 13-14 |
| **ز — الإطلاق** | رفع على Hostinger + اختبار Production | اليوم 15 |

**الإجمالي: أسبوعان بمساعدة AI**

---

## ❿ تسلسل أوامر البناء

### 1. إنشاء المشروع

```bash
cd D:\
npx create-next-app@14 Hikayati-Store --typescript --tailwind --app --no-src-dir
cd Hikayati-Store
npm install @supabase/supabase-js @supabase/ssr
npm install @moyasar/web stripe @stripe/stripe-js
npm install resend
```

### 2. ملف .env.local

```env
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

NEXT_PUBLIC_MOYASAR_PUBLISHABLE_KEY=pk_live_...
MOYASAR_SECRET_KEY=sk_live_...
MOYASAR_WEBHOOK_SECRET=your_moyasar_webhook_secret

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

RESEND_API_KEY=re_...
FROM_EMAIL=noreply@hikayati.com
STORE_URL=https://hikayati.com
```

### 3. تشغيل SQL في Supabase

```bash
# افتح Supabase Dashboard → SQL Editor
# الصق SQL من القسم ❷ وانقر Run
```

### 4. نشر على Hostinger

```bash
# في Hostinger → Node.js → Create Application
# Repository: GitHub repo للمتجر
# Build command: npm run build
# Start command: npm start
# Port: 3000
```

---

## ⓫ ملاحظات استراتيجية نهائية

**كروت الكريدت = أهم منتج لأسباب:**
- يُخفف الاعتماد على App Store (30% عمولة)
- يخدم من ليس لديهم بطاقة ائتمانية
- يمكن بيعه في متاجر جملة مستقبلاً

**شريك الطباعة — اختر واحداً قبل الإطلاق:**
- **Printful** (موثوق، واجهة API سهلة، لكن شحن دولي أطول)
- **Printify** (أرخص، شبكة موردين أكبر)
- **محلي سعودي** (شحن أسرع، لكن يحتاج تكامل يدوي)

**نصيحة:** أطلق الكتيبات والكروت فقط في الأول (بدون شريك طباعة خارجي)، ثم أضف التيشيرتات لاحقاً بعد اختيار الشريك.

**ترتيب الإطلاق المقترح:**
1. كروت الكريدت (لا شريك طباعة، لا شحن — أسهل منتج)
2. الكتيبات (شريك طباعة واحد فقط)
3. التيشيرتات
4. المجلة الشهرية

---

**آخر تحديث:** 2026-04-23 | **الحالة:** خطة نهائية جاهزة للتنفيذ



============================================================
FILE: D:\Hikayati.com\SPEC\STORY_ENGINE.md
============================================================

# STORY ENGINE — FINAL

## الهدف
تحديد كيف تُبنى القصة فعليًا في المشروع الحالي، وكيف يرتبط النص والصورة والصوت والأفاتار داخل بنية حديثة قابلة للتوسعة.

---

## المبدأ الأساسي

المشروع الحالي لا يعمل بمنطق v5.

في v5:
- الأفاتار كان يدخل فقط كصورة مرجعية للصور
- النص كان منفصلًا عن الأفاتار
- خدمة واحدة كانت مسؤولة عن كل شيء

في المشروع الحالي:
- القصة تُبنى بهوية موحدة
- الأفاتار ليس مجرد صورة، بل هوية بصرية
- الخدمات مفصولة
- يمكن تغيير المزودات بدون إعادة كتابة المشروع كله

---

## القانون الذهبي

القصة = حدث + بطل + مشاهد + هوية + صور + صوت + حفظ

---

## المكونات الرئيسية

### 1) Story Engine
مسؤوليته:
- توليد القصة الأساسية
- تقسيم القصة إلى مشاهد
- وصف ما يحدث في كل مشهد
- إخراج scene_description لكل مشهد

Story Engine يحدد:
- الحدث
- البيئة
- التسلسل
- الجو العام

❌ Story Engine لا يحدد هوية الطفل النهائية عند وجود أفاتار

---

### 2) Hakeem
حكيم ليس مراقبًا.

دوره:
- تحسين الفكرة
- تحسين وصف البطل
- مساعدة في بناء الهوية النصية
- دعم الوصف عندما يكون الأفاتار مفعّلًا
- المساهمة في إنتاج avatar_identity النصي الحديث

حكيم:
- داخل التطبيق = مساعد ذكي
- خارج التطبيق = محرك تسويق وجذب

---

### 3) Avatar System
الأفاتار في المشروع الحالي ليس مثل v5.

الأفاتار الحديث ينتج:
- face_description
- body_traits
- skin
- hair
- age
- current_clothes

هذا يشكل:
avatar_identity

---

### 4) Prompt Builder
هذه طبقة إلزامية.

مسؤوليته:
- دمج scene_description
- مع avatar_identity
- مع current_clothes
- مع style

ثم إنتاج prompt نهائي للصور

---

### 5) Content Monitor
طبقة مستقلة.

يفحص:
- النص
- الصورة
- المخرجات

قبل:
- العرض
- أو إرسال التوليد

حكيم ليس Content Monitor

---

### 6) Image Service
مسؤوليته:
- استقبال prompt النهائي فقط
- إرسال الطلب لمزود الصور
- إعادة image_url أو image asset

Image Service لا يبني المنطق
بل ينفذ فقط

---

### 7) Audio System
ينقسم إلى قسمين:

#### أ) الصوت العادي
- يستخدم المزود الصوتي الأساسي الأرخص
- يُستخدم لقراءة القصص العادية

#### ب) الصوت المستنسخ
- يستخدم ElevenLabs فقط
- يُستخدم لإنشاء الصوت المستنسخ
- ويُستخدم أيضًا لقراءة القصة بالصوت المستنسخ فقط

❌ ممنوع استخدام ElevenLabs للصوت العادي
❌ ممنوع جعل كل أصوات القصص تمر عبر ElevenLabs

---

### 8) Voice Clone Service
مسؤوليته:
- رفع العينة
- إنشاء voice_id
- حفظ voice_id للمستخدم
- تشغيل النص باستخدام voice_id المستنسخ

---

### 9) Save Layer
مسؤوليته:
- حفظ القصة
- حفظ المشاهد
- حفظ روابط الصور والصوت
- ربط القصة بالمستخدم
- تمكين إعادة فتح القصة لاحقًا بدون خصم

---

## تدفق القصة النهائي

### بدون أفاتار

User Input
→ Story Engine
→ scenes + scene_description
→ Content Monitor
→ Image Service
→ Audio Service
→ Save Layer
→ Cinema
→ Private Library

---

### مع أفاتار

User Input
→ Story Engine
→ scenes + scene_description
→ Hakeem / Avatar Identity Enrichment
→ Prompt Builder
→ Content Monitor
→ Image Service
→ Audio Service أو Voice Clone Service
→ Save Layer
→ Cinema
→ Private Library

---

## كيف يدخل الأفاتار؟

الأفاتار الحديث لا يدخل فقط كرابط صورة مثل v5.

بل يدخل كالتالي:
- avatar_identity
- current_clothes
- style
- same character rules

ويتم حقنه في كل prompt صورة

---

## كيف نثبت الشخصية؟

يتم تثبيت الشخصية عبر:
1. avatar_identity ثابت
2. current_clothes واضح
3. style ثابت لكل قصة
4. prompt structure ثابت
5. تعليمات مثل:
   Same character across all scenes

---

## كيف يختلف هذا عن v5؟

### v5
- avatar_url فقط
- النص منفصل عن الهوية
- خدمة واحدة تقوم بكل شيء
- لا يوجد حفظ ناضج
- لا يوجد Prompt Builder حقيقي

### المشروع الحالي
- avatar_identity
- فصل النص عن الصور عن الصوت
- Prompt Builder
- Content Monitor
- حفظ حقيقي
- قابلية تغيير المزودات

---

## ما الذي نأخذه من v5 فقط؟
- فكرة reference image للصور
- seed/style consistency
- بعض UX flows
- ElevenLabs voice clone logic فقط

---

## ما الذي لا نأخذه من v5؟
- الخدمة الموحدة
- ربط كل شيء في ملف واحد
- منطق الصوت العام عبر ElevenLabs
- عدم الحفظ
- فصل غير ناضج للهوية

---

## القرار التنفيذي

المشروع الحالي يجب أن يعتمد على:
- Story Engine مستقل
- Prompt Builder مستقل
- Image Service مستقل
- Audio Service مستقل
- Voice Clone Service مستقل
- Save Layer مستقل

---

## ممنوعات
- ممنوع إعادة بناء المشروع على منطق v5
- ممنوع اختصار الأفاتار إلى avatar_url فقط
- ممنوع دمج النص والصور والصوت في خدمة واحدة
- ممنوع تمرير الصوت العادي إلى ElevenLabs
- ممنوع تجاهل Content Monitor

---

## النتيجة المطلوبة
منتج يستطيع:
- توليد قصة
- استخدام أفاتار حقيقي
- الحفاظ على هوية البطل
- استخدام صوت عادي أو مستنسخ
- حفظ القصة
- فتحها لاحقًا بدون خصم



============================================================
FILE: D:\Hikayati.com\SPEC\SYSTEM_FLOW.md
============================================================

# SYSTEM FLOW

## الهدف
توضيح التدفق التشغيلي الحقيقي للتطبيق من الدخول حتى حفظ القصة

---

## 1) تسجيل الدخول
Login
→ تحميل الحساب
→ تحميل الرصيد
→ تحميل الأفاتارات
→ تحميل القصص الخاصة

---

## 2) الصفحة الرئيسية
Home
→ مكتبتي
→ اصنع بطلك
→ اصنع قصة
→ الصوت
→ المتجر

---

## 3) إنشاء أفاتار
رفع صورة
→ Vision Analyzer
→ استخراج الملامح والملابس
→ مراجعة
→ حفظ الأفاتار
→ خصم كريدت الإنشاء

---

## 4) إنشاء قصة

### بدون أفاتار
Story Wizard
→ GenerateStoryUseCase
→ Story Engine
→ image_prompt
→ Image Service
→ Cinema
→ Save Story
→ Private Library

### مع أفاتار
Story Wizard
→ GenerateStoryUseCase
→ Story Engine
→ Prompt Builder
→ Image Service
→ Cinema
→ Save Story
→ Private Library

---

## 5) الصوت

### صوت عادي
Story Text
→ Cheap Voice Provider
→ Audio Output

### صوت مستنسخ
Story Text
→ ElevenLabs
→ Audio Output

---

## 6) المكتبة الخاصة
فتح قصة محفوظة
→ لا خصم
→ لا إعادة توليد
→ فتح مباشر

---

## 7) المكتبة العامة
عرض القصص
→ اختيار قصة
→ تأكيد خصم
→ فتح دائم بعد الشراء

---

## القواعد
- السينما عرض فقط
- لا يوجد زر بلا وظيفة
- أي أصل تم إنشاؤه يجب أن يعاد استخدامه
- أي قصة محفوظة يجب أن تفتح بدون خصم جديد



============================================================
FILE: D:\Hikayati.com\SPEC\USER_JOURNEY.md
============================================================

# USER JOURNEY

## الهدف
توضيح رحلة العميل من أول دخول حتى العودة والشراء

---

## الرحلة الأساسية
تسجيل الدخول
→ الصفحة الرئيسية
→ إنشاء أفاتار (اختياري)
→ إنشاء قصة
→ مشاهدة القصة
→ حفظها
→ فتحها من مكتبتي لاحقًا

---

## الرحلة مع أفاتار
Login
→ Home
→ اصنع بطلك
→ حفظ الأفاتار
→ Story Wizard
→ تفعيل استخدام الأفاتار
→ Generate
→ Cinema
→ Save
→ My Library

---

## الرحلة مع صوت مستنسخ
Login
→ Home
→ استنساخ صوتك
→ حفظ الصوت
→ Story Wizard
→ تفعيل الصوت
→ Generate
→ Cinema

---

## الرحلة التجارية
Login
→ Home
→ توليد قصة
→ خصم كريدت
→ حفظ
→ العودة لاحقًا
→ مكتبة عامة
→ فتح قصة مدفوعة
→ المتجر
→ شراء كريدت

---

## النتيجة المطلوبة
- المستخدم يفهم ماذا يفعل
- لا يضيع بين الشاشات
- كل خطوة لها هدف واضح
- كل خصم واضح
- كل أصل منشأ له استخدام حقيقي لاحقًا



============================================================
FILE: D:\Hikayati.com\SPEC\IMAGE_SYSTEM.md
============================================================

# IMAGE SYSTEM — FINAL

## الهدف
ضمان ثبات شخصية الطفل في جميع الصور

---

## القانون
الصورة = المشهد + الهوية + الملابس + الستايل

---

## المكونات

### 1) Story Engine
- يولد:
  - scene_description فقط
- يحدد:
  - الحدث
  - البيئة
  - تسلسل المشاهد

❌ لا يحدد هوية الطفل النهائية عند وجود أفاتار

---

### 2) Avatar System
يولد:
- face_description
- body_traits
- skin
- hair
- age
- current_clothes

هذا = هوية ثابتة للبطل

---

### 3) Clothing Layer
- الملابس الحالية
- الإكسسوارات
- الحالة البصرية الحالية

يمكن تعديلها لاحقًا حسب منطق الكريدت

---

### 4) Style System
- Pixar
- Anime
- Realistic

ثابت لكل قصة

---

## Prompt Builder (إجباري)

### المدخلات:
- scene_description
- avatar_identity (اختياري)
- current_clothes
- style

### الناتج:
scene_description

Main character:
[face_description + traits]

Wearing:
[current_clothes]

Style:
[selected_style]

Same character across all scenes.

---

## المسارات

### بدون أفاتار
Story Engine
→ image_prompt
→ Content Monitor
→ Image API

### مع أفاتار
Story Engine
→ Prompt Builder
→ Content Monitor
→ Image API

---

## تثبيت الشخصية
1. نفس الوصف في كل مشهد
2. نفس style
3. نفس model
4. نفس بنية الـ prompt
5. إضافة:
Same character across all scenes

---

## مصدر الوصف

### بدون أفاتار
→ Story Engine يصف المشهد والشخصية داخل المشهد

### مع أفاتار
→ Avatar System يزوّد هوية البطل
→ Prompt Builder يدمج الهوية مع وصف المشهد

---

## ممنوع
- إرسال صورة الطفل وحدها والاعتماد عليها
- تغيير style بين المشاهد
- حذف الهوية من prompt
- إرسال prompt ناقص

---

## النتيجة المطلوبة
نفس الطفل في كل الصور بأعلى ثبات ممكن



============================================================
FILE: D:\Hikayati.com\SPEC\AI_KEYS_MAP.md
============================================================

# AI KEYS MAP

## الهدف
توضيح توجيه المفاتيح والمزوّدين داخل المشروع

---

## TEXT
- المسؤول: Story Engine / Text Generation
- المزوّد: مزود النص الحالي الأرخص أو المربوط فعليًا
- الاستخدام:
  - توليد القصة
  - تقسيم المشاهد
  - وصف المشهد الأساسي

---

## IMAGE
- المسؤول: Image Service
- المزوّد: مزود الصور الحالي
- الاستخدام:
  - توليد صور المشاهد
- المدخل:
  - image_prompt النهائي من Story Engine أو Prompt Builder

---

## VOICE NORMAL
- المسؤول: Narration Service
- المزوّد: المزوّد الأرخص
- الاستخدام:
  - قراءة القصة العادية

---

## VOICE CLONE
- المسؤول: Voice Clone Service
- المزوّد: ElevenLabs
- الاستخدام:
  - إنشاء الصوت المستنسخ
  - تشغيل القصة بصوت مستنسخ

---

## VISION / AVATAR
- المسؤول: Avatar Vision Service
- المزوّد: Vision API
- الاستخدام:
  - تحليل صورة الطفل
  - استخراج الملامح
  - استخراج الملابس

---

## القاعدة
- لا تخلط بين المفاتيح
- لا تستخدم ElevenLabs للصوت العادي
- لا تستخدم Image API لتحليل الأفاتار
- لا تستخدم Text API بدل Vision في تحليل الصورة



============================================================
FILE: D:\Hikayati.com\lib\features\store\screens\store_screen.dart
============================================================

import 'package:flutter/material.dart';
import 'package:supabase_flutter/supabase_flutter.dart';
import 'package:hikayati/core/theme/app_colors.dart';

class StoreScreen extends StatefulWidget {
  const StoreScreen({super.key});

  @override
  State<StoreScreen> createState() => _StoreScreenState();
}

class _StoreScreenState extends State<StoreScreen> {
  int _credits = 0;
  bool _loading = true;

  @override
  void initState() {
    super.initState();
    _fetchCredits();
  }

  Future<void> _fetchCredits() async {
    final userId = Supabase.instance.client.auth.currentUser?.id;
    if (userId == null) {
      if (mounted) setState(() => _loading = false);
      return;
    }
    try {
      final res = await Supabase.instance.client
          .from('profiles')
          .select('credits')
          .eq('user_id', userId)
          .maybeSingle();
      if (res != null && mounted) {
        setState(() {
          _credits = res['credits'] as int? ?? 0;
          _loading = false;
        });
      }
    } catch (_) {
      if (mounted) setState(() => _loading = false);
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.deepNight,
      appBar: AppBar(
        title: const Text('شحن الرصيد',
            style: TextStyle(fontWeight: FontWeight.bold)),
        backgroundColor: AppColors.appBarBackground,
        foregroundColor: AppColors.glassWhite,
        centerTitle: true,
      ),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            // ── الرصيد الحالي ──
            Container(
              width: double.infinity,
              padding: const EdgeInsets.symmetric(vertical: 24, horizontal: 20),
              decoration: BoxDecoration(
                gradient: LinearGradient(
                  colors: [
                    AppColors.primaryDeepPurple,
                    AppColors.primaryDeepPurple.withValues(alpha: 0.5),
                  ],
                  begin: Alignment.topRight,
                  end: Alignment.bottomLeft,
                ),
                borderRadius: BorderRadius.circular(16),
              ),
              child: Column(
                children: [
                  Text('رصيدك الحالي',
                      style: TextStyle(
                          color: AppColors.glassWhite.withValues(alpha: 0.8),
                          fontSize: 14)),
                  const SizedBox(height: 10),
                  _loading
                      ? SizedBox(
                          width: 24,
                          height: 24,
                          child: CircularProgressIndicator(
                              strokeWidth: 2, color: AppColors.vibrantOrange))
                      : Row(
                          mainAxisAlignment: MainAxisAlignment.center,
                          children: [
                            Text('$_credits',
                                style: const TextStyle(
                                    color: AppColors.vibrantOrange,
                                    fontSize: 40,
                                    fontWeight: FontWeight.bold)),
                            const SizedBox(width: 8),
                            const Icon(Icons.stars,
                                color: AppColors.vibrantOrange, size: 32),
                          ],
                        ),
                ],
              ),
            ),

            const SizedBox(height: 28),

            // ── عنوان ──
            Text('اختر باقة الشحن',
                style: TextStyle(
                    color: AppColors.glassWhite,
                    fontSize: 20,
                    fontWeight: FontWeight.bold)),
            const SizedBox(height: 6),
            Text('سيتم ربط الدفع عبر المتاجر الرسمية قريبًا',
                style: TextStyle(
                    color: AppColors.glassWhite.withValues(alpha: 0.5),
                    fontSize: 13)),
            const SizedBox(height: 20),

            // ── الباقات ──
            _buildPackage(
              gems: 50,
              name: 'باقة المبتدئ',
              price: '4.99',
              icon: Icons.star_outline,
            ),
            _buildPackage(
              gems: 100,
              name: 'باقة المغامر',
              price: '8.99',
              icon: Icons.star_half,
              badge: 'شائعة',
            ),
            _buildPackage(
              gems: 250,
              name: 'باقة الحكواتي',
              price: '19.99',
              icon: Icons.star,
            ),
            _buildPackage(
              gems: 500,
              name: 'باقة الأسطورة',
              price: '34.99',
              icon: Icons.auto_awesome,
              highlighted: true,
              badge: 'أفضل قيمة',
            ),

            const SizedBox(height: 40),
          ],
        ),
      ),
    );
  }

  Widget _buildPackage({
    required int gems,
    required String name,
    required String price,
    required IconData icon,
    bool highlighted = false,
    String? badge,
  }) {
    return Container(
      margin: const EdgeInsets.only(bottom: 14),
      child: InkWell(
        onTap: () {
          ScaffoldMessenger.of(context).showSnackBar(
            const SnackBar(
                content:
                    Text('سيتم تفعيل الشراء عبر المتاجر الرسمية قريبًا')),
          );
        },
        borderRadius: BorderRadius.circular(14),
        child: Container(
          padding: const EdgeInsets.all(16),
          decoration: BoxDecoration(
            color: highlighted
                ? AppColors.vibrantOrange.withValues(alpha: 0.08)
                : AppColors.cardSurface,
            borderRadius: BorderRadius.circular(14),
            border: Border.all(
              color: highlighted
                  ? AppColors.vibrantOrange
                  : Colors.white.withValues(alpha: 0.06),
              width: highlighted ? 2 : 1,
            ),
          ),
          child: Row(
            children: [
              // أيقونة
              Container(
                width: 50,
                height: 50,
                decoration: BoxDecoration(
                  shape: BoxShape.circle,
                  color: highlighted
                      ? AppColors.vibrantOrange.withValues(alpha: 0.2)
                      : AppColors.primaryDeepPurple.withValues(alpha: 0.3),
                ),
                child: Icon(icon, color: AppColors.vibrantOrange, size: 24),
              ),
              const SizedBox(width: 14),

              // اسم + رصيد (أخضر = إضافة)
              Expanded(
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    Row(
                      children: [
                        Text(name,
                            style: TextStyle(
                                color: AppColors.glassWhite,
                                fontSize: 15,
                                fontWeight: FontWeight.bold)),
                        if (badge != null) ...[
                          const SizedBox(width: 8),
                          Container(
                            padding: const EdgeInsets.symmetric(
                                horizontal: 6, vertical: 2),
                            decoration: BoxDecoration(
                              color: AppColors.vibrantOrange,
                              borderRadius: BorderRadius.circular(6),
                            ),
                            child: Text(badge,
                                style: TextStyle(
                                    color: AppColors.deepNight,
                                    fontSize: 10,
                                    fontWeight: FontWeight.bold)),
                          ),
                        ],
                      ],
                    ),
                    const SizedBox(height: 4),
                    // الإضافة بالأخضر
                    Row(
                      children: [
                        Text('+$gems',
                            style: const TextStyle(
                                color: Color(0xFF4CAF50),
                                fontSize: 16,
                                fontWeight: FontWeight.bold)),
                        const SizedBox(width: 3),
                        const Icon(Icons.stars,
                            color: Color(0xFF4CAF50), size: 16),
                      ],
                    ),
                  ],
                ),
              ),

              // سعر + زر
              Column(
                crossAxisAlignment: CrossAxisAlignment.end,
                children: [
                  Text('\$$price',
                      style: TextStyle(
                          color: AppColors.glassWhite,
                          fontSize: 16,
                          fontWeight: FontWeight.bold)),
                  const SizedBox(height: 4),
                  Container(
                    padding:
                        const EdgeInsets.symmetric(horizontal: 14, vertical: 6),
                    decoration: BoxDecoration(
                      color: highlighted
                          ? AppColors.vibrantOrange
                          : AppColors.vibrantOrange.withValues(alpha: 0.15),
                      borderRadius: BorderRadius.circular(8),
                    ),
                    child: Text('شراء',
                        style: TextStyle(
                            color: highlighted
                                ? AppColors.deepNight
                                : AppColors.vibrantOrange,
                            fontSize: 13,
                            fontWeight: FontWeight.bold)),
                  ),
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }
}



============================================================
FILE: D:\Hikayati.com\lib\features\story_engine\screens\story_creation_screen.dart
============================================================

import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:supabase_flutter/supabase_flutter.dart';
import 'package:hikayati/core/theme/app_colors.dart';
import 'package:hikayati/features/library/services/library_service.dart';

enum CreditBadgeType { deduction, addition, balance }

class StoryCreationScreen extends StatefulWidget {
  const StoryCreationScreen({super.key});

  @override
  State<StoryCreationScreen> createState() => _StoryCreationScreenState();
}

class _StoryCreationScreenState extends State<StoryCreationScreen> {
  final _nameController = TextEditingController();
  final _ageController = TextEditingController();
  final _themeController = TextEditingController();

  Map<String, dynamic>? _savedAvatar;
  String? _savedVoiceId;
  bool _useAvatar = false;
  bool _useClonedVoice = false;
  String _selectedStyle = '3d-model';
  String _selectedVoice = 'echo';

  final _libraryService = LibraryService();

  // التكلفة الفعلية (تطابق generate_story_use_case.dart)
  static const int _baseStoryCost = 10;
  static const int _avatarUsageCost = 10;
  static const int _clonedVoiceUsageCost = 20;

  int get _totalCost {
    int total = _baseStoryCost;
    if (_useAvatar) total += _avatarUsageCost;
    if (_useClonedVoice) total += _clonedVoiceUsageCost;
    return total;
  }

  @override
  void initState() {
    super.initState();
    _loadSavedData();
  }

  Future<void> _loadSavedData() async {
    final prefs = await SharedPreferences.getInstance();

    try {
      final userId = Supabase.instance.client.auth.currentUser?.id;
      if (userId != null) {
        final response = await Supabase.instance.client
            .from('profiles')
            .select('avatar_profile_summary')
            .eq('user_id', userId)
            .single();
        if (response['avatar_profile_summary'] != null && mounted) {
          setState(() => _savedAvatar = response['avatar_profile_summary']);
        }
      }
    } catch (e) {
      debugPrint('[StoryCreation] لم يتم العثور على بطل محفوظ: $e');
    }

    final voiceId = prefs.getString('cloned_voice_id');
    if (mounted) setState(() => _savedVoiceId = voiceId);
  }

  void _generateStory() async {
    final ageText = _ageController.text.trim();
    if (ageText.isNotEmpty) {
      final age = int.tryParse(ageText);
      if (age == null || age < 3 || age > 12) {
        if (!mounted) return;
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(
            content: Text('التطبيق مخصص للأطفال من 3 إلى 12 سنة'),
            backgroundColor: Colors.orange,
          ),
        );
        return;
      }
    }

    final count = await _libraryService.getStoryCount();
    if (count >= 10) {
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(
          content: Text('مكتبتك ممتلئة. احذف قصة واحدة على الأقل أولاً.'),
          backgroundColor: Colors.orange,
        ),
      );
      context.push('/private-library');
      return;
    }

    final heroName = _nameController.text.trim().isNotEmpty
        ? _nameController.text.trim()
        : 'البطل';
    final heroAge = ageText.isNotEmpty ? ageText : '7';
    final storyTheme = _themeController.text.trim().isNotEmpty
        ? _themeController.text.trim()
        : 'مغامرة خيالية ومشوقة';

    final requestData = {
      'heroName': _useAvatar ? (_savedAvatar?['name'] ?? heroName) : heroName,
      'heroAge': _useAvatar ? (_savedAvatar?['age'] ?? heroAge) : heroAge,
      'heroVisualDescription':
          _useAvatar ? (_savedAvatar?['promptSnippet'] ?? '') : '',
      'useAvatar': _useAvatar,
      'avatarData': _useAvatar ? _savedAvatar : null,
      'storyStyle': storyTheme,
      'imageStyle': _selectedStyle,
    };

    final voiceChoice =
        _useClonedVoice && _savedVoiceId != null ? 'cloned' : _selectedVoice;

    if (mounted) {
      context.push('/intro-cinematic', extra: {
        'requestData': requestData,
        'voice': voiceChoice,
        'saveToLibrary': true,
      });
    }
  }

  @override
  Widget build(BuildContext context) {
    final hasAvatar = _savedAvatar != null;
    final hasClonedVoice = _savedVoiceId != null;

    return Scaffold(
      backgroundColor: AppColors.deepNight,
      appBar: AppBar(
        title: const Text('صمّم قصتك',
            style: TextStyle(
                color: AppColors.vibrantOrange, fontWeight: FontWeight.bold)),
        backgroundColor: AppColors.deepNight,
        foregroundColor: AppColors.vibrantOrange,
        elevation: 0,
        centerTitle: true,
      ),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20.0),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            // ── 1) اسم البطل والعمر ──
            Row(
              children: [
                Expanded(
                  flex: 2,
                  child: _buildTextField(
                      _nameController, 'اسم بطل القصة', Icons.person_outline),
                ),
                const SizedBox(width: 12),
                Expanded(
                  flex: 1,
                  child: _buildTextField(
                      _ageController, 'العمر', Icons.cake_outlined,
                      isNumber: true),
                ),
              ],
            ),
            const SizedBox(height: 16),

            // ── 2) بطل الأفاتار ──
            _buildFeatureCard(
              icon: hasAvatar ? Icons.face : Icons.add_a_photo,
              title: hasAvatar ? 'البطل السحري جاهز!' : 'اصنع بطلك أولاً',
              subtitle: hasAvatar
                  ? (_useAvatar
                      ? 'مفعّل — سيظهر البطل في صور القصة'
                      : 'اضغط لتفعيل البطل في القصة')
                  : 'لم يتم إنشاء بطل بعد',
              isActive: _useAvatar,
              isAvailable: hasAvatar,
              usageCost: _avatarUsageCost,
              onTap: hasAvatar
                  ? () => setState(() => _useAvatar = !_useAvatar)
                  : () => context.push('/avatar-lab'),
              onToggle: hasAvatar
                  ? (v) => setState(() => _useAvatar = v)
                  : null,
            ),
            const SizedBox(height: 12),

            // ── 3) الصوت المستنسخ ──
            _buildFeatureCard(
              icon: hasClonedVoice ? Icons.record_voice_over : Icons.mic_none,
              title: hasClonedVoice
                  ? 'صوتك المستنسخ جاهز!'
                  : 'استنسخ صوتك أولاً',
              subtitle: hasClonedVoice
                  ? (_useClonedVoice
                      ? 'مفعّل — القصة ستُروى بصوتك'
                      : 'اضغط لتفعيل صوتك في القصة')
                  : 'لم يتم إنشاء صوت مستنسخ بعد',
              isActive: _useClonedVoice,
              isAvailable: hasClonedVoice,
              usageCost: _clonedVoiceUsageCost,
              onTap: hasClonedVoice
                  ? () => setState(() => _useClonedVoice = !_useClonedVoice)
                  : () => context.push('/voice-clone'),
              onToggle: hasClonedVoice
                  ? (v) => setState(() => _useClonedVoice = v)
                  : null,
            ),

            // ── صوت الراوي (يظهر فقط إذا الصوت المستنسخ غير مفعّل) ──
            if (!_useClonedVoice) ...[
              const SizedBox(height: 16),
              _buildDropdown(
                label: 'صوت الراوي',
                icon: Icons.record_voice_over_outlined,
                value: _selectedVoice,
                items: const {
                  'echo': 'رجالي قصصي',
                  'fable': 'نسائي',
                  'onyx': 'رجالي عميق',
                },
                onChanged: (v) => setState(() => _selectedVoice = v!),
              ),
            ],
            const SizedBox(height: 16),

            // ── 4) موضوع القصة ──
            _buildTextField(
                _themeController,
                'موضوع القصة (مثال: الشجاعة، الفضاء، الحيوانات)',
                Icons.auto_stories_outlined),
            const SizedBox(height: 16),

            // ── 5) النمط البصري ──
            _buildDropdown(
              label: 'النمط البصري',
              icon: Icons.palette_outlined,
              value: _selectedStyle,
              items: const {
                '3d-model': 'رسوم متحركة 3D (بيكسار)',
                'anime': 'أنمي ياباني',
                'digital-art': 'فن رقمي',
                'pixel-art': 'بيكسل آرت',
              },
              onChanged: (v) => setState(() => _selectedStyle = v!),
            ),
            const SizedBox(height: 24),

            // ── 6) ملخص التكلفة ──
            _buildPricingBreakdown(),
            const SizedBox(height: 16),

            // ── 7) زر التوليد ──
            ElevatedButton(
              onPressed: _generateStory,
              style: ElevatedButton.styleFrom(
                padding: const EdgeInsets.symmetric(vertical: 18),
                backgroundColor: AppColors.vibrantOrange,
                foregroundColor: AppColors.deepNight,
                shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(14)),
              ),
              child: Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  const Icon(Icons.auto_awesome, size: 24),
                  const SizedBox(width: 10),
                  const Text('اصنع السحر!',
                      style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
                  const SizedBox(width: 10),
                  Container(
                    padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
                    decoration: BoxDecoration(
                      color: AppColors.deepNight.withValues(alpha: 0.2),
                      borderRadius: BorderRadius.circular(20),
                    ),
                    child: Row(
                      mainAxisSize: MainAxisSize.min,
                      children: [
                        Text('-$_totalCost',
                            style: const TextStyle(
                                fontSize: 16, fontWeight: FontWeight.bold)),
                        const SizedBox(width: 3),
                        const Icon(Icons.stars, size: 18),
                      ],
                    ),
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }

  // ── بطاقة ميزة موحّدة (أفاتار / صوت) ──
  Widget _buildFeatureCard({
    required IconData icon,
    required String title,
    required String subtitle,
    required bool isActive,
    required bool isAvailable,
    required int usageCost,
    required VoidCallback onTap,
    ValueChanged<bool>? onToggle,
  }) {
    return GestureDetector(
      onTap: onTap,
      child: AnimatedContainer(
        duration: const Duration(milliseconds: 250),
        padding: const EdgeInsets.all(16),
        decoration: BoxDecoration(
          color: isActive
              ? AppColors.vibrantOrange.withValues(alpha: 0.08)
              : AppColors.cardSurface,
          borderRadius: BorderRadius.circular(14),
          border: Border.all(
            color: isActive
                ? AppColors.vibrantOrange
                : Colors.white.withValues(alpha: 0.08),
            width: isActive ? 2 : 1,
          ),
        ),
        child: Row(
          children: [
            Container(
              width: 50,
              height: 50,
              decoration: BoxDecoration(
                shape: BoxShape.circle,
                color: isAvailable
                    ? (isActive
                        ? AppColors.vibrantOrange
                        : AppColors.vibrantOrange.withValues(alpha: 0.15))
                    : Colors.white.withValues(alpha: 0.06),
              ),
              child: Icon(icon,
                  color: isAvailable
                      ? (isActive ? AppColors.deepNight : AppColors.vibrantOrange)
                      : Colors.grey,
                  size: 26),
            ),
            const SizedBox(width: 14),
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Row(
                    children: [
                      Expanded(
                        child: Text(title,
                            style: TextStyle(
                                fontSize: 15,
                                fontWeight: FontWeight.bold,
                                color: AppColors.glassWhite)),
                      ),
                      // شارة التكلفة — تظهر فقط إذا الميزة متاحة
                      if (isAvailable)
                        Container(
                          padding: const EdgeInsets.symmetric(
                              horizontal: 8, vertical: 3),
                          decoration: BoxDecoration(
                            color: isActive
                                ? const Color(0xFFFF5252).withValues(alpha: 0.15)
                                : Colors.white.withValues(alpha: 0.04),
                            borderRadius: BorderRadius.circular(8),
                            border: Border.all(
                              color: isActive
                                  ? const Color(0xFFFF5252).withValues(alpha: 0.5)
                                  : Colors.white.withValues(alpha: 0.08),
                              width: 1,
                            ),
                          ),
                          child: Row(
                            mainAxisSize: MainAxisSize.min,
                            children: [
                              Text(
                                '-$usageCost',
                                style: TextStyle(
                                  color: isActive
                                      ? const Color(0xFFFF5252)
                                      : Colors.grey[400],
                                  fontWeight: FontWeight.bold,
                                  fontSize: 12,
                                ),
                              ),
                              const SizedBox(width: 2),
                              Icon(Icons.stars,
                                  size: 12,
                                  color: isActive
                                      ? const Color(0xFFFF5252)
                                      : Colors.grey[400]),
                            ],
                          ),
                        ),
                    ],
                  ),
                  const SizedBox(height: 3),
                  Text(subtitle,
                      style: TextStyle(
                          fontSize: 12,
                          color: isActive
                              ? Colors.greenAccent
                              : Colors.grey[400])),
                ],
              ),
            ),
            const SizedBox(width: 8),
            if (onToggle != null)
              Switch(
                value: isActive,
                activeColor: AppColors.vibrantOrange,
                onChanged: onToggle,
              )
            else
              Icon(Icons.arrow_forward_ios,
                  size: 14, color: Colors.grey[600]),
          ],
        ),
      ),
    );
  }

  // ── ملخص التكلفة ──
  Widget _buildPricingBreakdown() {
    return Container(
      padding: const EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: AppColors.cardSurface,
        borderRadius: BorderRadius.circular(14),
        border: Border.all(color: AppColors.vibrantOrange.withValues(alpha: 0.15)),
      ),
      child: Column(
        children: [
          _pricingRow(Icons.auto_stories, 'القصة الأساسية', _baseStoryCost),
          if (_useAvatar)
            _pricingRow(Icons.face, 'الأفاتار', _avatarUsageCost),
          if (_useClonedVoice)
            _pricingRow(
                Icons.record_voice_over, 'الصوت المستنسخ', _clonedVoiceUsageCost),
          Divider(color: AppColors.vibrantOrange.withValues(alpha: 0.25), height: 22),
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            children: [
              Text('الإجمالي',
                  style: TextStyle(
                      color: AppColors.glassWhite,
                      fontWeight: FontWeight.bold,
                      fontSize: 16)),
              _creditBadge(-_totalCost, type: CreditBadgeType.deduction, large: true),
            ],
          ),
        ],
      ),
    );
  }

  Widget _pricingRow(IconData icon, String label, int cost) {
    return Padding(
      padding: const EdgeInsets.symmetric(vertical: 5),
      child: Row(
        children: [
          Icon(icon, size: 16, color: Colors.grey[500]),
          const SizedBox(width: 8),
          Expanded(
            child: Text(label,
                style: TextStyle(color: Colors.grey[400], fontSize: 13)),
          ),
          _creditBadge(-cost, type: CreditBadgeType.deduction),
        ],
      ),
    );
  }

  /// شارة الكريدت الموحّدة — نفس النجمة الذهبية في كل التطبيق
  /// Red للخصم، Green للإضافة، Orange للرصيد الحالي
  Widget _creditBadge(int amount,
      {required CreditBadgeType type, bool large = false}) {
    final fontSize = large ? 16.0 : 13.0;
    final iconSize = large ? 18.0 : 14.0;

    final Color color;
    switch (type) {
      case CreditBadgeType.deduction:
        color = const Color(0xFFFF5252);
        break;
      case CreditBadgeType.addition:
        color = const Color(0xFF4CAF50);
        break;
      case CreditBadgeType.balance:
        color = AppColors.vibrantOrange;
        break;
    }

    final sign = amount > 0 ? '+' : '';
    return Row(
      mainAxisSize: MainAxisSize.min,
      children: [
        Text('$sign$amount',
            style: TextStyle(
                color: color,
                fontWeight: FontWeight.bold,
                fontSize: fontSize)),
        const SizedBox(width: 3),
        Icon(Icons.stars, color: color, size: iconSize),
      ],
    );
  }

  Widget _buildTextField(
      TextEditingController ctrl, String label, IconData icon,
      {bool isNumber = false}) {
    return TextField(
      controller: ctrl,
      keyboardType: isNumber ? TextInputType.number : TextInputType.text,
      style: const TextStyle(color: Colors.white),
      decoration: InputDecoration(
        labelText: label,
        labelStyle: const TextStyle(color: Colors.grey),
        prefixIcon: Icon(icon, color: AppColors.vibrantOrange, size: 20),
        filled: true,
        fillColor: AppColors.cardSurface,
        enabledBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: BorderSide(color: Colors.white.withValues(alpha: 0.08)),
        ),
        focusedBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: const BorderSide(color: AppColors.vibrantOrange, width: 2),
        ),
      ),
    );
  }

  Widget _buildDropdown({
    required String label,
    required IconData icon,
    required String value,
    required Map<String, String> items,
    required ValueChanged<String?> onChanged,
  }) {
    return DropdownButtonFormField<String>(
      value: value,
      dropdownColor: AppColors.cardSurface,
      style: const TextStyle(color: Colors.white),
      decoration: InputDecoration(
        labelText: label,
        labelStyle: const TextStyle(color: Colors.grey),
        prefixIcon: Icon(icon, color: AppColors.vibrantOrange, size: 20),
        filled: true,
        fillColor: AppColors.cardSurface,
        enabledBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: BorderSide(color: Colors.white.withValues(alpha: 0.08)),
        ),
        focusedBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(12),
          borderSide: const BorderSide(color: AppColors.vibrantOrange, width: 2),
        ),
      ),
      items: items.entries
          .map((e) => DropdownMenuItem(
              value: e.key,
              child:
                  Text(e.value, style: const TextStyle(color: Colors.white))))
          .toList(),
      onChanged: onChanged,
    );
  }
}



============================================================
FILE: D:\Hikayati.com\lib\application\use_cases\generate_story_use_case.dart
============================================================

/// === GenerateStoryUseCase ===
/// Application Layer — Use Case
///
/// يمثّل هذا الكلاس الحد الفاصل بين شاشات Flutter وخدمات الذكاء الاصطناعي.
/// LAW 1 (من HIKAYATI_AGENT_MASTER_SPEC.md):
///   "UI does not generate stories. Screens collect input, trigger actions, show loading states."

import 'dart:async';
import 'dart:convert';
import 'package:flutter/foundation.dart';
import 'package:flutter_dotenv/flutter_dotenv.dart';
import 'package:http/http.dart' as http;
import 'package:shared_preferences/shared_preferences.dart';
import 'package:hikayati/features/story_engine/services/unified_engine.dart';
import 'package:hikayati/features/library/services/library_service.dart';
import 'package:supabase_flutter/supabase_flutter.dart';

/// استثناء خاص عندما يتجاوز المستخدم حد 10 قصص
class StoryLimitException implements Exception {
  final int currentCount;
  final List<dynamic> existingStories;
  const StoryLimitException({
    required this.currentCount,
    required this.existingStories,
  });

  @override
  String toString() => 'StoryLimitException: $currentCount قصص محفوظة';
}

class GenerateStoryUseCase {
  final LibraryService _libraryService = LibraryService();

  static const int maxStories = 10;

  /// قفل ثابت يمنع استدعاء execute أكثر من مرة في نفس الوقت
  static bool _isGenerating = false;

  /// معرّفات القصص التي خُصم رصيدها — يمنع الخصم المزدوج في نفس الجلسة
  static final Set<String> _deductedStoryIds = {};

  /// تحويل اختيار الصوت من UI إلى voice ID محدد لـ OpenAI TTS
  /// UI values: echo (رجالي قصصي), fable (نسائي), onyx (رجالي عميق)
  static String _resolveOpenAiVoice(String? voice) {
    switch (voice) {
      case 'echo':
        return 'echo';     // رجالي قصصي (الافتراضي)
      case 'fable':
        return 'fable';    // نسائي
      case 'onyx':
        return 'onyx';     // رجالي عميق
      default:
        debugPrint('[VoiceMap] ⚠️ صوت غير معروف: $voice → fallback to echo');
        return 'echo';
    }
  }

  Future<Uint8List?> _fetchAudioWithRetry(String url, Map<String, String> headers, [String? body]) async {
    for (int attempt = 1; attempt <= 3; attempt++) {
      try {
        http.Response res;
        if (body != null) {
          res = await http.post(Uri.parse(url), headers: headers, body: body).timeout(const Duration(seconds: 30));
        } else {
          final safeHeaders = {...headers};
          if (!safeHeaders.containsKey('User-Agent')) {
            safeHeaders['User-Agent'] = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)';
          }
          res = await http.get(Uri.parse(url), headers: safeHeaders).timeout(const Duration(seconds: 30));
        }
        if (res.statusCode == 200) return res.bodyBytes;
        debugPrint('[AudioRetry] Attempt $attempt failed: ${res.statusCode}');
        
        if (attempt < 3) await Future.delayed(Duration(seconds: 2 * attempt));
      } catch (e) {
        debugPrint('[AudioRetry] Attempt $attempt error: $e');
        
        if (attempt < 3) await Future.delayed(Duration(seconds: 2 * attempt));
      }
    }
    return null;
  }

  Future<Map<String, dynamic>> execute(
    Map<String, dynamic> requestData,
    {String? voice, bool saveToLibrary = true}
  ) async {
    debugPrint('[UseCase] GenerateStoryUseCase.execute() — بدء التوليد');

    // ====== منع التوليد المزدوج (Race Condition) ======
    if (_isGenerating) {
      debugPrint('[UseCase] ⚠️ طلب مكرر مرفوض — التوليد جارٍ بالفعل');
      throw Exception('جارٍ التوليد بالفعل. يرجى الانتظار حتى تكتمل القصة الحالية.');
    }
    _isGenerating = true;

    // التكلفة: أساس 10 + 10 عند استخدام الأفاتار + 20 عند استخدام الصوت المستنسخ
    const int baseStoryCost = 10;
    const int avatarUsageCost = 10;
    const int clonedVoiceUsageCost = 20;

    int totalCost = baseStoryCost;
    if (requestData['useAvatar'] == true) totalCost += avatarUsageCost;
    if (voice == 'cloned') totalCost += clonedVoiceUsageCost;

    final userId = Supabase.instance.client.auth.currentUser?.id;
    if (userId == null) throw Exception('يجب تسجيل الدخول أولاً.');

    // ====== التحقق من حد القصص (10 قصص) ======
    if (saveToLibrary) {
      final storyCount = await _libraryService.getStoryCount();
      if (storyCount >= maxStories) {
        final existingStories = await _libraryService.getPrivateStories();
        debugPrint('[UseCase] ⛔ تجاوز الحد: $storyCount قصص محفوظة');
        throw StoryLimitException(
          currentCount: storyCount,
          existingStories: existingStories,
        );
      }
    }

    // جلب الرصيد المباشر من قاعدة البيانات
    final profileRes = await Supabase.instance.client
        .from('profiles')
        .select('credits')
        .eq('user_id', userId)
        .single();
    final currentCredits = profileRes['credits'] as int? ?? 0;

    if (currentCredits < totalCost) {
      throw Exception(
          'الرصيد الفعلي غير كافٍ. تمتلك $currentCredits وتحتاج إلى $totalCost جواهر.');
    }

    // جلب الأفاتار إذا طُلب
    if (requestData['useAvatar'] == true) {
      debugPrint('[AvatarFetch] start');
      try {
        final avatarProfileRes = await Supabase.instance.client
            .from('profiles')
            .select('avatar_profile_summary')
            .eq('user_id', userId)
            .single();
        final avatarData = avatarProfileRes['avatar_profile_summary'];

        if (avatarData != null) {
          debugPrint('[AvatarFetch] found avatar_profile_summary');
          final face = avatarData['face_description'] ?? 'cute child';
          final clothes = avatarData['current_clothes'] ?? 'colorful clothes';
          // يستخدم prompt_snippet المحفوظ مباشرةً إذا كان موجوداً، وإلا يبنيه
          final promptSnippet = avatarData['prompt_snippet'] as String?;
          requestData['heroVisualDescription'] = promptSnippet ??
              '3D Pixar child, $face, wearing $clothes, clean white background';
          requestData['avatarData'] = avatarData;
          if (avatarData['name'] != null) requestData['heroName'] = avatarData['name'];
          if (avatarData['age'] != null) requestData['heroAge'] = avatarData['age'].toString();
        } else {
          debugPrint('[AvatarFetch] null -> disable avatar');
          requestData['useAvatar'] = false;
        }
      } catch (e) {
        debugPrint('[AvatarFetch] error -> disable avatar');
        requestData['useAvatar'] = false;
      }
    }

    debugPrint('[UseCase] heroName: ${requestData['heroName']}');
    debugPrint('[UseCase] storyStyle: ${requestData['storyStyle']}');
    debugPrint('[UseCase] useAvatar: ${requestData['useAvatar']}');
    debugPrint('[UseCase] Total Cost Paid: $totalCost credits');
    
    final bool useClonedVoice = (voice == 'cloned');
    debugPrint('[VoiceMap] selected_ui_voice_label=$voice');
    debugPrint('[VoiceMap] resolved_internal_voice=${_resolveOpenAiVoice(voice)}');
    debugPrint('[VoiceMap] useClonedVoice=$useClonedVoice');
    if (!useClonedVoice) debugPrint('[VoiceProvider] normal_story_provider=pollinations');

    try {
      final storyData = await UnifiedEngine.generateStory(requestData, saveToLibrary: saveToLibrary);


      final scenes = List<Map<String, dynamic>>.from(storyData['scenes'] ?? []);
      final storyId = storyData['id'];

      if (saveToLibrary && storyId != null && scenes.isNotEmpty) {
        debugPrint('[UseCase] بدء توليد الصوت...');
        final isCloned = voice == 'cloned';
        final elevenLabsKey = dotenv.env['ELEVENLABS_API_KEY'] ?? '';
        final prefs = await SharedPreferences.getInstance();
        final clonedVoiceId = prefs.getString('cloned_voice_id');

        // توليد الصوت لكل مشهد بالتسلسل (بدون Pool)
        for (int i = 0; i < scenes.length; i++) {
          final scene = scenes[i];
          final text = scene['text'] ?? '';
          if (text.isEmpty) continue;

          Uint8List? audioBytes;

          if (isCloned && elevenLabsKey.isNotEmpty && clonedVoiceId != null && clonedVoiceId.isNotEmpty) {
            final url = 'https://api.elevenlabs.io/v1/text-to-speech/$clonedVoiceId';
            final headers = {'xi-api-key': elevenLabsKey, 'Content-Type': 'application/json'};
            final body = jsonEncode({
              "text": text,
              "model_id": "eleven_multilingual_v2",
              "voice_settings": {"stability": 0.5, "similarity_boost": 0.75}
            });
            audioBytes = await _fetchAudioWithRetry(url, headers, body);
          } else {
            // الصوت العادي: Pollinations TTS أولاً (قرار معتمد 2026-04-15)
            final resolvedVoice = _resolveOpenAiVoice(voice);
            final encodedText = Uri.encodeComponent(text);
            final audioKey = dotenv.env['POLLINATIONS_AUDIO_API_KEY'] ?? '';
            final pollinationsUrl = 'https://gen.pollinations.ai/audio/$encodedText'
                '?voice=$resolvedVoice&model=elevenlabs';

            final Map<String, String> headers = {
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)',
            };
            if (audioKey.isNotEmpty) {
              headers['Authorization'] = 'Bearer $audioKey';
            }

            debugPrint('[AudioProvider] Using Pollinations TTS — voice=$resolvedVoice, hasKey=${audioKey.isNotEmpty}');
            if (i > 0) await Future.delayed(const Duration(milliseconds: 800));
            audioBytes = await _fetchAudioWithRetry(pollinationsUrl, headers);

            if (audioBytes == null || audioBytes.length <= 500) {
              // آخر مسار نجاة: Google TTS إذا فشل Pollinations
              debugPrint('[AudioProvider] Pollinations failed → Google TTS Fallback');
              final cleanText = text.replaceAll('\n', ' ').trim();
              final fallbackUrl = 'https://translate.google.com/translate_tts?ie=UTF-8&client=tw-ob&tl=ar&q=${Uri.encodeQueryComponent(cleanText)}';
              if (i > 0) await Future.delayed(const Duration(seconds: 2));
              audioBytes = await _fetchAudioWithRetry(fallbackUrl, {});
            }
          }

          // التحقق من أن النتيجة هي ملف MP3 فعلاً (وليس نص مثل رسائل الخطأ)
          if (audioBytes != null && audioBytes.length > 500) {
            try {
              // رفع الصوت إلى Supabase Storage
              final fileName = '$userId/${storyId}_scene_$i.mp3';
              await Supabase.instance.client.storage
                  .from('story_audio')
                  .uploadBinary(
                    fileName, 
                    audioBytes,
                    fileOptions: const FileOptions(contentType: 'audio/mpeg'),
                  );
              final audioUrl = Supabase.instance.client.storage
                  .from('story_audio')
                  .getPublicUrl(fileName);
              scenes[i]['audio_url'] = audioUrl;
              debugPrint('[UseCase] ✅ صوت المشهد $i محفوظ: $audioUrl');
            } catch (storageError) {
              debugPrint('[UseCase] ⚠️ خطأ رفع صوت المشهد $i: $storageError');
              scenes[i]['audio_url'] = null;
            }
          } else {
            scenes[i]['audio_url'] = null;
          }
        }

        // حفظ voice_type وروابط الصوت في قاعدة البيانات
        try {
          await Supabase.instance.client
              .from('stories')
              .update({
                'scenes_json': scenes,
                'voice_type': voice ?? 'nova',
              })
              .eq('id', storyId);
          debugPrint('[UseCase] ✅ تم حفظ روابط الصوت ونوع الصوت في قاعدة البيانات');
        } catch (dbErr) {
          debugPrint('[UseCase] ⚠️ خطأ تحديث مسارات الصوت: $dbErr');
        }

        storyData['scenes'] = scenes;
      }

      // ====== الخصم النهائي بعد نجاح النص + الصور + الصوت ======
      final deductStoryId = storyId?.toString() ?? '';

      // حماية الخصم المزدوج: إذا خُصمت هذه القصة مسبقاً في نفس الجلسة → تجاوز
      if (deductStoryId.isNotEmpty && _deductedStoryIds.contains(deductStoryId)) {
        debugPrint('[CREDIT] ⚠️ الخصم تم مسبقاً للقصة ($deductStoryId) — تجاوز');
      } else {
        try {
          // قراءة الرصيد الحالي من Supabase قبل الخصم
          final beforeRes = await Supabase.instance.client
              .from('profiles')
              .select('credits')
              .eq('user_id', userId)
              .single();
          final beforeCredits = beforeRes['credits'] as int? ?? 0;

          debugPrint('[CREDIT] before=$beforeCredits');
          debugPrint('[CREDIT] deducted=$totalCost');

          await Supabase.instance.client.rpc('deduct_credits', params: {
            'p_user_id': userId,
            'p_amount': totalCost,
            'p_reason': 'توليد قصة سينمائية',
          });

          // قراءة الرصيد الجديد من Supabase بعد الخصم للتأكيد
          final afterRes = await Supabase.instance.client
              .from('profiles')
              .select('credits')
              .eq('user_id', userId)
              .single();
          final afterCredits = afterRes['credits'] as int? ?? 0;

          debugPrint('[CREDIT] after=$afterCredits');
          debugPrint('[CREDIT] story_id=$deductStoryId');

          // تسجيل القصة لمنع أي خصم مزدوج لاحق
          if (deductStoryId.isNotEmpty) _deductedStoryIds.add(deductStoryId);

        } catch (deductErr) {
          if (saveToLibrary && storyId != null) {
            await Supabase.instance.client.from('stories').delete().eq('id', storyId);
            debugPrint('[UseCase] ❌ فشل الخصم، تم حذف القصة $storyId للتراجع.');
          }
          throw Exception('فشلت عملية الخصم، تم التراجع وحذف القصة: $deductErr');
        }
      }

      debugPrint('[UseCase] اكتمل التوليد — عدد المشاهد: ${scenes.length}');
      return storyData;
    } catch (e, stack) {
      debugPrint('[UseCase] خطأ في التوليد: $e\n$stack');
      rethrow;
    } finally {
      _isGenerating = false;
    }
  }
}



============================================================
FILE: D:\Hikayati.com\lib\application\use_cases\unlock_public_story_use_case.dart
============================================================

import 'package:hikayati/core/network/supabase_service.dart';

class UnlockPublicStoryUseCase {
  final _client = SupabaseService.client;

  Future<void> execute(Map<String, dynamic> story) async {
    final price = story['price_credits'] as int;
    final storyId = story['id'];
    final userId = _client.auth.currentUser?.id;

    if (userId == null) {
      throw Exception('يجب تسجيل الدخول لإتمام عملية الشراء.');
    }

    try {
      // 1. الخصم
      await SupabaseService.deductCredits(
        price,
        'Purchase public story: $storyId',
      );

      // 2. تسجيل الشراء
      await _client.from('purchases').insert({
        'user_id': userId,
        'story_id': storyId,
        'unlock_type': 'access',
        'credits_paid': price,
      });
    } catch (e) {
      if (e.toString().contains('Insufficient credits')) {
        throw Exception('رصيدك غير كافٍ. يرجى شحن الجواهر.');
      }
      throw Exception('فشلت عملية فتح القصة: $e');
    }
  }
}



============================================================
FILE: D:\Hikayati.com\lib\core\network\supabase_service.dart
============================================================

import 'package:flutter/foundation.dart';
import 'package:supabase_flutter/supabase_flutter.dart';
import 'package:flutter_dotenv/flutter_dotenv.dart';

class SupabaseService {
  static Future<void> initialize() async {
    final supabaseUrl = dotenv.env['SUPABASE_URL'];
    final supabaseAnonKey = dotenv.env['SUPABASE_ANON_KEY'];

    if (supabaseUrl == null || supabaseAnonKey == null) {
      throw Exception('أمر خطير: لم يتم العثور على مفاتيح Supabase في ملف .env!');
    }

    await Supabase.initialize(
      url: supabaseUrl,
      anonKey: supabaseAnonKey,
    );
  }

  static SupabaseClient get client => Supabase.instance.client;

  /// Deducts credits securely using the RPC function `deduct_credits`.
  /// Returns `true` if successful, or throws an exception if insufficient credits.
  static Future<bool> deductCredits(int amount, String reason) async {
    final userId = client.auth.currentUser?.id;
    if (userId == null) {
      throw Exception('يجب تسجيل الدخول لإتمام العملية');
    }

    try {
      final response = await client.rpc('deduct_credits', params: {
        'p_user_id': userId,
        'p_amount': amount,
        'p_reason': reason,
      });
      // الدالة في قاعدة البيانات ترجع boolean مباشرة وليس Map
      return response == true;
    } catch (e) {
      if (e.toString().contains('Insufficient credits')) {
        throw Exception('رصيدك غير كافٍ. يرجى زيارة المتجر لشحن الرصيد.');
      }
      throw Exception('فشلت عملية الخصم: $e');
    }
  }
  /// يتحقق من وجود صف للمستخدم في جدول profiles.
  /// إذا لم يكن موجوداً يُنشئه تلقائياً (يمنع خطأ stories_user_id_fkey).
  static Future<void> ensureProfileExists(String userId) async {
    try {
      final existing = await client
          .from('profiles')
          .select('user_id')
          .eq('user_id', userId)
          .maybeSingle();

      if (existing == null) {
        await client.from('profiles').insert({
          'user_id': userId,
          'credits': 100,
          'language': 'ar',
        });
        debugPrint('[SupabaseService] ✅ تم إنشاء Profile جديد للمستخدم: $userId');
      } else {
        debugPrint('[SupabaseService] Profile موجود مسبقاً — لا حاجة للإنشاء.');
      }
    } catch (e) {
      // لا نرمي الخطأ — نستمر في الحفظ ونسمح لـ FK يحسم الوضع
      debugPrint('[SupabaseService] ⚠️ خطأ في ensureProfileExists: $e');
    }
  }
}



============================================================
FILE: D:\Hikayati.com\supabase_schema.sql
============================================================

-- Schema required to build the backend of Hakawati App

-- 1. Profiles Table
CREATE TABLE public.profiles (
  user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  name TEXT,
  credits INTEGER DEFAULT 100,
  language VARCHAR(10) DEFAULT 'ar',
  avatar_data JSONB,
  voice_clone_enabled BOOLEAN DEFAULT false,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

-- Enable RLS
ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can insert their own profile." ON public.profiles FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own profile." ON public.profiles FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "Users can view own profile." ON public.profiles FOR SELECT USING (auth.uid() = user_id);

-- 2. Stories Table
CREATE TABLE public.stories (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES public.profiles(user_id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  language VARCHAR(10) DEFAULT 'ar',
  scenes_json JSONB,
  cover_image TEXT,
  is_public BOOLEAN DEFAULT false,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

-- Enable RLS
ALTER TABLE public.stories ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can create stories." ON public.stories FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own stories." ON public.stories FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "Users can view own stories." ON public.stories FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Public stories are viewable by everyone." ON public.stories FOR SELECT USING (is_public = true);

-- 3. Public Stories Table (Pre-made stories library)
CREATE TABLE public.public_stories (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  title TEXT NOT NULL,
  summary TEXT,
  cover TEXT,
  price_credits INTEGER DEFAULT 10,
  category VARCHAR(50),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

-- Enable RLS
ALTER TABLE public.public_stories ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public stories are viewable by everyone." ON public.public_stories FOR SELECT USING (true);
-- Insert/Update managed by Admin Dashboard, omit generic policies here.

-- 4. Purchases Table
CREATE TABLE public.purchases (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES public.profiles(user_id) ON DELETE CASCADE,
  story_id UUID,
  unlock_type VARCHAR(20), -- 'creation', 'access', 'premium'
  credits_paid INTEGER NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

-- Enable RLS
ALTER TABLE public.purchases ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own purchases." ON public.purchases FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can create purchases." ON public.purchases FOR INSERT WITH CHECK (auth.uid() = user_id);

-- Storage Buckets Configuration (Needs manual setup in dashboard or via API extensions)
-- 1. 'covers' -> for story covers.
-- 2. 'scenes' -> for generated scene images.
-- 3. 'audio' -> for narrative audios.

-- RPC Function: Securely Deduct Credits
CREATE OR REPLACE FUNCTION public.deduct_credits(
    p_user_id UUID,
    p_amount INTEGER,
    p_reason VARCHAR
)
RETURNS JSONB
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
    current_credits INT;
    new_credits INT;
BEGIN
    -- Get current credits with row-level lock
    SELECT credits INTO current_credits
    FROM public.profiles
    WHERE user_id = p_user_id
    FOR UPDATE;

    IF current_credits IS NULL THEN
        RAISE EXCEPTION 'User not found';
    END IF;

    IF current_credits < p_amount THEN
        RAISE EXCEPTION 'Insufficient credits';
    END IF;

    new_credits := current_credits - p_amount;

    -- Update the profile credits
    UPDATE public.profiles
    SET credits = new_credits
    WHERE user_id = p_user_id;

    -- Return JSON result
    RETURN json_build_object(
        'success', true,
        'remaining_credits', new_credits,
        'deducted', p_amount,
        'reason', p_reason
    );
END;
$$;

